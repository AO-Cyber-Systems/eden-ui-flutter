name: eden_lints
description: Dev-only custom_lint rules enforcing the mechanical half of the Eden design rules.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  analyzer: ">=6.0.0 <9.0.0"
  custom_lint_builder: ">=0.6.0 <0.8.0"

dev_dependencies:
  test: ^1.24.0
