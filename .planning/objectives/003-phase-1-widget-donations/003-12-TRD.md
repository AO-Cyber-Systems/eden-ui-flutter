---
objective: 003-phase-1-widget-donations
trd: "003-12"
type: standard
wave: 3
depends_on: []
files_modified:
  - lib/src/widgets/eden_ai/eden_ai_collapsible_section.dart
  - test/widgets/eden_ai/eden_ai_collapsible_section_test.dart
  - test/widgets/eden_ai/_fixtures/eden_ai_collapsible_section_fixtures.dart
  - lib/dev_app/screens/misc_screen.dart
  - lib/eden_ui.dart
co_modified_files:
  - lib/eden_ui.dart
autonomous: true
must_haves:
  - "EdenAiCollapsibleSection renders an outlined Container with header (icon + title + chevron) + animated body (AnimatedCrossFade, 150ms)"
  - "Default icon Icons.auto_awesome colored gold (0xFFD4A853); override-able via icon parameter"
  - "defaultExpanded parameter (default true) sets initial state"
  - "Tap header to toggle expanded/collapsed; chevron rotates 0deg → -90deg (-0.25 turns) on collapse"
  - "Child renders inside 12px horizontal padding + 12px bottom padding when expanded"
  - "iPhone-narrow safe"
---

<objective>
Port `trades-flutter/lib/shared/widgets/ai_collapsible_section.dart` to eden-ui-flutter as `EdenAiCollapsibleSection`. Cross-vertical AI-section wrapper used by domain insight surfaces (operations dashboards, finance summaries, dispatch panels).

Donor file is 105 LOC; library version expected ~120 LOC after dartdoc.

This is **independent** of 003-10 (no models dependency) — it's a pure visual wrapper. Runs in parallel with 003-10/11/13.
</objective>

<context>
**Locked design inputs (do NOT revisit):**
- Donor: `/Users/markemerson/Source/AOCyber-Trades/trades-flutter/lib/shared/widgets/ai_collapsible_section.dart` (105 LOC).
- StatefulWidget — owns `_expanded` internally. Donor's `defaultExpanded` parameter sets initial state.
- Material 3 outlined card style (surface background + outlineVariant border, 8px radius).
- Gold accent icon (0xFFD4A853) — matches the AI chat FAB color elsewhere in the donor tree.

**Why no `eden_ai_models.dart` dependency:**
- This widget takes `String title` + `IconData icon` + `Widget child` — no AI-typed parameters.
- Used as a visual wrapper around insight surfaces, not a renderer of insight data.
</context>

<gotchas>
- **`AnimatedCrossFade` between child and `SizedBox.shrink()`** — preserve. Duration 150ms (faster than the panel's 200ms expand animation; matches snappy collapse UX).
- **`AnimatedRotation` for the chevron** — `turns: _expanded ? 0 : -0.25`. -0.25 turns means -90 degrees (pointing left). Preserve.
- **Border radius transition:** when collapsed, the InkWell border radius is fully rounded (top + bottom 8px). When expanded, the top stays 8px but bottom is Radius.zero so the chevron stays visually attached. Preserve.
- **Default `icon: Icons.auto_awesome`** — sparkle icon. Color override is HARD-CODED 0xFFD4A853 (gold) — NOT theme-dependent. Preserve.
- **iPhone-narrow safety:** Row inside InkWell has icon + Text + Spacer + AnimatedRotation. The Text could grow long; wrap in `Flexible` for safety.
</gotchas>

## Test list (write FIRST)

**Happy path (defaultExpanded=true)**
- `EdenAiCollapsibleSection(title: 'Operations Insights', child: const Text('Insights body'))` → finds 'Operations Insights', finds 'Insights body' (child is visible), finds `Icons.auto_awesome`.

**defaultExpanded=false**
- `EdenAiCollapsibleSection(title: 'X', defaultExpanded: false, child: const Text('hidden'))` → finds 'X' but 'hidden' is NOT visible (AnimatedCrossFade is in secondChild state).

**Toggle**
- Tap header in expanded state → 'hidden' becomes NOT visible after pumpAndSettle.
- Tap header in collapsed state → 'hidden' becomes visible.

**Icon override**
- `EdenAiCollapsibleSection(title: 'X', icon: Icons.insights, child: const Text('Y'))` → finds Icons.insights, does NOT find Icons.auto_awesome.

**Chevron rotation**
- After tapping to collapse → AnimatedRotation has turns -0.25 (chevron points left).
- After tapping to expand → AnimatedRotation has turns 0 (chevron points down).

**Edge / responsive**
- Renders inside `SizedBox(width: 390)` with a 60-char title → no overflow (Flexible wraps the title).
- Renders inside `SizedBox(width: 280)` (sidebar width) → no overflow.

<tasks>

<task type="auto" tdd="true">
<name>Task 1 — Fixture + RED → GREEN: EdenAiCollapsibleSection</name>
<files>
test/widgets/eden_ai/_fixtures/eden_ai_collapsible_section_fixtures.dart
test/widgets/eden_ai/eden_ai_collapsible_section_test.dart
lib/src/widgets/eden_ai/eden_ai_collapsible_section.dart
lib/dev_app/screens/misc_screen.dart
lib/eden_ui.dart
</files>
<action>
**Fixture (hand-built, no LLM-generated test data).** Header line: `// Do NOT regenerate via LLM — hand-built fixtures for EdenAiCollapsibleSection.`

`EdenAiCollapsibleSectionFixtures` exposes:
- `const happyTitle = 'Operations Insights'`
- `const happyChildText = 'This is the insight body content'`
- `const longTitle = 'A very long section title that should ellipsize gracefully on narrow viewports — 60 chars'`
- `Widget wrap(Widget child) => MaterialApp(home: Scaffold(body: child));`

**RED phase.** Write 9 testWidgets one at a time (1 happy + 1 defaultExpanded false + 2 toggle + 1 icon override + 2 chevron rotation + 2 responsive = 9). Each MUST fail before GREEN. Commit pattern: `test(003-12): add failing tests for EdenAiCollapsibleSection`.

**GREEN phase.** Implement `lib/src/widgets/eden_ai/eden_ai_collapsible_section.dart` from donor. Read donor first.

Public API:
```dart
class EdenAiCollapsibleSection extends StatefulWidget {
  const EdenAiCollapsibleSection({
    super.key,
    required this.title,
    required this.child,
    this.defaultExpanded = true,
    this.icon = Icons.auto_awesome,
  });
  final String title;
  final Widget child;
  final bool defaultExpanded;
  final IconData icon;
  @override
  State<EdenAiCollapsibleSection> createState() => _EdenAiCollapsibleSectionState();
}
```

Implementation deltas vs donor:
- Class rename: `AiCollapsibleSection` → `EdenAiCollapsibleSection`.
- State rename: `_AiCollapsibleSectionState` → `_EdenAiCollapsibleSectionState`.
- iPhone-narrow safety: wrap title `Text` in `Flexible(child: Text(..., overflow: TextOverflow.ellipsis, maxLines: 1))`.
- Preserve `AnimatedCrossFade` + `AnimatedRotation`.
- Dartdoc: cross-vertical examples (operations insights, finance summary, dispatch panel).

**Catalog entry.** Append to `lib/dev_app/screens/misc_screen.dart` a section titled "EdenAiCollapsibleSection — Phase 1 (objective 003)":
- Default (expanded) with child = `Text('Hello insights')`.
- defaultExpanded=false demo.
- Custom icon (Icons.insights) demo.
- Long-title demo to show ellipsis.

**Export.** Append to `lib/eden_ui.dart` under the `// Phase 1 — AI surface (objective 003)` section header (which 003-10 created):
```dart
export 'src/widgets/eden_ai/eden_ai_collapsible_section.dart';
```

If 003-10 hasn't created the section header yet (parallel race within Wave 3, which shouldn't happen because 003-12 doesn't depend on 003-10 but the orchestrator may parallelize them), create it.

Commits (sequential): `test(003-12)` (RED set) → `feat(003-12): implement EdenAiCollapsibleSection (Phase 1)` → `docs(003-12): add EdenAiCollapsibleSection to misc_screen catalog`.

# CRITICAL: NO `import 'package:trades/...'`.
# CRITICAL: `lib/eden_ui.dart` edit MUST be serialized within Wave 3.
# GOTCHA: Gold accent color 0xFFD4A853 is HARD-CODED, NOT theme-dependent. Preserve for visual parity with the donor's AI chat FAB.
# GOTCHA: AnimatedRotation turns: `-0.25` (NOT positive 0.25) — chevron rotates LEFT, not right.
# PATTERN: Same shape as Wave 2 stateful widgets — single-task RED → GREEN → docs.
</action>
<verify>
- `flutter test test/widgets/eden_ai/eden_ai_collapsible_section_test.dart` → 9/9 pass.
- `flutter analyze` → 0 issues.
- `grep -c "eden_ai_collapsible_section" lib/eden_ui.dart` → 1.
- `flutter test` (full suite) → no regressions.
- Visual: `just dev-ui` Misc screen → verify EdenAiCollapsibleSection demo with toggle + icon override + long title.
</verify>
<done>
EdenAiCollapsibleSection shipped under Phase 1 — AI surface section. Tests cover defaultExpanded states + toggle + icon override + chevron rotation + iPhone-narrow safety.
</done>
</task>

</tasks>

<verification_commands>
- `flutter test test/widgets/eden_ai/eden_ai_collapsible_section_test.dart`
- `flutter test` (full suite — must not regress)
- `flutter analyze`
- Visual: `just dev-ui` Misc screen → verify EdenAiCollapsibleSection demo
</verification_commands>
