---
objective: 003-phase-1-widget-donations
trd: "003-09"
type: standard
wave: 2
depends_on: []
files_modified:
  - lib/src/widgets/eden_placeholder_page.dart
  - test/widgets/eden_placeholder_page_test.dart
  - test/widgets/_fixtures/eden_placeholder_page_fixtures.dart
  - lib/dev_app/screens/misc_screen.dart
  - lib/eden_ui.dart
co_modified_files:
  - lib/eden_ui.dart
autonomous: true
must_haves:
  - "EdenPlaceholderPage renders Scaffold with AppBar(title) + centered icon (64px) + heading + 'Coming soon' subtitle"
  - "Optional subtitle override (default 'Coming soon')"
  - "Optional onAction button (e.g. 'Notify me when ready') wired via callback"
  - "iPhone-narrow safe; centered layout doesn't overflow at 390pt"
---

<objective>
Port `trades-flutter/lib/shared/widgets/placeholder_page.dart` to eden-ui-flutter as `EdenPlaceholderPage`. Cross-vertical "coming soon" route screen used by all verticals during feature-flagged rollouts (trades, salon, medical, legal, gov, retail, fuel — every vertical has placeholder routes during incremental UI builds).

Donor file is 46 LOC; library version expected ~70 LOC after adding optional subtitle + onAction support + dartdoc.

**Enhancement over donor:** library version adds optional `subtitle` (default 'Coming soon') and optional `onAction` + `actionLabel` (default null = no button). Donor is rigid; library is configurable for cross-vertical use cases like 'Pending review', 'Migration in progress', 'Notify me'.
</objective>

<context>
**Locked design inputs (do NOT revisit):**
- Donor: `/Users/markemerson/Source/AOCyber-Trades/trades-flutter/lib/shared/widgets/placeholder_page.dart` (46 LOC).
- Library wraps Scaffold + AppBar — this is a PAGE widget, not a partial. Lives in `lib/src/widgets/` (not `lib/src/pages/`) because it's a reusable building block, not a vertical-specific page.
- Visual structure: 64px icon at alpha 0.5 + 16px gap + `headlineSmall` title text + 8px gap + bodyMedium muted subtitle.

**Cross-vertical examples:**
- Trades feature flag: route stub for `/admin/agent-builder` shows 'Coming soon'.
- Salon migration: route stub for `/admin/customizations` shows 'Migration in progress'.
- Medical phase-out: route stub shows 'Notify me when ready' button.
</context>

<gotchas>
- **`Scaffold + AppBar(title)`** — this widget OWNS the scaffold; consumers must NOT wrap it in another Scaffold (that's a Flutter anti-pattern). Document.
- **Icon alpha at 0.5** — donor uses `theme.colorScheme.onSurfaceVariant.withValues(alpha: 0.5)` for the 64px icon. Preserve.
- **Default subtitle 'Coming soon'** — if `subtitle: null` is passed, render nothing for the subtitle (not even the SizedBox above it). Test both default and null.
- **`onAction` adds a `FilledButton` below the subtitle** with 24px gap above it (mirror EdenErrorPage pattern). Only renders when `onAction != null && actionLabel != null`.
- **iPhone-narrow safety:** Column(mainAxisSize.min) is centered; no risk of overflow at 390pt. Verify anyway.
</gotchas>

## Test list (write FIRST)

**Happy path (donor parity)**
- `EdenPlaceholderPage(title: 'Reports', icon: Icons.bar_chart)` → AppBar title reads 'Reports', body shows Icons.bar_chart at 64px, headline reads 'Reports', subtitle reads 'Coming soon'.
- 'Coming soon' is the default subtitle.

**Optional subtitle override**
- `EdenPlaceholderPage(title: 'Reports', icon: Icons.bar_chart, subtitle: 'Pending review')` → subtitle text reads 'Pending review'.
- `EdenPlaceholderPage(title: 'Reports', icon: Icons.bar_chart, subtitle: null)` → NO subtitle Text rendered (find.text('Coming soon') findsNothing AND find.byType(Text).count for subtitle is verified by widget hierarchy assertion).

**Optional action button**
- `EdenPlaceholderPage(title: 'Reports', icon: Icons.bar_chart, onAction: () => tapped = true, actionLabel: 'Notify me')` → `FilledButton` rendered with text 'Notify me'; tap fires the callback.
- `onAction: null` → no `FilledButton` rendered.
- `onAction: () => {}, actionLabel: null` → no button (both must be non-null).

**Edge / responsive**
- Renders at `SizedBox(width: 390, height: 700)` (iPhone-narrow baseline) without overflow.

<tasks>

<task type="auto" tdd="true">
<name>Task 1 — Fixture + RED → GREEN: EdenPlaceholderPage</name>
<files>
test/widgets/_fixtures/eden_placeholder_page_fixtures.dart
test/widgets/eden_placeholder_page_test.dart
lib/src/widgets/eden_placeholder_page.dart
lib/dev_app/screens/misc_screen.dart
lib/eden_ui.dart
</files>
<action>
**Fixture (hand-built, no LLM-generated test data).** Header line: `// Do NOT regenerate via LLM — hand-built fixtures for EdenPlaceholderPage.`

`EdenPlaceholderPageFixtures` exposes:
- `const happyPathTitle = 'Reports'`
- `const happyPathIcon = Icons.bar_chart`
- `const customSubtitle = 'Pending review'`
- `const customActionLabel = 'Notify me when ready'`
- `Widget wrapNoMaterialApp(Widget child) => MaterialApp(home: child);` — note: this widget OWNS Scaffold, so the test wrapper is just `MaterialApp(home: ...)` not `MaterialApp(home: Scaffold(body: ...))`. Document.

**RED phase.** Write 7 testWidgets one at a time (2 happy-path + 2 subtitle + 2 action + 1 responsive = 7). Each MUST fail before GREEN. Commit pattern: `test(003-09): add failing tests for EdenPlaceholderPage`.

**GREEN phase.** Implement `lib/src/widgets/eden_placeholder_page.dart` from donor. Read donor first.

Public API:
```dart
class EdenPlaceholderPage extends StatelessWidget {
  const EdenPlaceholderPage({
    super.key,
    required this.title,
    required this.icon,
    this.subtitle = 'Coming soon',
    this.onAction,
    this.actionLabel,
  });
  final String title;
  final IconData icon;
  final String? subtitle;
  final VoidCallback? onAction;
  final String? actionLabel;
}
```

Implementation deltas vs donor:
- Class rename: `PlaceholderPage` → `EdenPlaceholderPage`.
- New optional `subtitle` parameter (defaults to 'Coming soon'; null = hide).
- New optional `onAction` + `actionLabel` parameters (both must be non-null to render button).
- Dartdoc: explain ownership of Scaffold (don't double-wrap); cross-vertical examples.

**Catalog entry.** Append to `lib/dev_app/screens/misc_screen.dart` a section titled "EdenPlaceholderPage — Phase 1 (objective 003)":
- Embedded preview using `SizedBox(height: 400, child: EdenPlaceholderPage(title: 'Reports', icon: Icons.bar_chart))` (default behavior).
- Variant with custom subtitle 'Pending review'.
- Variant with action button 'Notify me' (logs to debugPrint on tap).

**Export.** Append to `lib/eden_ui.dart` under the `// Phase 1 — Dashboard composites (objective 003)` section header:
```dart
export 'src/widgets/eden_placeholder_page.dart';
```

Commits (sequential): `test(003-09)` (RED set) → `feat(003-09): implement EdenPlaceholderPage with optional subtitle + action (Phase 1)` → `docs(003-09): add EdenPlaceholderPage to misc_screen catalog`.

# CRITICAL: `lib/eden_ui.dart` edit MUST be serialized within Wave 2.
# GOTCHA: This widget OWNS Scaffold — consumers must NOT wrap it in another Scaffold. Document.
# GOTCHA: `subtitle: null` hides the subtitle Text + its surrounding spacing. Distinct from default ('Coming soon' string).
# PATTERN: Catalog entry uses `SizedBox(height: 400, child: ...)` to embed a Scaffold-owning page in a list-style catalog screen.
</action>
<verify>
- `flutter test test/widgets/eden_placeholder_page_test.dart` → 7/7 pass.
- `flutter analyze` → 0 issues.
- `grep -c "eden_placeholder_page" lib/eden_ui.dart` → 1.
- `flutter test` (full suite) → no regressions.
- Visual: `just dev-ui` Misc screen → verify EdenPlaceholderPage demo with 3 variants.
</verify>
<done>
EdenPlaceholderPage shipped under Phase 1 Dashboard composites export section. Tests cover donor-parity happy path + subtitle override + action button + iPhone-narrow safety.
</done>
</task>

</tasks>

<verification_commands>
- `flutter test test/widgets/eden_placeholder_page_test.dart`
- `flutter test` (full suite — must not regress)
- `flutter analyze`
- Visual: `just dev-ui` Misc screen → verify EdenPlaceholderPage demo
</verification_commands>
