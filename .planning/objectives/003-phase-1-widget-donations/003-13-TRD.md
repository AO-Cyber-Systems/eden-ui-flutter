---
objective: 003-phase-1-widget-donations
trd: "003-13"
type: standard
wave: 3
depends_on: ["003-10"]
files_modified:
  - lib/src/widgets/eden_ai/eden_persona_selector.dart
  - test/widgets/eden_ai/eden_persona_selector_test.dart
  - lib/dev_app/screens/misc_screen.dart
  - lib/eden_ui.dart
co_modified_files:
  - lib/eden_ui.dart
autonomous: true
must_haves:
  - "EdenPersonaSelector renders a colored pill showing active persona icon + displayLabel + dropdown arrow"
  - "Tapping the pill opens a PopupMenu listing all 4 EdenAiPersona values; tapping one fires onChanged callback"
  - "Library is Riverpod-free: consumer passes activePersona + onChanged; library does NOT read/write a provider"
  - "Pill background uses activePersona.color at alpha 0.15; pill text + icon use activePersona.color"
  - "Active persona in dropdown shows check icon + bold weight; inactive personas show normal weight"
  - "iPhone-narrow safe; pill renders in chat header without overflow"
---

<objective>
Port `trades-flutter/lib/shared/widgets/persona_selector.dart` to eden-ui-flutter as `EdenPersonaSelector`. Cross-vertical AI persona switcher used in chat headers + persona-aware insight surfaces.

Donor file is 93 LOC; library version expected ~115 LOC after Riverpod strip + dartdoc.

**Critical decoupling:** donor is a `ConsumerWidget` that reads `activePersonaProvider` and writes to it via `ref.read(...notifier).state = persona`. The library version is a pure `StatelessWidget` with `EdenAiPersona activePersona` + `ValueChanged<EdenAiPersona> onChanged` parameters — NO Riverpod import. Per OBJECTIVE.md Constraint 4 ("transport-agnostic + Riverpod-free"). Consumers wire this up via Riverpod themselves at the call site.
</objective>

<context>
**Locked design inputs (do NOT revisit):**
- Donor: `/Users/markemerson/Source/AOCyber-Trades/trades-flutter/lib/shared/widgets/persona_selector.dart` (93 LOC).
- 4 personas come from `eden_ai_models.dart` (003-10): `fieldTech`, `finance`, `operations`, `executive`.
- Pill shape: 6h × 2v padding, alpha-0.15 background of persona color, 4px border radius.
- Dropdown opens with `offset: Offset(0, 32)` (below the pill).
- Donor uses `PopupMenuButton<AiPersona>` — preserve, with type renamed to `EdenAiPersona`.

**Why this requires 003-10 (depends_on):**
- Imports `EdenAiPersona` enum from `eden_ai_models.dart`.
- `EdenAiPersona` exposes `displayLabel`, `icon`, `color` getters used in the pill rendering.
</context>

<gotchas>
- **Riverpod stripped:** the donor's `ref.watch(activePersonaProvider)` and `ref.read(activePersonaProvider.notifier).state = persona` patterns are REMOVED. Library version exposes `activePersona` + `onChanged` props instead. This is non-negotiable per OBJECTIVE.md Constraint 4.
- **`PopupMenuButton.itemBuilder`** receives all 4 personas via `AiPersona.values.map(...)`. With the rename to `EdenAiPersona`, use `EdenAiPersona.values.map(...)`.
- **Active persona styling:** active persona in dropdown shows trailing check icon + `FontWeight.w600`; inactive shows `FontWeight.normal` + no check. Preserve.
- **`mainAxisSize: MainAxisSize.min` on pill Row** — pill auto-sizes to content. iPhone-narrow safe by design.
- **`offset: Offset(0, 32)`** — dropdown opens 32px below the pill (clears the pill's vertical extent). Preserve.
</gotchas>

## Test list (write FIRST)

**Happy path (default persona)**
- `EdenPersonaSelector(activePersona: EdenAiPersona.operations, onChanged: (p) => last = p)` → finds 'Operations' text in the pill, finds `Icons.dashboard` icon, finds `Icons.arrow_drop_down`. Pill background uses operations color (0xFFF59E0B) at alpha 0.15.

**Persona switching**
- `EdenPersonaSelector(activePersona: EdenAiPersona.finance, ...)` → pill displays 'Finance' + Icons.account_balance + green (0xFF22C55E).
- `EdenPersonaSelector(activePersona: EdenAiPersona.fieldTech, ...)` → 'Field Tech' + Icons.engineering + blue (0xFF3B82F6).
- `EdenPersonaSelector(activePersona: EdenAiPersona.executive, ...)` → 'Executive' + Icons.insights + purple (0xFF8B5CF6).

**Dropdown open**
- Tap the pill → PopupMenu opens. Use `await tester.tap(find.byType(EdenPersonaSelector))` then `await tester.pumpAndSettle()`. Assert all 4 personas' displayLabels appear in the open menu.

**Dropdown selection**
- Open menu, tap a persona → onChanged fires with that EdenAiPersona value.
- Captured value asserted: `expect(last, EdenAiPersona.finance)` after tapping the 'Finance' menu item.

**Active styling in dropdown**
- With `activePersona: EdenAiPersona.operations`, open menu → operations row has check icon visible + 'Operations' text style is bold (w600); the other 3 rows have NO check icon + normal weight.

**Edge / responsive**
- Renders inside a header Row at `SizedBox(width: 390)` (iPhone-narrow baseline) alongside other widgets → no overflow.

<tasks>

<task type="auto" tdd="true">
<name>Task 1 — Tests + GREEN: EdenPersonaSelector (Riverpod-stripped)</name>
<files>
test/widgets/eden_ai/eden_persona_selector_test.dart
lib/src/widgets/eden_ai/eden_persona_selector.dart
lib/dev_app/screens/misc_screen.dart
lib/eden_ui.dart
</files>
<action>
**Fixture reuse.** Use `EdenAiModelsFixtures` from 003-10. No new fixture file.

**RED phase.** Write 10 testWidgets one at a time (1 happy + 3 personas + 1 dropdown open + 1 selection + 1 active styling + 1 responsive + 2 misc shape = 10). Each MUST fail before GREEN. Commit pattern: `test(003-13): add failing tests for EdenPersonaSelector`.

Dropdown testing notes:
- `await tester.tap(find.byType(EdenPersonaSelector))` opens the popup.
- After `pumpAndSettle`, the popup items are findable via `find.text('Field Tech')` etc.
- To select an item: `await tester.tap(find.text('Finance').last)` (the `.last` distinguishes from the pill's display label if it happens to match).

**GREEN phase.** Implement `lib/src/widgets/eden_ai/eden_persona_selector.dart` from donor. Read donor first.

Public API:
```dart
class EdenPersonaSelector extends StatelessWidget {
  const EdenPersonaSelector({
    super.key,
    required this.activePersona,
    required this.onChanged,
    this.tooltip = 'Switch persona',
  });
  final EdenAiPersona activePersona;
  final ValueChanged<EdenAiPersona> onChanged;
  final String tooltip;
}
```

Implementation deltas vs donor:
- Class rename: `PersonaSelector` → `EdenPersonaSelector`.
- Base class change: `ConsumerWidget` → `StatelessWidget`.
- Constructor signature: `activePersona` + `onChanged` replace the Riverpod `ref.watch`/`ref.read` calls.
- Remove imports: `package:flutter_riverpod/flutter_riverpod.dart`, `package:trades/shared/providers/ai_providers.dart`.
- Add import: `import 'eden_ai_models.dart';` (relative).
- `onSelected: (persona) => onChanged(persona)` replaces `ref.read(activePersonaProvider.notifier).state = persona`.
- Optional `tooltip` parameter (default 'Switch persona') so consumers can localize.
- Dartdoc: explain Riverpod-free design + consumer-owned state.

**Catalog entry.** Append to `lib/dev_app/screens/misc_screen.dart` a section titled "EdenPersonaSelector — Phase 1 (objective 003)":
- StatefulBuilder with local `EdenAiPersona _persona = EdenAiPersona.operations;` + `EdenPersonaSelector(activePersona: _persona, onChanged: (p) => setState(() => _persona = p))`.
- Text below shows current selected persona.

**Export.** Append to `lib/eden_ui.dart` under the `// Phase 1 — AI surface (objective 003)` section header:
```dart
export 'src/widgets/eden_ai/eden_persona_selector.dart';
```

Commits (sequential): `test(003-13)` (RED set) → `feat(003-13): implement EdenPersonaSelector with Riverpod-free API (Phase 1)` → `docs(003-13): add EdenPersonaSelector to misc_screen catalog`.

# CRITICAL: NO `flutter_riverpod` import anywhere. Donor is Riverpod-coupled; library version is pure StatelessWidget with callback-driven state. Per OBJECTIVE.md Constraint 4.
# CRITICAL: NO `import 'package:trades/...'`.
# CRITICAL: `lib/eden_ui.dart` edit MUST be serialized within Wave 3.
# CRITICAL: `depends_on: ["003-10"]` — needs EdenAiPersona from foundation.
# GOTCHA: `EdenAiPersona.values.map(...)` instead of donor's `AiPersona.values.map(...)`.
# PATTERN: Riverpod stripping pattern matches Wave A's `EdenAuthenticatedImage` (which donor-was-Provider-coupled in trades-react; library-is-pure-Stateless).
</action>
<verify>
- `flutter test test/widgets/eden_ai/eden_persona_selector_test.dart` → 10/10 pass.
- `flutter analyze` → 0 issues.
- `grep -r "flutter_riverpod\|trades/shared/providers" lib/src/widgets/eden_ai/` → empty.
- `grep -c "eden_persona_selector" lib/eden_ui.dart` → 1.
- `flutter test` (full suite) → no regressions.
- Visual: `just dev-ui` Misc screen → verify EdenPersonaSelector demo with live persona swap.
</verify>
<done>
EdenPersonaSelector shipped under Phase 1 — AI surface section. Riverpod-free; consumer owns state via activePersona + onChanged. Tests cover all 4 personas + dropdown open + selection + active styling + iPhone-narrow safety.
</done>
</task>

</tasks>

<verification_commands>
- `flutter test test/widgets/eden_ai/eden_persona_selector_test.dart`
- `flutter test` (full suite — must not regress)
- `flutter analyze`
- `grep -r "flutter_riverpod" lib/` → MUST be empty (verifies Riverpod-free constraint)
- Visual: `just dev-ui` Misc screen → verify EdenPersonaSelector demo
</verification_commands>
