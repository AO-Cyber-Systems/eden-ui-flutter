---
objective: 003-phase-1-widget-donations
trd: "003-06"
type: standard
wave: 2
depends_on: []
files_modified:
  - lib/src/widgets/eden_activity_feed_item.dart
  - test/widgets/eden_activity_feed_item_test.dart
  - test/widgets/_fixtures/eden_activity_feed_item_fixtures.dart
  - lib/dev_app/screens/data_display_screen.dart
  - lib/eden_ui.dart
co_modified_files:
  - lib/eden_ui.dart
autonomous: true
must_haves:
  - "EdenActivityFeedItem renders an activity row with circular initials avatar + RichText(actor + action + entity) + timeAgo"
  - "Variant colors: success=green, warning=orange, danger=red, info=theme.colorScheme.primary"
  - "Library owns its own data class EdenActivityFeedItemData + enum EdenActivityVariant — NO import from trades/features/admin/domain"
  - "Optional onTap callback wraps the row in InkWell; null = static row"
  - "iPhone-narrow safe; actor/action/entity text wraps without overflow"
---

<objective>
Port `trades-flutter/lib/shared/widgets/activity_feed_item.dart` to eden-ui-flutter as `EdenActivityFeedItem`. Cross-vertical activity-log row used by trades (admin activity feed), salon (booking history), medical (chart audit log), legal (matter timeline), gov (audit trail), retail (transaction history).

Donor file is 110 LOC; library version expected ~140 LOC after defining the inlined data class + enum + dartdoc + iPhone-narrow safety pass.
</objective>

<context>
**Locked design inputs (do NOT revisit):**
- Donor: `/Users/markemerson/Source/AOCyber-Trades/trades-flutter/lib/shared/widgets/activity_feed_item.dart` (110 LOC).
- Donor imports `'package:trades/features/admin/domain/admin_stats_model.dart'` for `ActivityFeedItem` + `ActivityVariant` — this MUST NOT be reproduced in the library. The library inlines its own `EdenActivityFeedItemData` + `EdenActivityVariant` enum (per OBJECTIVE.md Constraint 5).
- Avatar shape: 36×36 circle, alpha-15 background, alpha-30 border (1.5px), centered initials text 13px bold colored by variant.

**Why inlining the data class is non-negotiable:**
- `eden-ui-flutter` cannot import from `trades-flutter` (trades-flutter is being absorbed; library is upstream of biz).
- Downstream consumers (eden-biz-flutter trades-vertical, eden-biz-flutter salon-vertical, etc.) can map their per-vertical activity models to `EdenActivityFeedItemData` at the call site.
</context>

<gotchas>
- **`actorInitials` is a plain `String`** in donor (e.g. 'JS' for 'John Smith') — consumer responsibility to compute initials. The library does NOT auto-compute from a full-name field. Document explicitly.
- **`RichText` with `TextSpan` hierarchy** — donor builds `actor + ' action ' + entity` as styled spans. Test must assert all three text fragments are findable via `find.text('John')`, `find.text(' deleted ')`, `find.text('Customer #123')` etc.
- **`onTap == null` path** — donor wraps in `InkWell` ONLY if `onTap != null`; otherwise returns the raw `Padding` content. Test both paths.
- **Variant colors use `theme.colorScheme.primary`** for info (not a hard-coded blue) — preserve theme-awareness.
- **Avatar text fits a single line** — `actorInitials` is conventionally 1-3 chars. Donor doesn't constrain length. Test edge case: `actorInitials: 'ABCDE'` — should still render in the 36×36 circle (text may shrink or clip; donor uses fontSize 13 with no auto-fit). Document.
</gotchas>

## Test list (write FIRST)

**Happy path (4 variants)**
- `EdenActivityFeedItem(item: EdenActivityFeedItemData(actorName: 'John Smith', actorInitials: 'JS', action: 'created', entityName: 'Customer #42', timeAgo: '5m ago', variant: EdenActivityVariant.success))` → finds 'John Smith', ' created ', 'Customer #42', '5m ago', 'JS'.
- Variant warning → avatar border + initials text are orange.
- Variant danger → red.
- Variant info → theme.colorScheme.primary.

**Avatar shape**
- Avatar `Container` is 36×36 with `BoxShape.circle`.
- Avatar initials text style fontSize 13, FontWeight.w700.

**Tap behavior**
- `onTap: () => tapped = true` → tapping the row sets `tapped = true`. (Use `tester.tap(find.byType(EdenActivityFeedItem))`.)
- `onTap: null` → row is wrapped in `Padding`, NOT `InkWell`. Assert `find.byType(InkWell)` finds 0 widgets (within the EdenActivityFeedItem subtree).

**Edge / responsive**
- Renders inside `SizedBox(width: 390)` (iPhone-narrow baseline) without overflow warnings.
- Long entity name (60 chars) doesn't overflow the row (Expanded + RichText with default soft wrap).
- Long actorInitials ('ABCDE') still renders within the 36×36 avatar (no overflow exception thrown).

<tasks>

<task type="auto" tdd="true">
<name>Task 1 — Fixture + RED → GREEN: EdenActivityFeedItem (with inlined data class + enum)</name>
<files>
test/widgets/_fixtures/eden_activity_feed_item_fixtures.dart
test/widgets/eden_activity_feed_item_test.dart
lib/src/widgets/eden_activity_feed_item.dart
lib/dev_app/screens/data_display_screen.dart
lib/eden_ui.dart
</files>
<action>
**Fixture (hand-built, no LLM-generated test data).** Header line: `// Do NOT regenerate via LLM — hand-built fixtures for EdenActivityFeedItem.`

`EdenActivityFeedItemFixtures` exposes:
- `const successSample = EdenActivityFeedItemData(actorName: 'John Smith', actorInitials: 'JS', action: 'created', entityName: 'Customer #42', timeAgo: '5m ago', variant: EdenActivityVariant.success)`
- `const warningSample = EdenActivityFeedItemData(actorName: 'Maria Garcia', actorInitials: 'MG', action: 'flagged', entityName: 'Invoice #1029', timeAgo: '12m ago', variant: EdenActivityVariant.warning)`
- `const dangerSample = EdenActivityFeedItemData(actorName: 'Alex Park', actorInitials: 'AP', action: 'deleted', entityName: 'Project Atlas', timeAgo: '1h ago', variant: EdenActivityVariant.danger)`
- `const infoSample = EdenActivityFeedItemData(actorName: 'Dev Bot', actorInitials: 'DB', action: 'commented on', entityName: 'Ticket T-501', timeAgo: 'just now', variant: EdenActivityVariant.info)`
- `const longEntitySample = EdenActivityFeedItemData(actorName: 'Long Name', actorInitials: 'LN', action: 'updated', entityName: 'A really long entity name that should ellipsize gracefully on narrow viewports', timeAgo: 'yesterday', variant: EdenActivityVariant.info)`
- `Widget wrap(Widget child) => MaterialApp(home: Scaffold(body: child));`

**RED phase.** Write 11 testWidgets one at a time (4 variants + 2 avatar shape + 2 tap behavior + 3 edge/responsive = 11). Each MUST fail before GREEN. Commit pattern: `test(003-06): add failing tests for EdenActivityFeedItem`.

**GREEN phase.** Implement `lib/src/widgets/eden_activity_feed_item.dart` from donor. Read donor first.

Public API:
```dart
enum EdenActivityVariant { success, warning, danger, info }

class EdenActivityFeedItemData {
  const EdenActivityFeedItemData({
    required this.actorName,
    required this.actorInitials,
    required this.action,
    required this.entityName,
    required this.timeAgo,
    this.variant = EdenActivityVariant.info,
  });
  final String actorName;
  final String actorInitials;
  final String action;
  final String entityName;
  final String timeAgo;
  final EdenActivityVariant variant;
}

class EdenActivityFeedItem extends StatelessWidget {
  const EdenActivityFeedItem({
    super.key,
    required this.item,
    this.onTap,
  });
  final EdenActivityFeedItemData item;
  final VoidCallback? onTap;
}
```

Implementation deltas vs donor:
- Class rename: `ActivityFeedItemWidget` → `EdenActivityFeedItem`.
- Data class rename + inlining: `ActivityFeedItem` (imported from trades) → `EdenActivityFeedItemData` (inlined into the same file).
- Enum rename + inlining: `ActivityVariant` → `EdenActivityVariant` (inlined).
- Remove `import 'package:trades/features/admin/domain/admin_stats_model.dart';` (replaced by inlined types).
- Preserve `_variantColor(theme, variant)` helper exactly.
- Dartdoc: explain `actorInitials` is consumer-computed (library does NOT extract from full name).

**Catalog entry.** Append to `lib/dev_app/screens/data_display_screen.dart` a section titled "EdenActivityFeedItem — Phase 1 (objective 003)":
- ListView showing 4 sample items (one per variant).
- One example with `onTap: () => debugPrint('tapped')` for the InkWell variant.
- One long-entity-name example to demonstrate iPhone-narrow soft-wrap.

**Export.** Append to `lib/eden_ui.dart` under the `// Phase 1 — Dashboard composites (objective 003)` section header (created by whichever Wave 2 TRD lands first):
```dart
export 'src/widgets/eden_activity_feed_item.dart';
```

Commits (sequential): `test(003-06)` (RED set) → `feat(003-06): implement EdenActivityFeedItem with inlined data class (Phase 1)` → `docs(003-06): add EdenActivityFeedItem to data_display_screen catalog`.

# CRITICAL: NO `import 'package:trades/...'` anywhere. The library version owns `EdenActivityFeedItemData` + `EdenActivityVariant` as inlined types. This is the cross-vertical decoupling lock per OBJECTIVE.md Constraint 5.
# CRITICAL: `lib/eden_ui.dart` edit MUST be serialized within Wave 2.
# GOTCHA: `actorInitials` is plain `String` — consumer pre-computes. Document.
# PATTERN: Same shape as Wave 1 TRDs but with inlined data class + enum exported in the same widget file (no separate models file for a single-consumer type).
</action>
<verify>
- `flutter test test/widgets/eden_activity_feed_item_test.dart` → 11/11 pass.
- `flutter analyze` → 0 issues. (NB: `grep -r "package:trades/" lib/` MUST return 0 lines.)
- `grep -c "eden_activity_feed_item" lib/eden_ui.dart` → 1.
- `flutter test` (full suite) → no regressions.
- Visual: `just dev-ui` Data Display screen → verify EdenActivityFeedItem demo with all 4 variants + tappable example + long-entity example.
</verify>
<done>
EdenActivityFeedItem shipped under Phase 1 Dashboard composites export section. Inlined data class + enum honor cross-vertical decoupling lock. Tests cover all 4 variants + avatar shape + tap behavior + iPhone-narrow safety.
</done>
</task>

</tasks>

<verification_commands>
- `flutter test test/widgets/eden_activity_feed_item_test.dart`
- `flutter test` (full suite — must not regress)
- `flutter analyze`
- `grep -r "package:trades/" lib/` → MUST be empty (verifies decoupling)
- Visual: `just dev-ui` Data Display screen → verify EdenActivityFeedItem demo
</verification_commands>
