---
objective: 003-phase-1-widget-donations
trd: "003-05"
type: standard
wave: 2
depends_on: []
files_modified:
  - lib/src/widgets/eden_cost_summary_card.dart
  - test/widgets/eden_cost_summary_card_test.dart
  - test/widgets/_fixtures/eden_cost_summary_card_fixtures.dart
  - lib/dev_app/screens/data_display_screen.dart
  - lib/eden_ui.dart
co_modified_files:
  - lib/eden_ui.dart
autonomous: true
must_haves:
  - "EdenCostSummaryCard renders a Card with title + labor/material/equipment line items + optional extra rows + total"
  - "Composes EdenCurrencyDisplay (001-04) for all monetary values (cents-based)"
  - "totalCents = laborCents + materialCents + equipmentCents + sum(extraRows.cents); extraRows do NOT affect total visible but the dartdoc commits to including them per donor behavior"
  - "Default title 'Cost Summary'; consumer can override via title parameter"
  - "iPhone-narrow safe; each row is a `Row(spaceBetween)` of label + currency display"
---

<objective>
Port `trades-flutter/lib/shared/widgets/cost_summary_card.dart` to eden-ui-flutter as `EdenCostSummaryCard`. Cross-vertical cost breakdown card used by trades (work-order cost summary, bids), salon (service breakdown), medical (treatment cost), legal (matter billing summary), gov (budget rollup), retail (cart breakdown).

Donor file is 124 LOC; library version expected ~145 LOC after dartdoc + dependency on EdenCurrencyDisplay.
</objective>

<context>
**Locked design inputs (do NOT revisit):**
- Donor: `/Users/markemerson/Source/AOCyber-Trades/trades-flutter/lib/shared/widgets/cost_summary_card.dart` (124 LOC).
- Composes existing `EdenCurrencyDisplay` (001-04) — no new currency-formatting logic.
- `CostRow` data class is exposed in the donor public API for `extraRows: const [CostRow(label: ..., cents: ...)]`. Library version: `EdenCostRow` (export the data class alongside the widget).

**Donor semantics (READ DONOR LINE 45 + 67 BEFORE PLANNING):**
- `totalCents = laborCents + materialCents + equipmentCents` (DOES NOT include extraRows — donor bug or feature? Test the donor behavior verbatim; if absorption surfaces it as undesirable, file a follow-up).
- Donor renders Labor/Materials/Equipment then `extraRows` BEFORE the divider + Total. So extraRows appear between Equipment and Total visually but are NOT summed into Total.

**Wait — verify donor semantics by re-reading.** The donor's `int get totalCents => laborCents + materialCents + equipmentCents;` confirms: extraRows are visually-only and not added to total. Document this clearly in dartdoc as a known-quirk of the library v1 (matches donor); a follow-up `EdenCostSummaryCard.includesExtraRowsInTotal` param could land later if needed.
</context>

<gotchas>
- **`extraRows` are visually rendered but NOT added to `totalCents`.** Donor bug-or-feature: preserve the bug for behavioral parity with trades; document in dartdoc with example. Test must explicitly assert this.
- **Composes `EdenCurrencyDisplay`** — relies on TRD 001-04 already being shipped (which it is on the main branch). No version pin needed.
- **`EdgeInsets.zero` on `Card.margin`** — donor uses zero margin so callers control spacing via outer `Padding`/`SizedBox`. Preserve.
- **`Divider(height: 20)`** — donor uses height 20 to give visual breathing room. Preserve exact value.
- **iPhone-narrow safety:** each `_CostLineItem` is a `Row(spaceBetween)`. If a label is long ("Equipment rental and operator labor"), it could push the currency off-screen. Wrap the label `Text` in `Expanded(flex: 1)` with `overflow: TextOverflow.ellipsis`.
</gotchas>

## Test list (write FIRST)

**Happy path (default behavior)**
- `EdenCostSummaryCard(laborCents: 120000, materialCents: 45000, equipmentCents: 25000)` → renders title 'Cost Summary', 3 line items showing $1,200.00 / $450.00 / $250.00, and total $1,900.00 (sum).
- Default title appears when no title passed.
- Custom title: `EdenCostSummaryCard(laborCents: 0, materialCents: 0, equipmentCents: 0, title: 'Project Budget')` → renders 'Project Budget' as title; total displays $0.00.

**Composition with EdenCurrencyDisplay**
- All 4 currency strings render via `EdenCurrencyDisplay` (assert `find.byType(EdenCurrencyDisplay)` count == 4 in the default 3-row case).
- Negative cents in one row renders with leading sign per `EdenCurrencyDisplay` semantics: `materialCents: -5000` → '-$50.00'.

**ExtraRows**
- `EdenCostSummaryCard(laborCents: 100, materialCents: 100, equipmentCents: 100, extraRows: [EdenCostRow(label: 'Subcontractor', cents: 50000)])` → 4 line items rendered (labor + material + equipment + subcontractor); total = 100+100+100 = $3.00 (DOES NOT include subcontractor). Test asserts label 'Subcontractor' is present and total Text reads '$3.00'.
- Two extraRows: both appear in order, total still excludes them.

**Edge / responsive**
- Renders inside `SizedBox(width: 390)` (iPhone-narrow baseline) without `RenderFlex overflowed` warnings.
- Long label (50 chars) ellipsizes; currency value stays right-aligned.

<tasks>

<task type="auto" tdd="true">
<name>Task 1 — Fixture + RED → GREEN: EdenCostSummaryCard</name>
<files>
test/widgets/_fixtures/eden_cost_summary_card_fixtures.dart
test/widgets/eden_cost_summary_card_test.dart
lib/src/widgets/eden_cost_summary_card.dart
lib/dev_app/screens/data_display_screen.dart
lib/eden_ui.dart
</files>
<action>
**Fixture (hand-built, no LLM-generated test data).** Header line: `// Do NOT regenerate via LLM — hand-built fixtures for EdenCostSummaryCard.`

`EdenCostSummaryCardFixtures` exposes:
- `const happyPath = (laborCents: 120000, materialCents: 45000, equipmentCents: 25000, expectedTotal: 190000)`
- `const withExtraRows = (laborCents: 100, materialCents: 100, equipmentCents: 100, extraRows: [EdenCostRow(label: 'Subcontractor', cents: 50000), EdenCostRow(label: 'Permits', cents: 7500)], expectedTotal: 300)` — note: total excludes extraRows per donor quirk
- `const longLabelExtraRow = EdenCostRow(label: 'Equipment rental + operator labor (50 chars)', cents: 100000)`
- `Widget wrap(Widget child) => MaterialApp(home: Scaffold(body: child));`

**RED phase.** Write 9 testWidgets one at a time (3 happy-path + 2 composition + 2 extraRows + 2 responsive = 9). Each MUST fail before GREEN. Commit pattern: `test(003-05): add failing tests for EdenCostSummaryCard`.

**GREEN phase.** Implement `lib/src/widgets/eden_cost_summary_card.dart` from donor. Read donor first.

Public API:
```dart
class EdenCostRow {
  const EdenCostRow({required this.label, required this.cents});
  final String label;
  final int cents;
}

class EdenCostSummaryCard extends StatelessWidget {
  const EdenCostSummaryCard({
    super.key,
    required this.laborCents,
    required this.materialCents,
    required this.equipmentCents,
    this.title = 'Cost Summary',
    this.extraRows = const [],
  });
  final int laborCents;
  final int materialCents;
  final int equipmentCents;
  final String title;
  final List<EdenCostRow> extraRows;
  int get totalCents => laborCents + materialCents + equipmentCents;
}
```

Implementation deltas vs donor:
- Class rename: `CostSummaryCard` → `EdenCostSummaryCard`; `CostRow` → `EdenCostRow`.
- Donor `import 'package:trades/shared/widgets/currency_display.dart'` → `import 'package:eden_ui_flutter/eden_ui.dart'` (or relative `import 'eden_currency_display.dart'` within `lib/src/widgets/`).
- Preserve donor's `_CostLineItem` private widget.
- iPhone-narrow safety: wrap label `Text` in `Expanded(child: Text(..., overflow: TextOverflow.ellipsis))`.
- Dartdoc: explain the extraRows-not-in-total quirk explicitly with a code example.

**Catalog entry.** Append to `lib/dev_app/screens/data_display_screen.dart` a section titled "EdenCostSummaryCard — Phase 1 (objective 003)":
- Default 3-row example: labor=$1,200, material=$450, equipment=$250, total=$1,900.
- With extraRows: same base + EdenCostRow(label: 'Subcontractor', cents: 50000) — note in caption that subcontractor row does NOT change the total.
- Custom title example: title='Project Budget'.

**Export.** Append to `lib/eden_ui.dart` under a NEW section header:
```dart
// Phase 1 — Dashboard composites (objective 003)
export 'src/widgets/eden_cost_summary_card.dart';
```

If 003-06/07/08/09 land first they may have created the section header. Append after, don't duplicate.

Commits (sequential): `test(003-05)` (RED set) → `feat(003-05): implement EdenCostSummaryCard (Phase 1)` → `docs(003-05): add EdenCostSummaryCard to data_display_screen catalog`.

# CRITICAL: `lib/eden_ui.dart` edit MUST be serialized within Wave 2.
# CRITICAL: This is the FIRST TRD touching the `// Phase 1 — Dashboard composites (objective 003)` export section header (if 003-05 runs before 003-06/07/08/09). Create the header line; do NOT touch any existing Wave A / obj 002 / Wave 1 sections.
# GOTCHA: `totalCents` does NOT include extraRows — that's the donor behavior. Test asserts it explicitly.
# PATTERN: Same shape as Wave 1 TRDs but with EdenCurrencyDisplay composition.
</action>
<verify>
- `flutter test test/widgets/eden_cost_summary_card_test.dart` → 9/9 pass.
- `flutter analyze` → 0 issues.
- `grep -c "eden_cost_summary_card" lib/eden_ui.dart` → 1.
- `flutter test` (full suite) → no regressions.
- Visual: `just dev-ui` Data Display screen → verify EdenCostSummaryCard demo with 3 variants.
</verify>
<done>
EdenCostSummaryCard shipped under Phase 1 Dashboard composites export section. Tests cover default behavior + EdenCurrencyDisplay composition + extraRows visual rendering with total-exclusion + iPhone-narrow safety.
</done>
</task>

</tasks>

<verification_commands>
- `flutter test test/widgets/eden_cost_summary_card_test.dart`
- `flutter test` (full suite — must not regress)
- `flutter analyze`
- Visual: `just dev-ui` Data Display screen → verify EdenCostSummaryCard demo
</verification_commands>
