---
objective: 003-phase-1-widget-donations
trd: "003-11"
type: standard
wave: 3
depends_on: ["003-10"]
files_modified:
  - lib/src/widgets/eden_ai/eden_ai_panel.dart
  - test/widgets/eden_ai/eden_ai_panel_test.dart
  - lib/dev_app/screens/chat_screen.dart
  - lib/eden_ui.dart
co_modified_files:
  - lib/eden_ui.dart
autonomous: true
must_haves:
  - "EdenAiPanel renders an AnimatedContainer that switches between 320px expanded and 40px collapsed states (200ms duration)"
  - "Expanded state: header (persona icon + 'AI Insights' + persona badge + chevron toggle) + content (ListView of EdenInsightCard items OR empty state OR shimmer)"
  - "Collapsed state: rotated 'AI' text + sparkle icon + chevron expand button (full panel is tappable to toggle)"
  - "isLoading=true → 3 grey shimmer placeholder rectangles; insights.isEmpty (and !isLoading) → empty state with 'No insights available'"
  - "onToggle is required; consumer owns expanded state"
  - "Composes EdenInsightCard (003-10) in compact mode"
---

<objective>
Port `trades-flutter/lib/shared/widgets/eden_ai_panel.dart` to eden-ui-flutter as `EdenAiPanel`. Cross-vertical collapsible AI-insight side panel used by trades (detail view AI panel), salon (booking insights panel), medical (patient insights panel), legal (matter AI panel).

Donor file is 201 LOC; library version expected ~225 LOC after dartdoc.

**Why not stateful:** the donor is `StatelessWidget` because the toggle state belongs to the consumer (host page tracks `isPanelExpanded` in its own state). Library preserves this pattern — consumers own state, library owns rendering.
</objective>

<context>
**Locked design inputs (do NOT revisit):**
- Donor: `/Users/markemerson/Source/AOCyber-Trades/trades-flutter/lib/shared/widgets/eden_ai_panel.dart` (201 LOC).
- Donor depends on `EdenInsightCard` (003-10) + `AiPersona` (003-10's `EdenAiPersona`).
- Width: 320 expanded / 40 collapsed. `AnimatedContainer` handles the transition.
- Header height ~40px (icon + title + badge + chevron in a 12hPad / 10vPad row).
- Content: `ListView.builder` with EdenInsightCard items in compact mode.

**Why this requires 003-10 (depends_on):**
- Composes `EdenInsightCard` for the list items.
- Reads `EdenAiPersona` for the header icon + badge.
- Reads `EdenInsightContent` list from the consumer.

All three types are exported from `eden_ai_models.dart` by 003-10.
</context>

<gotchas>
- **`AnimatedContainer` duration 200ms** — preserve. Test must verify width transitions are animated (use `tester.pumpAndSettle` to let the animation complete).
- **Collapsed state width = 40px** — the chevron + 'AI' text + sparkle icon fit in this narrow column via `RotatedBox(quarterTurns: 3)` for the 'AI' label. Preserve exactly.
- **`_PersonaBadge` private widget** — small pill showing `persona.displayLabel`. Background uses `persona.color.withValues(alpha: 0.15)`. Preserve.
- **`isLoading` AND `insights.isEmpty` priority:** if `isLoading` is true, shimmer renders regardless of insights count. Test priority explicitly.
- **`onToggle` is required** — donor signature has it as `required this.onToggle`. Library version stays required (consumer-owned state model).
- **Donor has NO `EdenAiInsightSlot`-style enabled flag** — this TRD's widget is unconditional. The gating widget is 003-15.
- **Mobile UX is separately handled** — donor's comment says "Mobile: not rendered (mobile uses bottom sheet via W17-07)". Library DOES NOT enforce this; consumers should conditionally not-render at mobile widths. Document.
</gotchas>

## Test list (write FIRST)

**Expanded state (default for test)**
- `EdenAiPanel(insights: [summaryInsight, metricInsight], persona: EdenAiPersona.operations, isExpanded: true, onToggle: () => toggleCount++)` → finds 'AI Insights', finds 'Operations' badge text, finds 2 `EdenInsightCard` widgets, finds `AnimatedContainer` with width 320.
- Persona icon visible (Icons.dashboard for operations).

**Collapsed state**
- `EdenAiPanel(insights: [...], persona: ..., isExpanded: false, onToggle: ...)` → width 40, finds `Icons.chevron_left`, finds 'AI' (rotated), finds `Icons.auto_awesome`. Does NOT find any `EdenInsightCard`.

**Toggle**
- Tapping the chevron in expanded state fires onToggle.
- Tapping anywhere in collapsed state fires onToggle (full panel is InkWell-wrapped).

**Loading state**
- `isLoading: true` → finds 3 grey shimmer Containers (height 60 each); does NOT find any EdenInsightCard.
- `isLoading: true, insights: [summaryInsight]` → shimmer takes priority; no insight card rendered.

**Empty state**
- `isLoading: false, insights: const []` → finds 'No insights available', finds `Icons.auto_awesome` (32px greyed); no EdenInsightCard.

**Persona variation**
- `persona: EdenAiPersona.fieldTech` → badge text 'Field Tech', icon Icons.engineering, color 0xFF3B82F6.
- `persona: EdenAiPersona.executive` → badge text 'Executive', icon Icons.insights, color 0xFF8B5CF6.

**Edge / responsive**
- Renders inside `SizedBox(width: 320, height: 600)` (sidebar width) with 5 insights → no overflow.

<tasks>

<task type="auto" tdd="true">
<name>Task 1 — Tests + GREEN: EdenAiPanel</name>
<files>
test/widgets/eden_ai/eden_ai_panel_test.dart
lib/src/widgets/eden_ai/eden_ai_panel.dart
lib/dev_app/screens/chat_screen.dart
lib/eden_ui.dart
</files>
<action>
**Fixture reuse.** Use `EdenAiModelsFixtures` from 003-10 (no new fixture file needed).

**RED phase.** Write 12 testWidgets one at a time (1 expanded happy + 1 collapsed + 2 toggle + 2 loading + 1 empty + 2 personas + 1 responsive + 2 misc shape assertions = 12). Each MUST fail before GREEN. Commit pattern: `test(003-11): add failing tests for EdenAiPanel`.

Key assertion strategies:
- `tester.widget<AnimatedContainer>(find.byType(AnimatedContainer)).constraints?.maxWidth` to check width.
- `tester.tap(find.byIcon(Icons.chevron_right))` for toggle in expanded.
- `tester.tap(find.byType(InkWell).first)` for collapsed full-tap (be careful — multiple InkWells exist; use `find.byKey` if needed).

**GREEN phase.** Implement `lib/src/widgets/eden_ai/eden_ai_panel.dart` from donor. Read donor first.

Public API:
```dart
class EdenAiPanel extends StatelessWidget {
  const EdenAiPanel({
    super.key,
    required this.insights,
    required this.persona,
    required this.isExpanded,
    required this.onToggle,
    this.isLoading = false,
  });
  final List<EdenInsightContent> insights;
  final EdenAiPersona persona;
  final bool isExpanded;
  final VoidCallback onToggle;
  final bool isLoading;
}
```

Implementation deltas vs donor:
- Class name stays `EdenAiPanel` (donor used `EdenAIPanel` with capital AI; library normalizes to `EdenAiPanel` matching Dart convention).
- Type renames throughout (donor's `InsightContent`/`AiPersona` → `EdenInsightContent`/`EdenAiPersona`).
- Import: `import 'eden_ai_models.dart';` + `import 'eden_insight_card.dart';`.
- Preserve `_PersonaBadge` private widget.
- Preserve shimmer Container behavior (3 grey rectangles, height 60, surfaceContainerHighest color).
- Preserve empty-state layout (icon + 'No insights available' text).
- Dartdoc: explain consumer-owned state pattern + isLoading priority + mobile-UX caveat (consumer hides at mobile widths).

**Catalog entry.** Update `lib/dev_app/screens/chat_screen.dart` adding a section titled "EdenAiPanel — Phase 1 (objective 003)":
- StatefulBuilder embedding the panel with a local `bool _expanded = true;` and a 'Toggle' button below — demonstrates expand/collapse.
- Side-by-side: one panel in normal state, one in loading state.
- One panel with empty insights showing the empty state.
- Persona dropdown to swap between all 4 personas live.

**Export.** Append to `lib/eden_ui.dart` under the `// Phase 1 — AI surface (objective 003)` section header (created by 003-10):
```dart
export 'src/widgets/eden_ai/eden_ai_panel.dart';
```

Commits (sequential): `test(003-11)` (RED set) → `feat(003-11): implement EdenAiPanel (Phase 1)` → `docs(003-11): add EdenAiPanel demo to chat screen catalog`.

# CRITICAL: NO `import 'package:trades/...'`.
# CRITICAL: `lib/eden_ui.dart` edit MUST be serialized within Wave 3.
# CRITICAL: `depends_on: ["003-10"]` — this TRD CANNOT execute until 003-10 (foundation + insight card) is GREEN. Orchestrator enforces.
# GOTCHA: Donor class name was `EdenAIPanel` (capital AI); library uses `EdenAiPanel` for Dart convention consistency (matches `EdenAi*` family naming).
# GOTCHA: `isLoading` priority over insights.isEmpty — both true means shimmer wins.
# PATTERN: Consumer owns the `isExpanded` bool; library provides `onToggle` callback to notify state changes.
</action>
<verify>
- `flutter test test/widgets/eden_ai/eden_ai_panel_test.dart` → 12/12 pass.
- `flutter analyze` → 0 issues.
- `grep -c "eden_ai_panel" lib/eden_ui.dart` → 1.
- `flutter test` (full suite) → no regressions.
- Visual: `just dev-ui` Chat screen → verify EdenAiPanel demo with toggle + loading + empty + persona-swap.
</verify>
<done>
EdenAiPanel shipped under Phase 1 — AI surface section. Tests cover expanded/collapsed states + loading priority + empty state + 4 personas + toggle wiring + iPhone-narrow safety.
</done>
</task>

</tasks>

<verification_commands>
- `flutter test test/widgets/eden_ai/eden_ai_panel_test.dart`
- `flutter test` (full suite — must not regress)
- `flutter analyze`
- Visual: `just dev-ui` Chat screen → verify EdenAiPanel demo
</verification_commands>
