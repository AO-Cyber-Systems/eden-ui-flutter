---
objective: 003-phase-1-widget-donations
trd: "003-03"
type: standard
wave: 1
depends_on: []
files_modified:
  - lib/src/widgets/eden_approval_status_badge.dart
  - test/widgets/eden_approval_status_badge_test.dart
  - test/widgets/_fixtures/eden_approval_status_badge_fixtures.dart
  - lib/dev_app/screens/badges_alerts_screen.dart
  - lib/eden_ui.dart
co_modified_files:
  - lib/eden_ui.dart
autonomous: true
must_haves:
  - "EdenApprovalStatusBadge renders pill badge with status-specific label + color for 10 known statuses"
  - "Status alias: 'pending' and 'pending_approval' both render 'Pending' (amber)"
  - "Unknown status renders via `.replaceAll('_', ' ')` (so 'in_review' renders 'in review')"
  - "Optional fontSize override; default uses theme.textTheme.labelSmall"
  - "iPhone-narrow safe; renders inside SizedBox(width: 90) without overflow"
---

<objective>
Port `trades-flutter/lib/shared/widgets/approval_status_badge.dart` to eden-ui-flutter as `EdenApprovalStatusBadge`. Cross-vertical approval-status indicator used by trades (purchase orders, change orders), salon (membership approval), medical (referral approval), legal (filing approval), gov (permit approval), retail (return approval).

Donor file is 126 LOC; library version expected ~145 LOC after dartdoc + iPhone-narrow Flexible wrap.
</objective>

<context>
**Locked design inputs (do NOT revisit):**
- Donor: `/Users/markemerson/Source/AOCyber-Trades/trades-flutter/lib/shared/widgets/approval_status_badge.dart` (126 LOC).
- Larger status vocabulary than urgency_badge (10 statuses) — donor handles via `switch (status)` (case-sensitive in donor).
- Filled style with no border; no icon (purely text-based).

**Known statuses (donor):**
- `pending` / `pending_approval` (alias) → 'Pending' (amber)
- `approved` → 'Approved' (green)
- `rejected` → 'Rejected' (theme.colorScheme.error)
- `draft` → 'Draft' (theme.colorScheme.onSurfaceVariant)
- `ordered` → 'Ordered' (blue)
- `received` → 'Received' (teal)
- `in_transit` → 'In Transit' (blue) — note label has space
- `completed` → 'Completed' (green)
- `fulfilled` → 'Fulfilled' (green)
- `cancelled` → 'Cancelled' (theme.colorScheme.onSurfaceVariant)
- (default) → input with `_` → ` ` (theme.colorScheme.onSurfaceVariant)
</context>

<gotchas>
- **Donor uses case-sensitive `switch (status)`** — preserve. Mixed-case input like `'Approved'` will fall through to the default branch. Document in dartdoc + add a test.
- **`pending` / `pending_approval` are aliases** — both produce identical config. Test both.
- **`in_transit` renders as 'In Transit' (with space)** — donor has this baked in as a hand-written label, NOT via `replaceAll('_', ' ')`. Edge case: don't confuse this with the default branch's underscore-replacement behavior. Test both explicitly.
- **Multiple statuses share the same green color** — `approved`, `completed`, `fulfilled`. Tests must distinguish on label, not color alone.
- **fontSize override** — donor passes `fontSize: fontSize` THROUGH labelSmall.copyWith. Same shape as urgency_badge.
</gotchas>

## Test list (write FIRST)

**Happy path (10 known statuses)**
- `EdenApprovalStatusBadge(status: 'pending')` → text 'Pending', amber background.
- `EdenApprovalStatusBadge(status: 'pending_approval')` → text 'Pending' (alias).
- `EdenApprovalStatusBadge(status: 'approved')` → text 'Approved', green background.
- `EdenApprovalStatusBadge(status: 'rejected')` → text 'Rejected', error color.
- `EdenApprovalStatusBadge(status: 'draft')` → text 'Draft', onSurfaceVariant.
- `EdenApprovalStatusBadge(status: 'ordered')` → text 'Ordered', blue.
- `EdenApprovalStatusBadge(status: 'received')` → text 'Received', teal.
- `EdenApprovalStatusBadge(status: 'in_transit')` → text 'In Transit' (with space), blue.
- `EdenApprovalStatusBadge(status: 'completed')` → text 'Completed', green.
- `EdenApprovalStatusBadge(status: 'fulfilled')` → text 'Fulfilled', green.
- `EdenApprovalStatusBadge(status: 'cancelled')` → text 'Cancelled', onSurfaceVariant.

**Case sensitivity**
- `EdenApprovalStatusBadge(status: 'Approved')` (uppercase A) → falls through to default branch → renders 'Approved' (via replaceAll which is a no-op when no underscore), onSurfaceVariant color.

**Unknown status with underscore**
- `EdenApprovalStatusBadge(status: 'in_review')` → text 'in review' (underscore replaced), onSurfaceVariant.

**fontSize override**
- `EdenApprovalStatusBadge(status: 'approved', fontSize: 20)` → rendered Text has effective fontSize 20.

**Edge / responsive**
- Renders inside `SizedBox(width: 90)` without overflow warnings.

<tasks>

<task type="auto" tdd="true">
<name>Task 1 — Fixture + RED → GREEN: EdenApprovalStatusBadge</name>
<files>
test/widgets/_fixtures/eden_approval_status_badge_fixtures.dart
test/widgets/eden_approval_status_badge_test.dart
lib/src/widgets/eden_approval_status_badge.dart
lib/dev_app/screens/badges_alerts_screen.dart
lib/eden_ui.dart
</files>
<action>
**Fixture (hand-built, no LLM-generated test data).** Header line: `// Do NOT regenerate via LLM — hand-built fixtures for EdenApprovalStatusBadge.`

`EdenApprovalStatusBadgeFixtures` exposes:
- `const knownStatuses = ['pending', 'pending_approval', 'approved', 'rejected', 'draft', 'ordered', 'received', 'in_transit', 'completed', 'fulfilled', 'cancelled']`
- `const expectedLabels = {'pending': 'Pending', 'pending_approval': 'Pending', 'approved': 'Approved', 'rejected': 'Rejected', 'draft': 'Draft', 'ordered': 'Ordered', 'received': 'Received', 'in_transit': 'In Transit', 'completed': 'Completed', 'fulfilled': 'Fulfilled', 'cancelled': 'Cancelled'}`
- `const unknownSamples = ['in_review', 'awaiting', 'Approved']` — Approved with uppercase falls to default
- `Widget wrap(Widget child) => MaterialApp(home: Scaffold(body: child));`

**RED phase.** Write 15 testWidgets one at a time (11 known statuses incl. alias + 1 case-sensitivity + 1 unknown-with-underscore + 1 fontSize + 1 responsive = 15). Each MUST fail before GREEN. Commit pattern: `test(003-03): add failing tests for EdenApprovalStatusBadge`.

**GREEN phase.** Implement `lib/src/widgets/eden_approval_status_badge.dart` from donor. Read donor first.

Public API:
```dart
class EdenApprovalStatusBadge extends StatelessWidget {
  const EdenApprovalStatusBadge({
    super.key,
    required this.status,
    this.fontSize,
  });
  final String status;
  final double? fontSize;
}
```

Implementation deltas vs donor:
- Class rename: `ApprovalStatusBadge` → `EdenApprovalStatusBadge`.
- `_BadgeConfig` and `_getConfig` stay file-private.
- iPhone-narrow safety: wrap label `Text` in `Flexible(child: Text(...))`.
- Dartdoc: list all 10 known statuses + alias + case-sensitivity + underscore-fallback semantics.

**Catalog entry.** Append to `lib/dev_app/screens/badges_alerts_screen.dart` a section titled "EdenApprovalStatusBadge — Phase 1 (objective 003)":
- `Wrap(spacing: 8, runSpacing: 8, children: [...])` with all 11 known status badges (including the alias).
- Sub-row showing 2 unknown samples + 1 case-mismatch sample.

**Export.** Append to `lib/eden_ui.dart` under the `// Phase 1 — Status & state primitives (objective 003)` section header (created by 003-01 OR 003-02 — whichever lands first):
```dart
export 'src/widgets/eden_approval_status_badge.dart';
```

Commits (sequential): `test(003-03)` (RED set) → `feat(003-03): implement EdenApprovalStatusBadge (Phase 1)` → `docs(003-03): add EdenApprovalStatusBadge to badges_alerts_screen catalog`.

# CRITICAL: `lib/eden_ui.dart` edit MUST be serialized within Wave 1 (co_modified_files lock).
# GOTCHA: Donor switch is case-sensitive — `'Approved'` falls through to default. Test this explicitly.
# GOTCHA: `'in_transit'` label is hand-written 'In Transit' (NOT a default-branch underscore replacement). Don't accidentally fold it into the default branch.
# PATTERN: Same shape as 003-01 / 003-02.
</action>
<verify>
- `flutter test test/widgets/eden_approval_status_badge_test.dart` → 15/15 pass.
- `flutter analyze` → 0 issues.
- `grep -c "eden_approval_status_badge" lib/eden_ui.dart` → 1.
- `flutter test` (full suite) → no regressions.
- Visual: `just dev-ui` Badges & Alerts screen → verify EdenApprovalStatusBadge demo with all 11 statuses + unknown samples.
</verify>
<done>
EdenApprovalStatusBadge shipped under Phase 1 export section. Tests cover 10 known statuses + alias + case-sensitivity + underscore fallback + fontSize override + iPhone-narrow safety.
</done>
</task>

</tasks>

<verification_commands>
- `flutter test test/widgets/eden_approval_status_badge_test.dart`
- `flutter test` (full suite — must not regress)
- `flutter analyze`
- Visual: `just dev-ui` Badges & Alerts screen → verify EdenApprovalStatusBadge demo
</verification_commands>
