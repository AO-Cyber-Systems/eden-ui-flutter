---
objective: 003-phase-1-widget-donations
trd: "003-01"
type: standard
wave: 1
depends_on: []
files_modified:
  - lib/src/widgets/eden_urgency_badge.dart
  - test/widgets/eden_urgency_badge_test.dart
  - test/widgets/_fixtures/eden_urgency_badge_fixtures.dart
  - lib/dev_app/screens/badges_alerts_screen.dart
  - lib/eden_ui.dart
co_modified_files:
  - lib/eden_ui.dart
autonomous: true
must_haves:
  - "EdenUrgencyBadge renders pill badge with stage label + optional icon for 4 known urgency levels"
  - "Color mapping: low=blue, medium=amber, high=orange (+priority_high icon), critical=theme.colorScheme.error (+warning_rounded icon)"
  - "Unknown urgency string renders gracefully (uses theme.colorScheme.onSurfaceVariant + raw string as label)"
  - "Optional fontSize override; default uses theme.textTheme.labelSmall"
  - "iPhone-narrow safe; renders inside SizedBox(width: 80) without RenderFlex overflowed warnings"
---

<objective>
Port `trades-flutter/lib/shared/widgets/urgency_badge.dart` to eden-ui-flutter as `EdenUrgencyBadge`. Cross-vertical urgency indicator used by trades (work orders, dispatch), medical (triage), legal (priority), gov (incidents), retail (loss prevention).

Donor file is 98 LOC; library version expected ~115 LOC after dartdoc + iPhone-narrow Flexible wrap.
</objective>

<context>
**Locked design inputs (do NOT revisit):**
- Donor: `/Users/markemerson/Source/AOCyber-Trades/trades-flutter/lib/shared/widgets/urgency_badge.dart` (98 LOC).
- Color decisions hard-coded (blue/amber/orange/error) — keep donor colors for v1; re-skin to tokens later if needed (objective scope §7).
- Eden-ui-flutter constraint: NO `flutter_riverpod`, NO transport packages — this widget is pure presentation.

**Why this is cross-vertical (not trades-only):**
- Trades: work-order priority, dispatch urgency, callback urgency.
- Medical: patient triage levels.
- Legal: case priority (P0/P1/P2/P3 maps to critical/high/medium/low).
- Gov: incident response urgency.
- Retail: loss-prevention alert priority.
</context>

<gotchas>
- **Donor uses `Colors.amber.withValues(alpha: 0.15)`** — Flutter 3.x `Color.withValues` syntax (NOT `withOpacity`). Donor was already migrated; keep `withValues` for consistency with rest of library.
- **`fontSize` parameter on the donor is unused-in-text-style-style** — donor passes `fontSize: fontSize` THROUGH `textTheme.labelSmall.copyWith(...)`. Keep that signature shape; tests must cover both default and override.
- **Unknown urgency string fallback** — donor falls through to a config using `theme.colorScheme.onSurfaceVariant` and the raw input as label. Edge case must be tested (e.g. `urgency: 'CUSTOM'`).
- **No icon for low/medium** — donor only renders icon for high (`Icons.priority_high`) and critical (`Icons.warning_rounded`). Tests must assert icon presence/absence per case.
- **iPhone-narrow safety:** the donor uses `Row(mainAxisSize: MainAxisSize.min)` which is OK inside `SizedBox` constraints; but if a consumer places `EdenUrgencyBadge` inside a fixed-width `Row` child WITHOUT `Flexible`, label overflow is possible for non-canonical input. Add `Flexible` wrap around the label `Text` to be safe, matching the `EdenButton` overflow fix at `e081038`.
</gotchas>

## Test list (write FIRST)

**Happy path (known urgency levels)**
- `EdenUrgencyBadge(urgency: 'low')` → renders text 'Low', NO icon.
- `EdenUrgencyBadge(urgency: 'medium')` → renders text 'Medium', NO icon.
- `EdenUrgencyBadge(urgency: 'high')` → renders text 'High', `Icons.priority_high` icon.
- `EdenUrgencyBadge(urgency: 'critical')` → renders text 'Critical', `Icons.warning_rounded` icon.

**Unknown urgency**
- `EdenUrgencyBadge(urgency: 'urgent_af')` → renders text 'urgent_af' (raw string), NO icon, color uses theme.colorScheme.onSurfaceVariant.

**fontSize override**
- `EdenUrgencyBadge(urgency: 'high', fontSize: 18)` → the rendered `Text` widget has effective fontSize 18.
- `EdenUrgencyBadge(urgency: 'high')` (no fontSize) → Text fontSize inherits from theme.textTheme.labelSmall.

**Edge / responsive**
- Renders inside `SizedBox(width: 80)` (≤390pt narrow context) at all 5 cases without `RenderFlex overflowed` warnings (use `tester.takeException()` to assert no `FlutterError`).

<tasks>

<task type="auto" tdd="true">
<name>Task 1 — Fixture + RED → GREEN: EdenUrgencyBadge</name>
<files>
test/widgets/_fixtures/eden_urgency_badge_fixtures.dart
test/widgets/eden_urgency_badge_test.dart
lib/src/widgets/eden_urgency_badge.dart
lib/dev_app/screens/badges_alerts_screen.dart
lib/eden_ui.dart
</files>
<action>
**Fixture (hand-built, no LLM-generated test data).** Header line: `// Do NOT regenerate via LLM — hand-built fixtures for EdenUrgencyBadge.`

`EdenUrgencyBadgeFixtures` exposes:
- `const knownLevels = ['low', 'medium', 'high', 'critical']`
- `const expectedLabels = {'low': 'Low', 'medium': 'Medium', 'high': 'High', 'critical': 'Critical'}`
- `const iconLevels = {'high': Icons.priority_high, 'critical': Icons.warning_rounded}`
- `const unknownSamples = ['urgent_af', 'p0', 'wat']`
- `Widget wrap(Widget child) => MaterialApp(home: Scaffold(body: child));` — local copy of the standard test wrap helper.

**RED phase.** Write 8 testWidgets one at a time (4 known levels + 1 unknown + 2 fontSize + 1 iPhone-narrow safety). Each MUST fail before GREEN. Commit pattern: `test(003-01): add failing tests for EdenUrgencyBadge`.

**GREEN phase.** Implement `lib/src/widgets/eden_urgency_badge.dart` from donor `/Users/markemerson/Source/AOCyber-Trades/trades-flutter/lib/shared/widgets/urgency_badge.dart`. Read donor first.

Public API:
```dart
class EdenUrgencyBadge extends StatelessWidget {
  const EdenUrgencyBadge({
    super.key,
    required this.urgency,
    this.fontSize,
  });
  final String urgency;
  final double? fontSize;
}
```

Implementation deltas vs donor:
- Class rename: `UrgencyBadge` → `EdenUrgencyBadge`.
- Library prefix on internal config classes: `_UrgencyConfig` stays `_UrgencyConfig` (file-private).
- iPhone-narrow safety: wrap the label `Text` in `Flexible(child: Text(...))`. Mirror the `EdenButton` overflow fix at commit `e081038`.
- Dartdoc: add a one-paragraph dartdoc explaining cross-vertical usage and the 4 known levels + unknown fallback behavior.

**Catalog entry.** Append a section to `lib/dev_app/screens/badges_alerts_screen.dart` titled "EdenUrgencyBadge — Phase 1 (objective 003)":
- Row showing all 4 known levels.
- Row showing 2 unknown-string fallbacks.

**Export.** Append to `lib/eden_ui.dart` under a NEW section header:
```dart
// Phase 1 — Status & state primitives (objective 003)
export 'src/widgets/eden_urgency_badge.dart';
```

Commits (sequential): `test(003-01)` (RED set) → `feat(003-01): implement EdenUrgencyBadge (Phase 1)` → `docs(003-01): add EdenUrgencyBadge to badges_alerts_screen catalog`.

# CRITICAL: This is the FIRST TRD touching the `// Phase 1 — Status & state primitives (objective 003)` export section header. Create the header line; do NOT touch any existing Wave A / obj 002 sections.
# GOTCHA: Donor uses `Color.withValues(alpha: 0.15)` (Flutter 3.x). Do NOT regress to `withOpacity`.
# PATTERN: Mirror obj 001 / obj 002 TRD-04 shape. Single task = fixture + tests + impl + export + catalog.
</action>
<verify>
- `flutter test test/widgets/eden_urgency_badge_test.dart` → 8/8 pass.
- `flutter analyze` → 0 issues.
- `grep -c "eden_urgency_badge" lib/eden_ui.dart` → 1.
- `flutter test` (full suite) → 445 (existing) + 8 = 453 tests pass.
- Visual: `just dev-ui` → navigate to Badges & Alerts screen → verify 4 known + 2 unknown variants render with correct colors and icons.
</verify>
<done>
EdenUrgencyBadge shipped under Phase 1 export section. Tests cover 4 known levels + unknown fallback + fontSize override + iPhone-narrow safety. Demo wired in badges_alerts_screen.
</done>
</task>

</tasks>

<verification_commands>
- `flutter test test/widgets/eden_urgency_badge_test.dart`
- `flutter test` (full suite — must not regress)
- `flutter analyze`
- Visual: `just dev-ui` Badges & Alerts screen → verify EdenUrgencyBadge demo
</verification_commands>
