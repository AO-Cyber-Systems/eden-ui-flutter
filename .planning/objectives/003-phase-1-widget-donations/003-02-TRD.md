---
objective: 003-phase-1-widget-donations
trd: "003-02"
type: standard
wave: 1
depends_on: []
files_modified:
  - lib/src/widgets/eden_pipeline_badge.dart
  - test/widgets/eden_pipeline_badge_test.dart
  - test/widgets/_fixtures/eden_pipeline_badge_fixtures.dart
  - lib/dev_app/screens/badges_alerts_screen.dart
  - lib/eden_ui.dart
co_modified_files:
  - lib/eden_ui.dart
autonomous: true
must_haves:
  - "EdenPipelineBadge renders pill badge with stage-specific icon + label for 5 known stages"
  - "Stage aliases supported: 'won'|'accepted' → Won variant, 'lost'|'rejected' → Lost variant (case-insensitive)"
  - "Unknown stage renders 'Unknown' with help_outline icon + grey color"
  - "Outlined pill: bordered (alpha 0.35) on color-tinted background (alpha 0.12)"
  - "iPhone-narrow safe; renders inside SizedBox(width: 100) without overflow"
---

<objective>
Port `trades-flutter/lib/shared/widgets/pipeline_badge.dart` to eden-ui-flutter as `EdenPipelineBadge`. Cross-vertical pipeline-stage indicator used by trades (bid pipeline), salon (membership stage), medical (referral stage), legal (case stage), retail (lead funnel).

Donor file is 91 LOC; library version expected ~110 LOC after dartdoc + iPhone-narrow Flexible wrap.
</objective>

<context>
**Locked design inputs (do NOT revisit):**
- Donor: `/Users/markemerson/Source/AOCyber-Trades/trades-flutter/lib/shared/widgets/pipeline_badge.dart` (91 LOC).
- Donor uses `switch (stage.toLowerCase())` with multi-key pattern (`'won' || 'accepted'`) — preserve case-insensitive matching + aliases.
- Bordered (outlined) style; visually distinct from `EdenUrgencyBadge` (filled-only).

**Cross-vertical mapping (informational; not enforced in library):**
- Trades bidding: draft / sent / won / lost / expired.
- Salon membership: draft / sent / accepted / rejected / expired.
- Legal cases: draft / filed / won / lost / withdrawn (caller maps "withdrawn" → "expired" if reuse desired).
- Retail leads: draft / sent / converted ("won") / dropped ("lost") / expired.
</context>

<gotchas>
- **Case-insensitive matching is via `stage.toLowerCase()`** — preserve. Tests must include mixed-case input (`'WON'`, `'Sent'`).
- **Alias matching:** `'won'` and `'accepted'` both map to the Won variant; `'lost'` and `'rejected'` both to Lost. Edge case must be tested explicitly.
- **Stage style is `const`** in donor — keep `const` for compile-time tree-shake friendliness.
- **Border alpha differs from background alpha** — donor uses 0.35 border on 0.12 background fill. Visually thin but visible at small font sizes; preserve exact values.
- **iPhone-narrow safety:** wrap label `Text` in `Flexible` (mirror urgency_badge fix).
</gotchas>

## Test list (write FIRST)

**Happy path (5 known stages)**
- `EdenPipelineBadge(stage: 'draft')` → text 'Draft', `Icons.edit_outlined` icon, grey color.
- `EdenPipelineBadge(stage: 'sent')` → text 'Sent', `Icons.send_outlined` icon, blue color.
- `EdenPipelineBadge(stage: 'won')` → text 'Won', `Icons.check_circle_outline` icon, green color.
- `EdenPipelineBadge(stage: 'lost')` → text 'Lost', `Icons.cancel_outlined` icon, red color.
- `EdenPipelineBadge(stage: 'expired')` → text 'Expired', `Icons.schedule_outlined` icon, orange color.

**Aliases**
- `EdenPipelineBadge(stage: 'accepted')` → renders identical to `stage: 'won'` (text 'Won', check_circle_outline, green).
- `EdenPipelineBadge(stage: 'rejected')` → renders identical to `stage: 'lost'` (text 'Lost', cancel_outlined, red).

**Case-insensitive**
- `EdenPipelineBadge(stage: 'WON')` → renders identical to `stage: 'won'`.
- `EdenPipelineBadge(stage: 'Sent')` → renders identical to `stage: 'sent'`.

**Unknown stage**
- `EdenPipelineBadge(stage: 'invented_stage')` → text 'Unknown', `Icons.help_outline` icon, grey color.

**Edge / responsive**
- Renders inside `SizedBox(width: 100)` without `RenderFlex overflowed` warnings (assert `tester.takeException() == null`).

<tasks>

<task type="auto" tdd="true">
<name>Task 1 — Fixture + RED → GREEN: EdenPipelineBadge</name>
<files>
test/widgets/_fixtures/eden_pipeline_badge_fixtures.dart
test/widgets/eden_pipeline_badge_test.dart
lib/src/widgets/eden_pipeline_badge.dart
lib/dev_app/screens/badges_alerts_screen.dart
lib/eden_ui.dart
</files>
<action>
**Fixture (hand-built, no LLM-generated test data).** Header line: `// Do NOT regenerate via LLM — hand-built fixtures for EdenPipelineBadge.`

`EdenPipelineBadgeFixtures` exposes:
- `const knownStages = ['draft', 'sent', 'won', 'lost', 'expired']`
- `const aliasMap = {'accepted': 'won', 'rejected': 'lost'}`
- `const expectedLabels = {'draft': 'Draft', 'sent': 'Sent', 'won': 'Won', 'lost': 'Lost', 'expired': 'Expired'}`
- `const expectedIcons = {'draft': Icons.edit_outlined, 'sent': Icons.send_outlined, 'won': Icons.check_circle_outline, 'lost': Icons.cancel_outlined, 'expired': Icons.schedule_outlined}`
- `Widget wrap(Widget child) => MaterialApp(home: Scaffold(body: child));`

**RED phase.** Write 10 testWidgets one at a time (5 known + 2 aliases + 2 case-insensitive + 1 unknown + 1 responsive = 11; recount: 5+2+2+1+1=11). Each MUST fail before GREEN. Commit pattern: `test(003-02): add failing tests for EdenPipelineBadge`.

**GREEN phase.** Implement `lib/src/widgets/eden_pipeline_badge.dart` from donor. Read donor first.

Public API:
```dart
class EdenPipelineBadge extends StatelessWidget {
  const EdenPipelineBadge({
    super.key,
    required this.stage,
  });
  final String stage;
}
```

Implementation deltas vs donor:
- Class rename: `PipelineBadge` → `EdenPipelineBadge`.
- `_StageStyle` and `_styleForStage` stay file-private.
- iPhone-narrow safety: wrap label `Text` in `Flexible(child: Text(...))`.
- Dartdoc: explain cross-vertical usage + alias behavior.

**Catalog entry.** Append to `lib/dev_app/screens/badges_alerts_screen.dart` (same screen as TRD-01) a section titled "EdenPipelineBadge — Phase 1 (objective 003)":
- Wrap of all 5 known stages.
- Sub-row showing 2 aliases ('accepted', 'rejected') + 1 unknown.

**Export.** Append to `lib/eden_ui.dart` under the `// Phase 1 — Status & state primitives (objective 003)` section header (created by 003-01):
```dart
export 'src/widgets/eden_pipeline_badge.dart';
```

If 003-01's section header doesn't exist yet (parallel execution race), create it. The orchestrator MUST serialize within-wave export edits via `co_modified_files: [lib/eden_ui.dart]`.

Commits (sequential): `test(003-02)` (RED set) → `feat(003-02): implement EdenPipelineBadge (Phase 1)` → `docs(003-02): add EdenPipelineBadge to badges_alerts_screen catalog`.

# CRITICAL: `lib/eden_ui.dart` edit MUST be serialized within Wave 1 — only one TRD at a time can append to it. The orchestrator uses `co_modified_files` to enforce this.
# GOTCHA: Case-insensitive matching via `stage.toLowerCase()` — preserve exact donor code path.
# PATTERN: Same shape as 003-01 (one task = fixture + tests + impl + export + catalog).
</action>
<verify>
- `flutter test test/widgets/eden_pipeline_badge_test.dart` → 11/11 pass.
- `flutter analyze` → 0 issues.
- `grep -c "eden_pipeline_badge" lib/eden_ui.dart` → 1.
- `flutter test` (full suite) → 445 (existing) + 8 (003-01) + 11 (003-02) = 464 tests pass (assuming parallel completion).
- Visual: `just dev-ui` Badges & Alerts screen → verify EdenPipelineBadge demo.
</verify>
<done>
EdenPipelineBadge shipped under Phase 1 export section. Tests cover 5 known stages + alias mapping + case-insensitive + unknown fallback + iPhone-narrow safety.
</done>
</task>

</tasks>

<verification_commands>
- `flutter test test/widgets/eden_pipeline_badge_test.dart`
- `flutter test` (full suite — must not regress)
- `flutter analyze`
- Visual: `just dev-ui` Badges & Alerts screen → verify EdenPipelineBadge demo
</verification_commands>
