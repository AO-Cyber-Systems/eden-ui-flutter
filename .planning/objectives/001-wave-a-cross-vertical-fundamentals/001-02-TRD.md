---
objective: 001-wave-a-cross-vertical-fundamentals
trd: "001-02"
type: standard
wave: 1
depends_on: []
files_modified:
  - lib/src/widgets/eden_detail_page_scaffold.dart
  - lib/src/widgets/eden_detail_header.dart
  - test/widgets/eden_detail_page_scaffold_test.dart
  - test/widgets/eden_detail_header_test.dart
  - test/widgets/_fixtures/eden_detail_page_scaffold_fixtures.dart
  - lib/dev_app/screens/layouts_screen.dart
  - lib/eden_ui.dart
co_modified_files:
  - lib/eden_ui.dart
autonomous: true
must_haves:
  - "EdenDetailHeader renders avatar/icon + title + subtitle + breadcrumb + action-icon row + status badge slot at iPhone-narrow, tablet, desktop"
  - "EdenDetailPageScaffold composes EdenDetailHeader + tabs slot + side rail slot + body slot"
  - "iPhone-narrow (≤480pt): side rail collapses to a bottom sheet trigger; tabs scroll horizontally; action icons collapse to overflow menu"
  - "Both widgets exported from lib/eden_ui.dart under '// Wave A — Cross-vertical scaffolds' section"
  - "Dev catalog entry under lib/dev_app/screens/layouts_screen.dart"
---

<objective>
Port trades-flutter `lib/shared/widgets/detail_view_scaffold.dart` + `detail_header.dart` into eden-ui-flutter as two paired widgets: `EdenDetailHeader` (header strip used standalone or inside the scaffold) + `EdenDetailPageScaffold` (header → tabs → side rail composition). **Transport-agnostic** — strip any Riverpod/RBAC plumbing. Every Eden Biz detail page (customer, project, fleet truck, matter, patient, retail product, gov case) composes against this scaffold.
</objective>

## Test list (write FIRST)

**EdenDetailHeader**
Happy: renders title only; renders title+subtitle; renders breadcrumb above title; renders action icons row; renders status badge slot.
Edge: no avatar/icon → leading column collapses; no breadcrumb → top row collapses; 6+ action icons at narrow → overflows to PopupMenuButton.
Failure/safety: iPhone-narrow (390pt) — no overflow; long title (60+ chars) wraps to 2 lines with ellipsis at line 3.

**EdenDetailPageScaffold**
Happy: composes header + tabs + body; tab switching renders different body content (state lifted via TabController fixture).
Edge: no side rail → body takes full width; tabs slot null → header + body only with no TabBar.
Failure/safety: iPhone-narrow — side rail collapses to a "Details" bottom-sheet trigger button at the bottom of the body; tabs scroll horizontally; renders without overflow.

<tasks>

<task type="auto" tdd="true">
<name>Task 1 — Fixture builders (shared across both test files)</name>
<files>
test/widgets/_fixtures/eden_detail_page_scaffold_fixtures.dart
</files>
<action>
Hand-built factories for both widgets. NO LLM-generated test data.

Define `EdenDetailHeaderFixtures` + `EdenDetailPageScaffoldFixtures` classes with static factory methods:
- Header: `minimal()`, `withSubtitle()`, `withBreadcrumb()`, `withActions(int n)`, `narrowOverflowProbe()`.
- Scaffold: `headerOnly()`, `withTabs(List<String> tabLabels)`, `withSideRail()`, `full()`, `narrowResponsive()`.

Sample data: title='Customer ABC123', subtitle='Acme HVAC Co.', breadcrumb=['Customers', 'Active']. Avatar icon: `Icons.business`. Actions: `[(Icons.star, 'Star'), (Icons.push_pin, 'Pin'), (Icons.share, 'Share'), (Icons.archive, 'Archive')]`.

File header comment: "Hand-built fixtures. Do NOT regenerate via LLM."
</action>
<verify>
`flutter analyze test/widgets/_fixtures/eden_detail_page_scaffold_fixtures.dart` → 0 issues.
</verify>
<done>
Fixture file with 10 factory methods compiles cleanly.
</done>
</task>

<task type="auto" tdd="true">
<name>Task 2 — RED → GREEN: EdenDetailHeader (inner widget first; outside-in from scaffold POV means scaffold tests would block on header — write header first)</name>
<files>
test/widgets/eden_detail_header_test.dart
lib/src/widgets/eden_detail_header.dart
lib/eden_ui.dart
</files>
<action>
RED phase: write `test/widgets/eden_detail_header_test.dart` mirroring `eden_alert_test.dart` shape with `wrap()` helper. One test at a time through RED→GREEN. 9 testWidgets total (5 happy + 3 edge + 1 narrow-overflow probe). After all RED: `test(001-02): add failing tests for EdenDetailHeader`.

GREEN phase: implement `lib/src/widgets/eden_detail_header.dart` from trades-flutter donor at `/Users/markemerson/Source/AOCyber-Trades/trades-flutter/lib/shared/widgets/detail_header.dart`. Read donor first; strip Riverpod/RBAC; replace with raw slots.

Public API:
```
class EdenDetailHeader extends StatelessWidget {
  const EdenDetailHeader({
    super.key,
    required this.title,
    this.subtitle,
    this.breadcrumb,            // List<String>?
    this.leading,               // Widget? (avatar/icon)
    this.actions,               // List<EdenDetailHeaderAction>?
    this.statusBadge,           // Widget?
    this.actionsOverflowAtCount = 4, // collapse to PopupMenuButton beyond this
  });
  // ...
}

class EdenDetailHeaderAction {
  const EdenDetailHeaderAction({required this.icon, required this.tooltip, required this.onPressed});
  final IconData icon;
  final String tooltip;
  final VoidCallback onPressed;
}
```

Use `LayoutBuilder` for actions collapse at narrow widths. Use `Wrap` for breadcrumb segments. Add export to `lib/eden_ui.dart` under '// Wave A — Cross-vertical scaffolds'.

Commit `feat(001-02): implement EdenDetailHeader (Wave A)`.

# GOTCHA: Action icon row at narrow widths is the most common overflow point. Test with 6 actions at 390pt width — must collapse.
# PATTERN: Mirror EdenPageHeader's LayoutBuilder breakpoint (480pt) for consistency.
</action>
<verify>
- `flutter test test/widgets/eden_detail_header_test.dart` → 9/9 pass.
- `flutter analyze` → 0 issues.
</verify>
<done>
EdenDetailHeader implemented, exported, all 9 tests pass.
</done>
</task>

<task type="auto" tdd="true">
<name>Task 3 — RED → GREEN: EdenDetailPageScaffold + dev catalog entry</name>
<files>
test/widgets/eden_detail_page_scaffold_test.dart
lib/src/widgets/eden_detail_page_scaffold.dart
lib/dev_app/screens/layouts_screen.dart
lib/eden_ui.dart
</files>
<action>
RED: 8 testWidgets (3 happy + 2 edge + 3 failure/safety). One test at a time. After RED: `test(001-02): add failing tests for EdenDetailPageScaffold`.

GREEN: implement `lib/src/widgets/eden_detail_page_scaffold.dart` from trades-flutter donor `/Users/markemerson/Source/AOCyber-Trades/trades-flutter/lib/shared/widgets/detail_view_scaffold.dart`.

Public API:
```
class EdenDetailPageScaffold extends StatelessWidget {
  const EdenDetailPageScaffold({
    super.key,
    required this.header,     // EdenDetailHeader (or any Widget)
    this.tabs,                // List<EdenDetailTab>?
    this.sideRail,            // Widget?
    required this.body,       // Widget (or List<Widget> if tabs provided — one per tab)
    this.sideRailWidth = 320,
    this.narrowBreakpoint = 480,
  });
}

class EdenDetailTab {
  const EdenDetailTab({required this.label, this.icon});
  final String label;
  final IconData? icon;
}
```

Narrow responsive: at width < `narrowBreakpoint`, side rail collapses to a `OutlinedButton('Details')` at the bottom of the body that opens a `showModalBottomSheet`.

Body parameter accepts `Widget`; if `tabs != null`, expects body to be a `TabBarView` constructed by the caller (or auto-wraps in `DefaultTabController` if a `List<Widget>` is passed via a `bodyForTab` builder — pick the simpler API: caller supplies their own TabController via `body: TabBarView(children: [...])` and Scaffold provides the TabBar inside the header column).

Add export to `lib/eden_ui.dart`. Add dev catalog entry to `lib/dev_app/screens/layouts_screen.dart` showing header-only + with-tabs + with-side-rail + narrow-responsive demos.

2-3 commits: `test(001-02): ... DetailPageScaffold` → `feat(001-02): ... DetailPageScaffold` → optional `refactor` + `docs(001-02): add EdenDetailPageScaffold to dev catalog`.

# CRITICAL: Tab API decision affects every consumer. Pick the explicit-TabController approach (caller wires DefaultTabController + TabBarView) rather than the auto-wrap; less magic, fewer edge cases.
# GOTCHA: Side rail collapse at narrow widths must use `Scaffold.bottomSheet` or `showModalBottomSheet` — NOT a Drawer (drawer is for left nav).
</action>
<verify>
- `flutter test test/widgets/eden_detail_page_scaffold_test.dart` → 8/8 pass.
- `flutter test` → full suite passes.
- `flutter analyze` → 0 issues.
- `grep -c "eden_detail_" lib/eden_ui.dart` → 2 exports under '// Wave A — Cross-vertical scaffolds'.
</verify>
<done>
EdenDetailPageScaffold + EdenDetailHeader both shipped, exported, demoed in catalog.
</done>
</task>

</tasks>

<verification_commands>
- `flutter test` — all tests pass
- `flutter analyze` — 0 issues
- `grep -E "eden_detail_(header|page_scaffold)" lib/eden_ui.dart` — 2 lines
</verification_commands>
