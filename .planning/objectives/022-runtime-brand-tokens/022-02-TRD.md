---
objective: 022-runtime-brand-tokens
trd: "022-02"
type: standard
stack: flutter
wave: 2
depends_on: ["022-01"]
files_modified:
  - lib/src/theme/eden_brand_swatch.dart
  - lib/eden_ui.dart
  - test/theme/_fixtures/brand_swatch_fixtures.dart
  - test/theme/eden_brand_swatch_test.dart
co_modified_files:
  - lib/eden_ui.dart
autonomous: true
must_haves:
  - "EdenBrandSwatch.tryParse(String?) returns MaterialColor? — null for ANY malformed input, never throws, for any String or null whatsoever"
  - "Accepted hex forms: '#RRGGBB', 'RRGGBB', '#RGB', 'RGB', '#AARRGGBB', 'AARRGGBB', case-insensitive, surrounding whitespace tolerated"
  - "Generated MaterialColor carries all ELEVEN shades 50,100,200,300,400,500,600,700,800,900,950 — 950 is mandatory because EdenTheme.dark() dereferences primary[950]!"
  - "Shade 500 equals the supplied colour EXACTLY (same ARGB int), and the MaterialColor's own primary value equals shade 500"
  - "Lightness ramp is monotonically non-increasing from shade 50 through shade 950 for every input, including #FFFFFF, #000000, #FAFAFA and #101010"
  - "For the hexes named in the `realPartnerHexes` and `degenerate` fixtures ONLY, hue and saturation are preserved across every shade WITHIN TOLERANCE: hue +/- 1.0 degrees, saturation +/- 0.02. The residual is 8-bit channel quantization in HSLColor.toColor(), not implementation error — measured worst cases over that fixture are 0.8682 deg hue (#D4A853 shade 950) and 0.00830 saturation (#0F62FE shade 50)"
  - "That tolerance is FITTED TO THOSE NAMED INPUTS — it is NOT a general property of the algorithm. Hue drift scales inversely with the INPUT saturation, so a low-saturation hex blows it wide open (MEASURED: #6B7280 drifts 20.00 deg / 0.0227 sat, #B0A99F drifts 35.29 deg / 0.0305 sat). Adding any hex to `realPartnerHexes` REQUIRES re-measuring the tolerance for that hex; do not assume 1.0 deg carries"
  - "Exactness sub-case, measured at full precision: greys (#808080) round-trip hue AND saturation bit-exactly on all eleven shades. Fully-saturated primaries (#FF0000, #00FF00) preserve HUE bit-exactly on all eleven shades, but their SATURATION is 1-2 ULP low on shades 400/900/950 (worst delta -2.22e-16) — assert it with closeTo(expected, 1e-12), never equality. Do NOT assert exact hue or saturation for any other input"
  - "EdenTheme.light(brand: generated) and EdenTheme.dark(brand: generated) both build without throwing — proving 950/400/100/900 dereferences are satisfied"
  - "EdenBrandPreset keeps its MaterialColor field and the 15-preset registry is unchanged — the generator is an escape hatch, not a replacement (LOCKED decision 3)"
  - "lib/eden_ui.dart exports src/theme/eden_brand_swatch.dart"
  - "test/theme/eden_theme_back_compat_test.dart (TRD 022-01) still passes unmodified"
---

<objective>
Add `EdenBrandSwatch` — a pure function turning ONE arbitrary hex string into a full
11-shade Eden `MaterialColor`.

Purpose: `EdenBrandPresetRegistry` is a CLOSED set of 15 presets, so a partner brand
colour outside that set has no path into an Eden theme today (022-CONTEXT.md finding 2).
A downstream Flutter web app reads `"#0F62FE"` out of `window.APP_CONFIG` at boot and
needs a `MaterialColor` from it. This is capability 1 of 3.

Output: one new self-contained file under `lib/src/theme/`, its export, and a test suite.
This TRD adds a NEW capability and changes no existing behaviour.
</objective>

<file_tree>
lib/src/theme/
└── eden_brand_swatch.dart                  ← CREATE
lib/eden_ui.dart                            ← MODIFY (add one export line)
test/theme/
├── _fixtures/
│   └── brand_swatch_fixtures.dart          ← CREATE (hand-built input cases)
└── eden_brand_swatch_test.dart             ← CREATE
</file_tree>

<execution_context>
@~/.claude/devflow/workflows/execute-trd.md
@~/.claude/devflow/templates/summary.md
</execution_context>

<embedded_context>

<codebase_examples>
Every `EdenColors` swatch has ELEVEN shades — note the non-standard `950`, which stock
Flutter `MaterialColor`s do not have (lib/src/tokens/colors.dart):

```dart
  static const MaterialColor gold = MaterialColor(0xFFD4A853, <int, Color>{
    50: Color(0xFFFDF8EF),
    100: Color(0xFFFAECD5),
    200: Color(0xFFF4D5AA),
    300: Color(0xFFEDB974),
    400: Color(0xFFE59A3C),
    500: Color(0xFFD4A853),
    600: Color(0xFFC49545),
    700: Color(0xFFA67A38),
    800: Color(0xFF856131),
    900: Color(0xFF6C5029),
    950: Color(0xFF3D2A14),
  });
```

`EdenTheme.dark()` dereferences shades 400, 900, 950 and 100 with `!`
(lib/src/theme/eden_theme.dart):

```dart
    final colorScheme = ColorScheme.dark(
      primary: primary[400]!,
      onPrimary: primary[950]!,
      primaryContainer: primary[900]!,
      onPrimaryContainer: primary[100]!,
```

`EdenTheme.light()` dereferences 100 and 900. A swatch missing ANY of those keys makes
`EdenTheme.dark()` throw a null-check error at theme construction — i.e. at app boot.

Hand-built fixture convention (test/theme/_fixtures/brand_preset_fixtures.dart header):

```dart
// Hand-built brand preset registry. Do NOT regenerate via LLM —
// mutate in-place when the registry contract changes.
// Per global TDD Playbook habit 4 + resolver no_llm_test_data constraint.
```
</codebase_examples>

<anti_patterns>
- DO NOT use `ColorScheme.fromSeed` / Material's tonal palette to build the ramp. It
  re-tones the seed, so shade 500 would NOT equal the supplied colour — which is the one
  hard requirement in 022-CONTEXT.md § Discretion. Rejected on that basis.
- DO NOT build a 10-shade swatch. Flutter's own `Colors.blue` style swatches stop at 900;
  Eden's stop at 950 and `EdenTheme.dark()` depends on it. Omitting 950 turns a partner
  hex into a boot crash in dark mode.
- DO NOT throw on malformed input, and do NOT `assert` on it either — asserts are live in
  debug/profile builds and a partner's typo'd config would take the app down in exactly
  the environment where you would want a graceful fallback.
- DO NOT mutate or extend `EdenBrandPresetRegistry`, and DO NOT change
  `EdenBrandPreset.color` away from `MaterialColor`. LOCKED decision 3: the 15 presets
  stay; this is an escape hatch alongside them.
- DO NOT touch `surfaceTonalSeed`, `radiusMultiplier`, `minimumTouchTargetPx` or
  `preferBorderOverShadow`. They are OUT OF SCOPE for this objective (Objective 0b).
- DO NOT reach for a property-based testing library. Resolver constraint
  `no_property_based_default` is active — use the named, hand-built cases in the fixture.
</anti_patterns>

<error_recovery>
**Malformed input contract — this is a quality gate for the objective.** A bad partner
value must never crash an app's boot. `tryParse` returns `null`; the caller decides the
fallback. Cases that MUST return null rather than throw:
`null`, `''`, `'   '`, `'#'`, `'nope'`, `'#GGGGGG'`, `'#12345'` (5 digits),
`'#1234567'` (7 digits), `'#RRGGBBAA'`-ordered input is NOT supported (Flutter is ARGB —
treat 8 digits as AARRGGBB), `'rgb(1,2,3)'`, `'blue'`, and the SIGNED forms
`'#-0F62FEE'`, `'#+0F62FEE'`, `'#-0F62F'`, `'#-F0'`.

**The signed forms are a real hole, not a hypothetical.** `int.tryParse` accepts a
leading `+` or `-` sign: MEASURED 2026-08-26, `int.tryParse('-0F62FE', radix: 16)`
returns -1008382. A signed string whose length happens to be 3, 6 or 8 therefore passes
the length check and produces a swatch from a negative ARGB int. It never throws, so
boot survival is not at risk — but the result is nonsense. The explicit hex-digit guard
in Task 2 is the fix; do not rely on the length check alone.

**Degenerate but valid inputs** must still produce a usable swatch, not null:
`#FFFFFF` and `#000000` are valid colours. The clamp described in Task 1 keeps their
ramps monotonic. `#000000` collapses shades 500..950 to black — that is acceptable and
expected for a pathological brand colour; it must not throw and 500 must still be exact.

**If `flutter test` reports a null-check error building the dark theme**, the swatch is
missing shade 950 (or 400/900/100). Verify the shade map has all eleven keys.
</error_recovery>

</embedded_context>

<context>
@.planning/objectives/022-runtime-brand-tokens/022-CONTEXT.md
@lib/src/tokens/colors.dart
@lib/src/theme/eden_theme.dart
@lib/src/theme/eden_brand_preset.dart
@test/theme/_fixtures/brand_preset_fixtures.dart
</context>

<research_context>
**The ramp algorithm is already chosen and empirically validated — implement this, do
not re-derive it.** Measured 2026-08-26 against the eight hand-tuned `EdenColors`
swatches, then verified across nine inputs including the pathological ones.

Chosen: **HSL lightness ramp anchored at the input's own lightness.** Preserve the
input's hue and saturation on every shade; vary only lightness. This is the only family
of algorithms that can satisfy "shade 500 equals the supplied colour" exactly.

The interpolation curve was derived from the MEAN HSL lightness of the seven non-neutral
`EdenColors` swatches, so a generated ramp looks like a sibling of the hand-tuned ones:

    shade:  50    100   200   300   400   500   600   700   800   900   950
    meanL:  97.0  92.8  86.2  76.3  62.9  53.1  44.7  37.5  30.7  25.2  14.6

Because existing 500-shades range from L=39.4 (emerald) to L=65.1 (purple), an absolute
target curve would break the 500==input requirement. So normalise the curve into
position weights and interpolate from the anchor outward:

    lightEnd = max(97.0, (anchorL + 100.0) / 2.0)   // clamp engages only when anchorL > 97
    darkEnd  = min(14.6, anchorL / 2.0)             // clamp engages only when anchorL < 29.2

    for shades < 500:  t = (meanL[s] - meanL[500]) / (meanL[50] - meanL[500])
                       L[s] = anchorL + t * (lightEnd - anchorL)
    for shade  == 500: L[500] = anchorL              // exact
    for shades > 500:  u = (meanL[500] - meanL[s]) / (meanL[500] - meanL[950])
                       L[s] = anchorL - u * (anchorL - darkEnd)

Precomputed weights — embed these as constants:

    t[50]=1.000000  t[100]=0.904328  t[200]=0.753986  t[300]=0.528474  t[400]=0.223235
    u[600]=0.218182 u[700]=0.405195  u[800]=0.581818  u[900]=0.724675  u[950]=1.000000

The two clamps exist because the unclamped form INVERTS for extreme inputs (verified:
`#FAFAFA` produced shade 50 darker than shade 500; `#101010` produced shade 950 lighter
than shade 500). The clamps are no-ops for every normal brand colour.

VERIFIED output — all nine cases monotonic with shade 500 exact:

  #0F62FE (IBM Carbon Blue 60, the partner hex named in the objective):
    50 #F0F5FF · 100 #DAE7FF · 200 #B8D1FF · 300 #86B0FE · 400 #4183FE · 500 #0F62FE
    600 #014FE2 · 700 #0142BD · 800 #01369B · 900 #012D7F · 950 #001A4A
    Contrast: white-on-500 = 5.00:1 (AA for normal text); shade 400 on Eden dark
    surface `neutral[900]` #18181B = 4.98:1. Legible on BOTH light and dark surfaces,
    which is the 022-CONTEXT.md § Discretion requirement.

  #D4A853 (AO gold) reproduces a close analogue of the hand-tuned gold ramp, confirming
  the curve matches house style.

  #FFFFFF, #000000, #FAFAFA, #101010, #FF0000, #808080, #00FF00: all monotonic, all
  500-exact.

Dart has no HSL helper you should hand-roll: use `HSLColor.fromColor(c)` and
`hslColor.withLightness(l).toColor()` from `dart:ui`/`package:flutter/material.dart`.
`HSLColor.lightness` is 0..1, so divide the L values above by 100.
</research_context>

<gotchas>
- `HSLColor.fromColor` on a fully-desaturated input yields `saturation == 0` and an
  arbitrary hue; `withLightness` still behaves correctly, producing a grey ramp. No
  special-casing needed, but include a grey input in the fixture to lock the behaviour.
- **The HSL round trip is lossy in hue and saturation, by design of 8-bit colour.**
  `hsl.withLightness(l).toColor()` rounds each channel to an integer 0..255, and
  re-deriving HSL from those rounded channels lands slightly off the input's hue and
  saturation. MEASURED 2026-08-26: worst case across the fixture's real partner hexes is
  0.8682 degrees of hue (`#D4A853` shade 950) and 0.00830 of saturation (`#0F62FE`
  shade 50). Greys are fully exempt (both components exact). Fully-saturated primaries
  are exempt in HUE only — their saturation still lands 1-2 ULP off (see Test list 8b).
  This is why Test list item 8 asserts `closeTo`, not equality — the tolerance is
  describing the colour space, not papering over a defect.
  The size of that drift is a function of the INPUT's saturation, not a constant: the
  lower the input saturation, the further hue wanders. The 1.0-degree figure is fitted to
  the named fixture hexes and does not generalise to arbitrary muted brand colours.
- Round-tripping `Color -> HSLColor -> Color` is NOT exactly lossy-free for all inputs.
  Shade 500 must therefore be the ORIGINAL parsed `Color`, assigned directly — do NOT
  compute it through the HSL round trip, or `500 == input` will fail by a bit or two on
  some hexes.
- `Color.value` is deprecated in newer Flutter SDKs in favour of the float channel
  accessors, but this repo's existing code (`eden_theme.dart`, the 009 tests) uses
  `.value` throughout. Stay consistent with the repo; do not start a migration here.
- The `MaterialColor` constructor's first argument is the primary ARGB int; it must equal
  shade 500 or `ColorScheme.light(primary: swatch)` and `swatch[500]` disagree.
- `EdenTheme.brandColor` is a mutable static. Do not assign to it from library code.
</gotchas>

## Test list

Behaviour cases, outermost (theme builds) to innermost (parsing). Written BEFORE any
implementation, per the resolver's `test_list_first: required`.

**Theme integration — the reason 950 matters**
1. `EdenTheme.dark(brand: EdenBrandSwatch.tryParse('#0F62FE')!)` constructs without
   throwing (dereferences shades 950, 900, 400, 100).
2. `EdenTheme.light(brand: EdenBrandSwatch.tryParse('#0F62FE')!)` constructs without
   throwing (dereferences 100, 900).
3. `EdenTheme.light(brand: generated).colorScheme.primary` == the supplied hex.

**Swatch shape**
4. A generated swatch exposes all eleven keys 50,100,200,300,400,500,600,700,800,900,950
   — none null.
5. Shade 500 ARGB == input ARGB, for every hex in `realPartnerHexes`.
6. The `MaterialColor`'s own value (`swatch.value`) == shade 500's ARGB.
7. HSL lightness is monotonically non-increasing from shade 50 to shade 950, for every
   hex in `realPartnerHexes` AND every hex in `degenerate`.
8. Hue and saturation are preserved from the input on every shade, WITHIN TOLERANCE —
   `closeTo(hsl.hue, 1.0)` and `closeTo(hsl.saturation, 0.02)`. **Scope this assertion to
   the hexes named in the `realPartnerHexes` and `degenerate` fixtures.** Skip the hue
   check on any shade whose saturation is 0 (hue is arbitrary for a grey).

   Do NOT write `expect(HSLColor.fromColor(sw[50]!).hue, hsl.hue)`. That assertion goes
   RED on a CORRECT implementation. MEASURED 2026-08-26 at full precision against this
   exact algorithm, worst drift over all eleven shades:

     #0F62FE  hue 0.83682 deg (shade 50)   saturation 0.0082988 (shade 50)
     #D4A853  hue 0.86822 deg (shade 950)  saturation 0.0032389 (shade 600)
     #FF0000  hue 0.0 (exact)              saturation 2.22e-16  (shade 950)
     #00FF00  hue 0.0 (exact)              saturation 2.22e-16  (shade 950)
     #808080  hue 0.0 (exact)              saturation 0.0 (exact)  — grey

   The cause is `HSLColor.toColor()` rounding back to 8-bit channels — a real,
   irreducible property of the round trip, not a bug to chase.

8a. **The 1.0-degree / 0.02 tolerance is FITTED to the nine fixture hexes above, not an
    invariant of the algorithm — and the hue headroom is only 0.13 deg.** Hue drift
    scales INVERSELY with the input's saturation: the more muted the brand colour, the
    further hue wanders. Item 8's `saturation == 0` skip does not help here, because a
    muted colour has a small NON-zero saturation and is therefore still checked.
    MEASURED 2026-08-26, all of these BLOW the tolerance on a correct implementation:

      #B0A99F  src sat 0.097  worst hue 35.29 deg  worst sat 0.0305
      #8B8680  src sat 0.045  worst hue 32.73 deg  worst sat 0.0214
      #6B7280  src sat 0.089  worst hue 20.00 deg  worst sat 0.0227   (Tailwind gray-500)
      #5C5470  src sat 0.143  worst hue 12.86 deg  worst sat 0.0179

    So: if you ADD a hex to `realPartnerHexes` — especially a muted or neutral corporate
    colour, which is exactly what an arbitrary partner is likely to send — you MUST
    re-measure its drift and widen or special-case the tolerance for it. Do not assume
    the fitted tolerance carries, and do NOT "fix" the ramp when it does not. The ramp is
    verified correct against the golden (item 9); the tolerance is the fitted part.

8b. EXACT-equality sub-case, kept as a separate assertion so the strict property is not
    lost — but note WHICH half is exact, because hue and saturation differ here:

    - `#808080` (grey) — hue AND saturation are bit-exact on all eleven shades. Assert
      both with plain equality: `expect(g.hue, hsl.hue)`, `expect(g.saturation,
      hsl.saturation)`.
    - `#FF0000` and `#00FF00` — **HUE ONLY** is bit-exact on all eleven shades (measured
      delta exactly `0.0` on all 11). Assert hue with plain equality. Their SATURATION is
      NOT bit-exact — it is 1-2 ULP low on shades 400, 900 and 950. MEASURED 2026-08-26
      at full precision, for BOTH hexes:

        shade 400 -> 0.99999999999999989   delta -1.110e-16
        shade 900 -> 0.99999999999999989   delta -1.110e-16
        shade 950 -> 0.99999999999999978   delta -2.220e-16

      Assert their saturation with `closeTo(hsl.saturation, 1e-12)`. Writing
      `expect(g.saturation, hsl.saturation)` for these two goes RED on a CORRECT
      implementation — 1e-12 leaves ~4500x headroom over the measured 2.22e-16 while
      still being strict enough to catch any real ramp defect.

    Do not extend this list without re-measuring, and measure with
    `toStringAsPrecision(17)` or by comparing the delta against `1e-15`. NOT with
    `toStringAsFixed(4)` — 4-decimal formatting prints 1-ULP error as a clean `0.0000`,
    which is precisely how the false "saturation is exact" claim got here.

**Golden ramp**
9. `tryParse('#0F62FE')` reproduces `carbonBlueExpectedRamp` exactly, all eleven shades.

**Degenerate-but-valid inputs**
10. `#FFFFFF` produces a monotonic ramp with 500 == white and a usable dark end.
11. `#000000` produces 500 == black without throwing (dark end collapsing to black is
    expected and acceptable).
12. `#FAFAFA` (near-white) does NOT invert — shade 50 is not darker than shade 500.
13. `#101010` (near-black) does NOT invert — shade 950 is not lighter than shade 500.

**Accepted input forms — all resolve to the same swatch**
14. `'#0F62FE'`, `'0f62fe'`, `'  #0F62FE  '` and `'#FF0F62FE'` all yield an identical
    shade map.
15. 3-digit shorthand `'#F00'` expands to `0xFFFF0000`.

**Malformed input — the boot-survival contract**
16. `tryParse(null)` returns null.
17. `tryParse('')`, `tryParse('   ')`, `tryParse('#')` each return null.
18. `tryParse('nope')`, `tryParse('blue')`, `tryParse('rgb(1,2,3)')` each return null.
19. `tryParse('#GGGGGG')` returns null (valid length, invalid hex digits).
20. `tryParse('#12345')` and `tryParse('#1234567')` return null (bad lengths).
20b. Signed input returns null: `tryParse('#-0F62FEE')`, `tryParse('#+0F62FEE')`,
     `tryParse('#-0F62F')` and `tryParse('#-F0')` are each null. `int.tryParse` accepts a
     leading `+`/`-` — MEASURED: `int.tryParse('-0F62FE', radix: 16)` == -1008382 — so a
     signed string of an accepted LENGTH slips past the length check and yields a swatch
     from a negative ARGB. The hex-digit guard in Task 2 closes this.
21. No input in the `malformed` fixture causes a throw of any kind — assert with
    `returnsNormally` across the whole list.

**Registry non-regression (LOCKED decision 3)**
22. `EdenBrandPresetRegistry.all()` still returns exactly 15 presets.
23. `EdenBrandPresetRegistry.byId('gold')!.color` is still a `MaterialColor`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hand-build the swatch fixture and write the failing spec</name>
  <files>test/theme/_fixtures/brand_swatch_fixtures.dart, test/theme/eden_brand_swatch_test.dart</files>
  <action>
Write the fixture and the RED test suite before `EdenBrandSwatch` exists.

`brand_swatch_fixtures.dart` — hand-built named cases, carrying the repo's standard
provenance header ("Do NOT regenerate via LLM ... no_llm_test_data constraint"):

  - `validForms`: the same colour expressed every accepted way —
    `'#0F62FE'`, `'0f62fe'`, `'  #0F62FE  '`, `'#0F62FEFF'`-style 8-digit AARRGGBB
    (`'#FF0F62FE'`), and the 3-digit shorthand pair (`'#F00'` -> `0xFFFF0000`).
  - `malformed`: every case listed in error_recovery, plus `null`. This INCLUDES the
    four signed forms `'#-0F62FEE'`, `'#+0F62FEE'`, `'#-0F62F'`, `'#-F0'` — they are the
    cases the length check alone does not catch.
  - `realPartnerHexes`: `'#0F62FE'` (IBM Carbon Blue 60 — the objective's named partner
    hex), `'#D4A853'` (AO gold), `'#FF0000'`, `'#808080'`, `'#00FF00'`.
  - `degenerate`: `'#FFFFFF'`, `'#000000'`, `'#FAFAFA'`, `'#101010'`.
  - `carbonBlueExpectedRamp`: the eleven VERIFIED shades for `#0F62FE` from
    research_context, as `Map<int, int>` shade -> ARGB. This is the golden ramp.

`eden_brand_swatch_test.dart` — a `## Test list`-driven suite (see Test list below).
Use `testWidgets` only where a theme is built; plain `test` is fine for pure parsing
since `EdenBrandSwatch` itself must not touch GoogleFonts.

# CRITICAL: this task must end RED — the suite must fail to COMPILE or fail its
#           assertions because EdenBrandSwatch does not exist yet. Do not stub the
#           production class here.
# CRITICAL: no property-based testing library (no_property_based_default is active).
#           Named cases only.
# GOTCHA:   assert monotonicity on HSL LIGHTNESS, not on the raw ARGB int — ARGB ints
#           are not ordered by perceived lightness.
  </action>
  <verify>
`flutter test test/theme/eden_brand_swatch_test.dart` FAILS (compile error on the
missing `EdenBrandSwatch`, or failing assertions). Capture the RED output for the
SUMMARY.
`flutter analyze test/theme/_fixtures/brand_swatch_fixtures.dart` is clean apart from
the unresolved `EdenBrandSwatch` reference.
  </verify>
  <done>
Fixture holds all five hand-built case groups including the verified Carbon Blue golden
ramp; the spec covers every item in the Test list; the suite is RED for the right reason
(missing production class, not a typo).
  </done>
  <recovery>
If the suite passes, something already provides `EdenBrandSwatch` — stop and investigate
rather than proceeding, because the RED step is what proves the assertions bite.
  </recovery>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement EdenBrandSwatch and take the suite green</name>
  <files>lib/src/theme/eden_brand_swatch.dart, lib/eden_ui.dart</files>
  <action>
Implement the parser and the ramp exactly as specified in research_context.

Public surface — keep it small and boring:

```dart
/// Generates a full 11-shade Eden [MaterialColor] from one arbitrary hex string.
///
/// This is the ESCAPE HATCH for a partner brand colour that is not one of the 15
/// [EdenBrandPresetRegistry] presets (022-CONTEXT.md LOCKED decision 3) — the
/// registry is unchanged and remains the preferred path.
///
/// Designed for runtime config: a Flutter web app reads `"#0F62FE"` out of
/// `window.APP_CONFIG` at boot and passes the String straight in.
class EdenBrandSwatch {
  EdenBrandSwatch._();

  /// Parses [hex] and returns an 11-shade swatch, or null when [hex] is not a
  /// colour this can understand. NEVER throws — a malformed partner value must
  /// not take an app's boot down.
  static MaterialColor? tryParse(String? hex);

  /// Builds an 11-shade swatch around an already-parsed [seed].
  /// Shade 500 is [seed] exactly.
  static MaterialColor fromColor(Color seed);
}
```

Approach:

1. `tryParse`:
   - null / blank -> null
   - trim, strip a single leading '#', uppercase
   - **reject anything that is not pure hex digits BEFORE parsing:**
     `if (!RegExp(r'^[0-9A-Fa-f]+$').hasMatch(value)) return null;`
     This is not redundant with the length check. `int.tryParse` accepts a leading
     `+`/`-`, so `'-0F62FEE'` is 8 characters, passes the length check, and parses to a
     negative int. Anchor the pattern at both ends (`^...$`) or it matches a substring.
   - accept length 3 (expand each nibble: 'F00' -> 'FF0000'), 6 (prefix 'FF'), or 8
     (already AARRGGBB). Any other length -> null
   - `int.tryParse(value, radix: 16)`; null result -> null
   - `return fromColor(Color(argb))`

2. `fromColor`:
   - `final hsl = HSLColor.fromColor(seed); final anchorL = hsl.lightness * 100;`
   - `final lightEnd = max(97.0, (anchorL + 100.0) / 2.0);`
   - `final darkEnd  = min(14.6, anchorL / 2.0);`
   - for each shade, compute L from the embedded t/u weight constants, then
     `hsl.withLightness((L / 100).clamp(0.0, 1.0)).toColor()`
   - **assign shade 500 to `seed` DIRECTLY**, not via the HSL round trip
   - `return MaterialColor(seed.value, {50: ..., ..., 950: ...});` — eleven keys

Then add the export to `lib/eden_ui.dart`, placed with the other theme exports
(after `export 'src/theme/eden_brand_preset.dart';`, around line 384).

# CRITICAL: eleven shades. 950 is mandatory — EdenTheme.dark() does `primary[950]!`.
# CRITICAL: shade 500 is the original Color object, not a recomputed one.
# CRITICAL: never throw and never assert on bad input; return null from tryParse.
# GOTCHA:   MaterialColor's first ctor arg must equal shade 500's ARGB.
# PATTERN:  file header comment style follows lib/src/theme/eden_brand_preset.dart.
  </action>
  <verify>
`flutter test test/theme/eden_brand_swatch_test.dart` — all green.
`flutter test test/theme/` — green, including TRD 022-01's back-compat suite and the
122 pre-existing tests.
`flutter analyze` — no new issues.
`grep -n "eden_brand_swatch" lib/eden_ui.dart` shows the export.
  </verify>
  <done>
`EdenBrandSwatch.tryParse` handles every fixture case; the Carbon Blue ramp matches the
golden values byte for byte; every generated swatch has all eleven shades with 500
exact and a monotonic lightness ramp; `EdenTheme.light(brand:)` and `.dark(brand:)` both
build from a generated swatch without throwing; the back-compat spine is still green.
  </done>
  <recovery>
If SHADE 500 is off by a bit or two, check whether it is being computed through the HSL
round trip instead of assigned from `seed` — that is the usual cause, and 500 must be
exact. If a whole shade is wrong, re-check the t/u weight constant for that shade against
research_context.

**Do NOT apply that reasoning to a hue or saturation assertion.** If a hue/saturation
check fails by under a degree on a shade OTHER than 500, the implementation is correct
and the ASSERTION is too strict — it is 8-bit quantization in `HSLColor.toColor()`, which
no amount of fixing `fromColor` will remove. Widen to `closeTo(expected, 1.0)` for hue
and `closeTo(expected, 0.02)` for saturation (Test list item 8) rather than touching the
ramp. Shade 500's exactness comes from direct assignment, not from the round trip, so it
is unaffected either way.
If `EdenTheme.dark(brand: generated)` throws a null-check error, a shade key is missing.
Revert with `git checkout lib/src/theme/eden_brand_swatch.dart lib/eden_ui.dart` and
re-apply; nothing else in the tree depends on this file yet.
  </recovery>
</task>

</tasks>

<validation_gates>
<lint>flutter analyze</lint>
<test>flutter test test/theme/</test>
</validation_gates>

<verification>
1. `flutter analyze` clean.
2. `flutter test test/theme/` green — new suite plus TRD 022-01 back-compat plus the 122
   pre-existing tests.
3. `EdenBrandSwatch.tryParse('#0F62FE')!` reproduces the eleven verified Carbon Blue
   shades exactly.
4. For every input in `realPartnerHexes` + `degenerate`: eleven shades present, shade 500
   ARGB == input ARGB (EXACT), HSL lightness monotonically non-increasing 50 -> 950, and
   hue/saturation within `closeTo` tolerance of the input (hue 1.0 deg, saturation 0.02 —
   see Test list item 8 for the measured drift figures and why exact equality is wrong).
5. For every input in `malformed`: `tryParse` returns null and throws nothing — including
   the signed forms `'#-0F62FEE'`, `'#+0F62FEE'`, `'#-0F62F'`, `'#-F0'`.
5b. `grep -n "RegExp" lib/src/theme/eden_brand_swatch.dart` shows the anchored hex-digit
    guard `^[0-9A-Fa-f]+$` present and applied BEFORE `int.tryParse`.
6. `EdenTheme.light(brand: s)` and `EdenTheme.dark(brand: s)` construct successfully for
   a generated `s` — this is the real proof that shade 950 exists.
7. `git diff lib/src/theme/eden_brand_preset.dart` is EMPTY — the 15-preset registry is
   untouched (LOCKED decision 3).
8. `git diff` contains no reference to `surfaceTonalSeed`, `radiusMultiplier`,
   `minimumTouchTargetPx`, `preferBorderOverShadow`, `EdenRadii` or `EdenColors` call
   sites.
</verification>

<success_criteria>
- An arbitrary partner hex becomes a usable Eden `MaterialColor` with a house-consistent
  11-shade ramp.
- Shade 500 is exactly the supplied colour; the ramp is legible on both light and dark
  Eden surfaces (verified 5.00:1 on white, 4.98:1 on the dark surface for Carbon Blue).
- Malformed input yields null, never an exception — an app's boot survives a typo'd
  partner config.
- The 15 presets and `EdenBrandPreset.color`'s `MaterialColor` type are unchanged.
- The objective 022 back-compat spine is still green.
</success_criteria>

<output>
After completion, create
`.planning/objectives/022-runtime-brand-tokens/022-02-SUMMARY.md`.
Record the RED output captured in Task 1 and confirm the Carbon Blue golden ramp matched.
</output>
