---
objective: 021-a4c-template-block-builder
trd: "021-01"
type: standard
wave: 1
depends_on: []
files_modified:
  - lib/src/widgets/eden_template_builder/template_models.dart
  - lib/src/widgets/eden_template_builder/template_block_registry.dart
  - lib/src/widgets/eden_template_builder/template_variables_registry.dart
  - lib/src/widgets/eden_template_builder/template_variables_resolver.dart
  - lib/src/widgets/eden_template_builder/eden_template_builder_exports.dart
  - lib/eden_ui.dart
  - test/widgets/eden_template_builder/_fixtures/eden_template_models_fixtures.dart
  - test/widgets/eden_template_builder/eden_template_models_test.dart
  - test/widgets/eden_template_builder/eden_template_block_registry_test.dart
  - test/widgets/eden_template_builder/eden_template_variables_registry_test.dart
  - test/widgets/eden_template_builder/eden_template_variables_resolver_test.dart
co_modified_files:
  - lib/eden_ui.dart
autonomous: true
must_haves:
  - "EdenTemplateGraph value class with id, name, category, version, status (String), blocks (List<EdenTemplateBlock>), createdAt, updatedAt — parity G-1"
  - "EdenTemplateBlock value class with id, type (String — registry-driven, NOT enum), content (Map<String, dynamic>), order (int), section (EdenTemplateSection) — parity G-2 + G-5"
  - "EdenTemplateSection enum: header, body, footer — parity G-4"
  - "EdenTemplateBlockDescriptor value class with id, displayName, description, icon (IconData), category (String), isAdvanced (bool default false), placeholderBuilder (optional callback) — supports registry registration"
  - "EdenTemplatePageSize enum: letter, a4, legal with extension exposing dimensions (in inches or mm) — parity L-2"
  - "EdenTemplateOrientation enum: portrait, landscape — parity L-3"
  - "EdenTemplateLayoutSettings value class with pageSize, orientation, marginTop/Right/Bottom/Left (double), differentFirstPageHeader (bool), pageNumbersInFooter (bool) — parity L-4 + L-5 + L-6"
  - "EdenTemplateStyleSettings value class with brandColor (Color), fontFamily (String), headerFontSize (double), bodyFontSize (double) — parity S-5"
  - "EdenTemplateBlockPaletteItem value class with icon, displayName, description, blockTypeId (String), isAdvanced (bool default false), category (String) — palette grid render shape"
  - "EdenTemplateVariableGroup value class with groupName, fields (List<String>) — variables panel render shape"
  - "EdenTemplateMissingVariableException exception class — thrown by resolver when throwOnMissing is true"
  - "EdenTemplateBlockRegistry singleton with register/lookup/all/byCategory/reset/resetToDefaults methods; resetToDefaults registers 12 built-in block types (text, field, table, list, photoGrid, signature, divider, spacer, image, qrCode, conditional, repeater) — parity R-1 + B-1..B-12"
  - "EdenTemplateVariablesRegistry singleton with register(group, fields)/lookup/all/reset methods; ships EMPTY (consumers fill it) — parity R-2"
  - "EdenTemplateVariablesResolver.resolve(String text, Map<String, dynamic> data, {bool throwOnMissing=false, String missingPlaceholder=''}) static pure function — substitutes {{group.field}} tokens, supports dotted nested paths — parity V-6"
  - "All value classes round-trip through toJson/fromJson losslessly (where applicable)"
  - "Zero new pubspec dependencies"
  - "All existing tests continue to pass (no obj 006 modifications)"
---

<objective>
Wave 1 foundation. Establish the value-type vocabulary (`EdenTemplateGraph`, `EdenTemplateBlock`, `EdenTemplateBlockDescriptor`, `EdenTemplateLayoutSettings`, `EdenTemplateStyleSettings`, `EdenTemplateBlockPaletteItem`, `EdenTemplateVariableGroup`, enums `EdenTemplateSection` / `EdenTemplatePageSize` / `EdenTemplateOrientation`, exception `EdenTemplateMissingVariableException`) that every downstream TRD depends on. Add the two locked registries (`EdenTemplateBlockRegistry`, `EdenTemplateVariablesRegistry`) per parity rows R-1 and R-2. Add the pure-function `EdenTemplateVariablesResolver.resolve(...)` per parity row V-6.

This TRD is the choke point for the entire objective — every other TRD imports from `template_models.dart` and consumes the two registries + resolver. Get it right; it's locked from Wave 2 onward.
</objective>

<context>
**Locked design inputs (do NOT revisit):**
- Donor `template_model.dart` at `/Users/markemerson/Source/AOCyber-Trades/trades-flutter/lib/features/templates/domain/template_model.dart` (313 LOC) — the source vocabulary. Strip trades-specific commentary; preserve field shapes.
- Donor `Template`, `TemplateBlock`, `BlockType` enum, `BlockPaletteItem`, `MergeFieldGroup`, `LayoutSettings`, `StyleSettings` — map to library names (prefix `EdenTemplate`).
- Donor uses `BlockType` Dart enum (14 values). Library REPLACES this with a registry-driven String id (per locked constraint 2 + R-1). The 14 donor values become the 12 built-in registry entries (drop `approvalStamp` and `calculation` from the default registry — these are domain-specific; consumers register them if needed).
- Locked decision §7.1 analogue: template builder stays GENERIC (vertical-agnostic). No trades-specific fields in value types.
- Registry pattern follows obj 006's `EdenProcessEntityTypeRegistry` / `EdenProcessRuntimeComponentRegistry` shape exactly. See `lib/src/widgets/eden_process_canvas/process_entity_type_registry.dart` and `process_runtime_component_registry.dart` for the canonical implementation.

**Decomposition principle (per Constraint 14 of OBJECTIVE.md):** strip donor's domain commentary; map donor's enum-typed `type` field to a registry-driven String. Donor's `TemplateBlock.content: Map<String, dynamic>` stays as-is — it's the vertical-agnostic content slot. Donor's `StyleSettings.brandColor: Color = Color(0xFFD4A853)` (gold) — library default is `EdenColors.semantic.primary` or equivalent token; if no clean equivalent, hard-code `Color(0xFFD4A853)` with comment.

**Registry pattern (per Constraint 13):** both registries are singletons exposing `instance` (lazy init), `register()`, `lookup(String id) → T?`, `all() → List<T>`, `reset()`. Tests MUST call `reset()` (or `resetToDefaults()` for block-registry) in `setUp` to avoid cross-test bleed. The library ships `EdenTemplateBlockRegistry` pre-registered with 12 defaults (`text`, `field`, `table`, `list`, `photoGrid`, `signature`, `divider`, `spacer`, `image`, `qrCode`, `conditional`, `repeater`); consumers add their own. `EdenTemplateVariablesRegistry` ships EMPTY — verticals fill it.

**Variables resolver — pure function design:**
- `EdenTemplateVariablesResolver` is a **stateless class** with a single static method `resolve(String text, Map<String, dynamic> data, {bool throwOnMissing = false, String missingPlaceholder = ''}) → String`.
- Token format: `{{group.field}}` or `{{group.field.subfield}}` for nested paths.
- Regex: `RegExp(r'\{\{\s*([a-zA-Z_][a-zA-Z0-9_.]*)\s*\}\}')` — extracts the path inside `{{ ... }}`.
- For each match: split path on `.`, walk `data` recursively (`data[parts[0]][parts[1]]...`). If any intermediate is null or not a Map: missing.
- Missing token handling: if `throwOnMissing`, throw `EdenTemplateMissingVariableException(path)`; else replace with `missingPlaceholder`.
- Final substitution uses `String.replaceAllMapped` for correctness across multiple tokens.
</context>

<gotchas>
- **`EdenTemplateBlock.type` is a String, NOT an enum.** Donor types it as a Dart enum (`BlockType`); library types it as `String` (the registry-driven knob). The registry validates ids at lookup time; arbitrary strings are accepted at the value-type layer. Test: `EdenTemplateBlock(type: 'made_up_future_type', ...)` constructs without error (verification happens at the registry lookup boundary, not at construction).
- **`EdenTemplateGraph.status` is a String (NOT enum).** Donor types `status: String` ('draft' / 'published'). Library follows. Convenience getter `bool get isPublished => status == 'published'` matches donor.
- **`EdenTemplateBlock.section` IS an enum.** Header / Body / Footer is a closed set in donor (sections aren't extensible per template); library uses `enum EdenTemplateSection { header, body, footer }`. JSON serialization writes `.name` ('header' / 'body' / 'footer') to match donor wire shape (donor uses String 'header' / 'body' / 'footer' for the same field).
- **JSON round-trip for all value types.** Every value type MUST expose `toJson() → Map<String, dynamic>` and `fromJson(Map<String, dynamic>) → T` factory. Mirror the existing donor pattern (`Template.fromJson` / `toJson` at `template_model.dart:36-65`). Tests exercise round-trip.
- **`EdenTemplateLayoutSettings` page-size in donor is String ('letter' / 'a4' / 'legal').** Library uses `enum EdenTemplatePageSize { letter, a4, legal }` with `extension` exposing dimensions. JSON serializes the enum name to match donor wire format.
- **`EdenTemplateLayoutSettings.marginTop` etc. are double (inches).** Donor uses `0.75` for top/bottom and `0.5` for left/right defaults. Library keeps these. Document in dartdoc: "Margins are in inches when pageSize is letter/legal; ignored unit-wise when pageSize is a4 (which renders in mm). The interpretation is consumer's responsibility — the value is just a double."
- **`EdenTemplateStyleSettings.brandColor` default — pick a non-domain-specific neutral.** Donor uses `Color(0xFFD4A853)` (trades gold). Library default is `EdenColors.brand.primary` IF such a token exists; otherwise fallback to `Color(0xFF6750A4)` (Material 3 default primary purple) or whatever the project's default brand token is. Document in dartdoc that consumers SHOULD override per-tenant.
- **`EdenTemplateStyleSettings.fontFamily` default 'Plus Jakarta Sans'.** Match donor.
- **`EdenTemplateStyleSettings` defaults for font sizes.** Donor: headerFontSize=16.0, bodyFontSize=11.0. Library matches.
- **Don't introduce a Dart `sealed` class for value types.** Plain classes with `const` constructors + immutable fields. Avoid `freezed` / `dataclass` — new deps (Constraint 12 violation).
- **`EdenTemplateBlockDescriptor.placeholderBuilder` signature.** `Widget Function(BuildContext context, EdenTemplateBlock block)?` — optional. When null, the canvas renders the default placeholder (icon + label + subtext). When non-null, the canvas calls the builder for custom rendering. Document that this is the consumer's hook for rendering domain-specific blocks (PaymentLine, VitalsBlock, etc.).
- **Registry singleton + test isolation.** Both registries expose:
  - `EdenTemplateBlockRegistry.instance.reset()` (clears all)
  - `EdenTemplateBlockRegistry.instance.resetToDefaults()` (clears + re-registers 12 defaults)
  - `EdenTemplateVariablesRegistry.instance.reset()` (clears all)
  Tests MUST call `setUp(() { EdenTemplateBlockRegistry.instance.resetToDefaults(); EdenTemplateVariablesRegistry.instance.reset(); });`.
- **Variables resolver token regex — be careful with greedy matching.** Use `\{\{\s*([a-zA-Z_][a-zA-Z0-9_.]*)\s*\}\}` — anchors the path to a strict alphanumeric/underscore/dot character class to avoid matching across multiple tokens or unexpected content. Whitespace allowed inside `{{ }}`.
- **Nested path resolution edge.** `{{customer.address.line1}}` — split on `.` → `['customer', 'address', 'line1']`. Walk: `data['customer']` must be Map; `data['customer']['address']` must be Map; `data['customer']['address']['line1']` is the value. If any step is null or not Map (where not the final): treat as missing. If the final value is null: treat as missing. If the final value is non-null but not String: convert via `toString()` (e.g., int → '42').
- **Resolver `missingPlaceholder` default is empty string.** Donor doesn't have this concept (donor doesn't ship a resolver — net-new). Library default: `''`. Common consumer override: `'{{$path}}'` to make missing tokens visible during template authoring.
- **`EdenTemplateMissingVariableException` carries the path.** Field: `final String path;`. Useful message: `'Variable not found: $path'`.
- **`EdenTemplateBlockPaletteItem` is a presentation-layer value class, distinct from `EdenTemplateBlockDescriptor`.** Donor has both `BlockPaletteItem` (palette card render) and the implicit BlockType (registry value). Library keeps them separate: `EdenTemplateBlockDescriptor` is the registry entry (logical metadata); `EdenTemplateBlockPaletteItem` is the palette card render config (visual + display name + description for the palette grid). The palette in TRD 02 reads from the registry but uses palette-item shape for rendering.
  - Actually — given DRY concerns, we can collapse these: `EdenTemplateBlockDescriptor` already has `icon`, `displayName`, `description`, `category`, `isAdvanced` — the palette can render directly from descriptors. Decision: SKIP `EdenTemplateBlockPaletteItem` as a separate class; the palette consumes `EdenTemplateBlockDescriptor`s directly. Note this in the descriptor's dartdoc.
- **Default registry entries — 12 (not 14).** Donor enum has 14 values; library defaults are 12 (drop `approvalStamp` + `calculation` — these are domain-specific and consumers register them if needed). The 12 defaults are: text (Content), field (Content), table (Content), list (Content), photoGrid (Content), signature (Content), divider (Content), spacer (Content), image (Content), qrCode (Content), conditional (Advanced), repeater (Advanced).
- **Icon mappings — match donor.** text→`Icons.text_fields`, field→`Icons.input`, table→`Icons.table_chart_outlined`, list→`Icons.format_list_bulleted`, photoGrid→`Icons.grid_view`, signature→`Icons.draw_outlined`, divider→`Icons.horizontal_rule`, spacer→`Icons.expand`, image→`Icons.image_outlined`, qrCode→`Icons.qr_code`, conditional→`Icons.alt_route`, repeater→`Icons.repeat`.
- **Descriptions for defaults — keep short (donor pattern).** e.g. text='Static text content', field='Merge field placeholder', table='Data table layout', list='Bulleted or numbered', photoGrid='Image gallery grid', signature='Signature capture area', divider='Horizontal separator', spacer='Vertical whitespace', image='Uploaded image', qrCode='Generated QR code', conditional='Show/hide by rule', repeater='Repeating row group'.
</gotchas>

<embedded_context>
  <codebase_examples>
**Reference pattern for registry singletons** — see `lib/src/widgets/eden_process_canvas/process_entity_type_registry.dart` and `process_runtime_component_registry.dart` (obj 006). Singleton instance, reset() for tests, resetToDefaults() for the one with built-ins.

**Reference pattern for value classes with toJson/fromJson** — see donor `template_model.dart:36-65` for the `Template.fromJson` / `toJson` shape. Same idiom applies.

**Reference pattern for enum with String wire shape** — see `lib/src/widgets/eden_diagram/diagram_data.dart` and how `EdenPortSide` serializes. Use `.name` for serialize, `.firstWhere` for deserialize.

**Test wrap() helper** — see `test/widgets/eden_alert_test.dart` top — `Widget wrap(Widget child) => MaterialApp(home: Scaffold(body: child));`. Use the same pattern.

**Fixture file pattern** — see `test/widgets/eden_process_canvas/_fixtures/eden_process_models_fixtures.dart` for the hand-built fixture builder shape. Header comment `// Do NOT regenerate via LLM — hand-built fixtures for EdenTemplate<Aspect>.` is mandatory.
  </codebase_examples>

  <anti_patterns>
- Do NOT use Dart `enum` for block type (`type` field on `EdenTemplateBlock`). Enum locks the value space; the locked decision is registry-driven.
- Do NOT introduce a `freezed` or `dataclass` dependency. Plain classes only.
- Do NOT add a separate `EdenTemplateBlockPaletteItem` class — collapse with `EdenTemplateBlockDescriptor`.
- Do NOT use LLM-generated test fixtures. Hand-built only (per `no_llm_test_data` constraint).
- Do NOT use property-based testing libraries (per `no_property_based_default` constraint).
- Do NOT emit `.feature` / Cucumber files (per `no_gherkin_layer` constraint).
- Do NOT modify any file in `lib/src/widgets/eden_diagram/` or `lib/src/widgets/eden_process_canvas/`.
  </anti_patterns>

  <error_recovery>
- If a registry test bleeds (a test mutates the registry, the next test sees the mutation): verify `setUp` calls `resetToDefaults()` (block registry) or `reset()` (variables registry) per gotcha. Run tests with `--shuffle` flag to catch ordering bugs.
- If the variables resolver fails to substitute a token: check the regex matches (`{{...}}` with whitespace tolerance) and the path walk doesn't break on null intermediate values.
- If `toJson` / `fromJson` round-trip fails: enum serialization is the most likely culprit. Use `.name` for serialize and `EnumValue.values.firstWhere((e) => e.name == raw, orElse: () => defaultValue)` for deserialize.
  </error_recovery>
</embedded_context>

<file_tree>
lib/src/widgets/eden_template_builder/                              ← CREATE (new subsuite)
  template_models.dart                                              ← CREATE
  template_block_registry.dart                                      ← CREATE
  template_variables_registry.dart                                  ← CREATE
  template_variables_resolver.dart                                  ← CREATE
  eden_template_builder_exports.dart                                ← CREATE

lib/eden_ui.dart                                                    ← MODIFY (append exports under new section header)

test/widgets/eden_template_builder/                                 ← CREATE
  _fixtures/
    eden_template_models_fixtures.dart                              ← CREATE (hand-built)
  eden_template_models_test.dart                                    ← CREATE
  eden_template_block_registry_test.dart                            ← CREATE
  eden_template_variables_registry_test.dart                        ← CREATE
  eden_template_variables_resolver_test.dart                        ← CREATE
</file_tree>

## Test list (write FIRST — RED before any GREEN code)

**Value class — EdenTemplateGraph**
- `EdenTemplateGraph(id: 't1', name: 'Invoice Template', category: 'Invoice', version: 'v1.0', status: 'draft', blocks: const [])` constructs; all fields exposed.
- `graph.isPublished` returns false when status='draft'; true when status='published'.
- `graph.createdAt` and `graph.updatedAt` default null; nullable DateTime settable.
- `EdenTemplateGraph` round-trips through `toJson` / `fromJson` losslessly including nested blocks list and nullable timestamps.

**Value class — EdenTemplateBlock**
- `EdenTemplateBlock(id: 'b1', type: 'text', content: const {'label': 'Hello'}, order: 0, section: EdenTemplateSection.body)` constructs; all fields exposed.
- `EdenTemplateBlock(type: 'made_up_type', ...)` constructs without error (no enum lock — string accepted).
- `block.content` is `Map<String, dynamic>`; supports nested data (`{'rows': [...], 'columns': [...]}`).
- Round-trips through `toJson` / `fromJson` losslessly; section enum serialized as String name.

**Enum — EdenTemplateSection**
- `EdenTemplateSection.values` is [header, body, footer].
- `EdenTemplateSection.body.name == 'body'`.
- Round-trip: `EdenTemplateSection.values.firstWhere((s) => s.name == 'body') == EdenTemplateSection.body`.

**Enum — EdenTemplatePageSize**
- `EdenTemplatePageSize.values` is [letter, a4, legal].
- Extension exposes dimensions (e.g., `EdenTemplatePageSize.letter.widthInches == 8.5`, `.heightInches == 11`). For a4 in mm (`widthMm == 210`, `heightMm == 297`). For legal in inches (`widthInches == 8.5`, `heightInches == 14`).
- `EdenTemplatePageSize.letter.name == 'letter'`.

**Enum — EdenTemplateOrientation**
- `EdenTemplateOrientation.values` is [portrait, landscape].
- `.portrait.name == 'portrait'`.

**Value class — EdenTemplateLayoutSettings**
- `EdenTemplateLayoutSettings()` constructs with defaults: pageSize=letter, orientation=portrait, marginTop=0.75, marginRight=0.5, marginBottom=0.75, marginLeft=0.5, differentFirstPageHeader=true, pageNumbersInFooter=false.
- All fields settable via named params.
- Round-trips through `toJson` / `fromJson` losslessly (enums serialized as String names).

**Value class — EdenTemplateStyleSettings**
- `EdenTemplateStyleSettings()` constructs with defaults: brandColor=`Color(0xFFD4A853)` (or the chosen library default), fontFamily='Plus Jakarta Sans', headerFontSize=16.0, bodyFontSize=11.0.
- All fields settable.
- Round-trips through `toJson` / `fromJson` (Color serialized as `.value` int; deserialized via `Color(rawInt)`).

**Value class — EdenTemplateBlockDescriptor**
- `EdenTemplateBlockDescriptor(id: 'text', displayName: 'Text', description: 'Static text content', icon: Icons.text_fields, category: 'Content')` constructs.
- `isAdvanced` defaults false; settable true for the advanced category.
- `placeholderBuilder` is nullable; when null, palette/canvas use default rendering.

**Value class — EdenTemplateVariableGroup**
- `EdenTemplateVariableGroup(groupName: 'customer', fields: const ['name', 'email'])` constructs.
- Round-trips through `toJson` / `fromJson` losslessly.

**Exception — EdenTemplateMissingVariableException**
- `EdenTemplateMissingVariableException('customer.name').path == 'customer.name'`.
- `.toString()` contains the path: `'EdenTemplateMissingVariableException: Variable not found: customer.name'` (or similar).

**Registry — EdenTemplateBlockRegistry**
- `EdenTemplateBlockRegistry.instance` is a singleton (multiple accesses return same instance).
- After `resetToDefaults()`: `all().length == 12`; includes ids 'text', 'field', 'table', 'list', 'photoGrid', 'signature', 'divider', 'spacer', 'image', 'qrCode', 'conditional', 'repeater'.
- `lookup('text')` returns the text descriptor (with icon=Icons.text_fields, displayName='Text', category='Content').
- `lookup('nonexistent')` returns null.
- `byCategory('Content').length == 10`; `byCategory('Advanced').length == 2`.
- `register(EdenTemplateBlockDescriptor(id: 'payment_line', displayName: 'Payment Line', description: 'Receipt payment row', icon: Icons.attach_money, category: 'Commerce'))` then `lookup('payment_line')` returns the descriptor; `byCategory('Commerce').length == 1`.
- `reset()` clears all entries; `all().isEmpty`.
- `resetToDefaults()` re-registers the 12 defaults; `all().length == 12`.

**Registry — EdenTemplateVariablesRegistry**
- `EdenTemplateVariablesRegistry.instance` is a singleton.
- Ships EMPTY: after creation (without any operations), `all()` is empty Map.
- `register('customer', ['name', 'email', 'phone'])` then `lookup('customer') == ['name', 'email', 'phone']`.
- `register('customer', ['address'])` — APPENDS to existing fields list (does NOT replace). After: `lookup('customer') == ['name', 'email', 'phone', 'address']`. (Decision: append-semantics is more useful for additive vertical registration.)
- `all()` returns Map<String, List<String>> with all groups.
- `groups()` returns the list of group names (e.g., `['customer']`).
- `reset()` clears all; `all().isEmpty`.

**Resolver — EdenTemplateVariablesResolver.resolve**
- `resolve('Hello {{customer.name}}', {'customer': {'name': 'Mark'}})` returns `'Hello Mark'`.
- `resolve('Total: {{invoice.total}}', {'invoice': {'total': 100.50}})` returns `'Total: 100.5'` (or '100.50' depending on toString impl — assert the substring 'Total: 100.5' is present).
- `resolve('Hi {{user.first}} {{user.last}}', {'user': {'first': 'A', 'last': 'B'}})` returns `'Hi A B'` (multiple tokens).
- `resolve('{{a.b.c}}', {'a': {'b': {'c': 'deep'}}})` returns `'deep'` (nested dotted path).
- `resolve('{{ customer.name }}', {'customer': {'name': 'Mark'}})` returns `'Mark'` (whitespace inside `{{ }}` tolerated).
- `resolve('Hello {{customer.name}}', {})` returns `'Hello '` (default empty `missingPlaceholder`).
- `resolve('Hello {{customer.name}}', {}, missingPlaceholder: '???')` returns `'Hello ???'`.
- `resolve('Hello {{customer.name}}', {}, throwOnMissing: true)` throws `EdenTemplateMissingVariableException` with path='customer.name'.
- `resolve('Hello {{customer.name}}', {'customer': null})` — null intermediate — returns `'Hello '` (or throws with throwOnMissing).
- `resolve('Static text only.', {'anything': 'ignored'})` returns `'Static text only.'` (no tokens to substitute).
- `resolve('', {})` returns `''`.
- `resolve('Bad {{not-an-identifier!@#}}', {})` — token regex doesn't match — returns the literal string unchanged (`'Bad {{not-an-identifier!@#}}'`).

**Library export — eden_ui.dart**
- After this TRD lands, `import 'package:eden_ui_flutter/eden_ui.dart' show EdenTemplateGraph, EdenTemplateBlock, EdenTemplateSection, EdenTemplatePageSize, EdenTemplateOrientation, EdenTemplateLayoutSettings, EdenTemplateStyleSettings, EdenTemplateBlockDescriptor, EdenTemplateVariableGroup, EdenTemplateMissingVariableException, EdenTemplateBlockRegistry, EdenTemplateVariablesRegistry, EdenTemplateVariablesResolver` compiles clean (downstream consumer).

## Tasks

<task type="auto" tdd="true">
  <name>Create template_models.dart with value classes + enums + exception</name>
  <files>
    lib/src/widgets/eden_template_builder/template_models.dart,
    test/widgets/eden_template_builder/_fixtures/eden_template_models_fixtures.dart,
    test/widgets/eden_template_builder/eden_template_models_test.dart
  </files>
  <action>
Create the value-class module containing:

1. `EdenTemplateSection` enum: header, body, footer.
2. `EdenTemplatePageSize` enum: letter, a4, legal. Extension `EdenTemplatePageSizeDimensions` exposing `widthInches`, `heightInches` (for letter/legal) and `widthMm`, `heightMm` (for a4). For other pageSize values, the "wrong unit" getter returns `double.nan` or throws — document behavior.
3. `EdenTemplateOrientation` enum: portrait, landscape.
4. `EdenTemplateGraph` value class — const constructor, all fields immutable. Required: id (String), name (String), category (String), version (String), status (String), blocks (List<EdenTemplateBlock>). Optional: createdAt (DateTime?), updatedAt (DateTime?). Getter `bool get isPublished => status == 'published';`. `toJson()` + `factory EdenTemplateGraph.fromJson(Map<String, dynamic> json)`. `==` + `hashCode` based on `id`.
5. `EdenTemplateBlock` value class — const constructor. Required: id (String), type (String), content (Map<String, dynamic>), order (int), section (EdenTemplateSection). `toJson()` + `factory fromJson`. Section serializes as `.name`. `==` + `hashCode` based on `id`.
6. `EdenTemplateLayoutSettings` value class — const constructor with defaults per gotchas (pageSize=letter, orientation=portrait, marginTop=0.75, marginRight=0.5, marginBottom=0.75, marginLeft=0.5, differentFirstPageHeader=true, pageNumbersInFooter=false). `toJson` + `fromJson` (enum names).
7. `EdenTemplateStyleSettings` value class — const constructor with defaults (brandColor=`Color(0xFFD4A853)` with comment `// donor color — keep until token system has equivalent`, fontFamily='Plus Jakarta Sans', headerFontSize=16.0, bodyFontSize=11.0). `toJson` (Color as int via `.value`) + `fromJson` (Color via `Color(json['brandColor'] as int)`).
8. `EdenTemplateBlockDescriptor` value class — const constructor. Required: id, displayName, description, icon (IconData), category. Optional: isAdvanced (bool default false), placeholderBuilder (`Widget Function(BuildContext, EdenTemplateBlock)?`).
9. `EdenTemplateVariableGroup` value class — const constructor. Required: groupName (String), fields (List<String>). `toJson` + `fromJson`.
10. `EdenTemplateMissingVariableException` exception — final field `path: String`, `@override String toString() => 'EdenTemplateMissingVariableException: Variable not found: $path';`.

Fixture file `eden_template_models_fixtures.dart`:
```dart
// Do NOT regenerate via LLM — hand-built fixtures for EdenTemplateModels.

EdenTemplateGraph buildGraphFixture({
  String id = 't1',
  String name = 'Invoice Template',
  String category = 'Invoice',
  String version = 'v1.0',
  String status = 'draft',
  List<EdenTemplateBlock> blocks = const [],
  DateTime? createdAt,
  DateTime? updatedAt,
}) => EdenTemplateGraph(id: id, name: name, category: category, version: version, status: status, blocks: blocks, createdAt: createdAt, updatedAt: updatedAt);

EdenTemplateBlock buildBlockFixture({
  String id = 'b1',
  String type = 'text',
  Map<String, dynamic> content = const {'label': 'Sample'},
  int order = 0,
  EdenTemplateSection section = EdenTemplateSection.body,
}) => EdenTemplateBlock(id: id, type: type, content: content, order: order, section: section);

// Add similar builders for LayoutSettings, StyleSettings, BlockDescriptor, VariableGroup.
```

Test file `eden_template_models_test.dart` covers every test-list item under "Value class — EdenTemplateGraph", "Value class — EdenTemplateBlock", "Enum — EdenTemplateSection", "Enum — EdenTemplatePageSize", "Enum — EdenTemplateOrientation", "Value class — EdenTemplateLayoutSettings", "Value class — EdenTemplateStyleSettings", "Value class — EdenTemplateBlockDescriptor", "Value class — EdenTemplateVariableGroup", "Exception — EdenTemplateMissingVariableException".

# CRITICAL: Hand-built fixtures only — no LLM-generated test data.
# GOTCHA: Section enum serializes as .name to match donor wire shape.
# GOTCHA: Color serialization — toJson uses .value (int); fromJson wraps with Color(...).
# PATTERN: Mirror obj 006's process_models.dart shape.

Order RED → GREEN strictly: write ONE test → run → assert it fails → write the minimal value class field/method → run → assert it passes → repeat. One test at a time per playbook habit 3.
  </action>
  <verify>
`flutter test test/widgets/eden_template_builder/eden_template_models_test.dart` passes.
`flutter analyze lib/src/widgets/eden_template_builder/template_models.dart` clean.
Test count: ~25-35 tests covering all value class fields + JSON round-trip + enum serialization.
Parity rows satisfied: G-1, G-2, G-4, G-5, L-2, L-3, L-4, L-5, L-6, S-5.
  </verify>
  <done>
template_models.dart compiles; all value classes + enums + exception class defined with toJson/fromJson where applicable; all tests in eden_template_models_test.dart pass; fixture file has the mandatory header comment.
  </done>
  <recovery>
If JSON round-trip fails for an enum: check `.name` is used in toJson and `Values.firstWhere((v) => v.name == raw)` in fromJson with a sensible default.
If Color round-trip fails: check `toJson` outputs `.value` (an int) and `fromJson` wraps with `Color(...)`.
If a test mutates a default and the next test sees the mutation: value classes are const and immutable; this shouldn't happen — re-check the test isn't sharing a mutable Map fixture.
  </recovery>
</task>

<task type="auto" tdd="true">
  <name>Create both registries (block + variables) with singleton + reset</name>
  <files>
    lib/src/widgets/eden_template_builder/template_block_registry.dart,
    lib/src/widgets/eden_template_builder/template_variables_registry.dart,
    test/widgets/eden_template_builder/eden_template_block_registry_test.dart,
    test/widgets/eden_template_builder/eden_template_variables_registry_test.dart
  </files>
  <action>
Create the two singleton registries.

**`EdenTemplateBlockRegistry`** in `template_block_registry.dart`:
- Private constructor `EdenTemplateBlockRegistry._()`.
- Static `instance` (lazy via `static final EdenTemplateBlockRegistry instance = EdenTemplateBlockRegistry._()..resetToDefaults();`).
- Private `_descriptors: Map<String, EdenTemplateBlockDescriptor> = {};`.
- `void register(EdenTemplateBlockDescriptor descriptor)` — adds/overwrites by id.
- `EdenTemplateBlockDescriptor? lookup(String id)` — returns null if missing.
- `List<EdenTemplateBlockDescriptor> all()` — sorted by category then displayName.
- `List<EdenTemplateBlockDescriptor> byCategory(String category)` — filtered by category.
- `void reset()` — clears.
- `void resetToDefaults()` — clears + registers the 12 defaults (text, field, table, list, photoGrid, signature, divider, spacer, image, qrCode for Content category; conditional, repeater for Advanced category). Icons and descriptions per gotchas.

**`EdenTemplateVariablesRegistry`** in `template_variables_registry.dart`:
- Private constructor `EdenTemplateVariablesRegistry._()`.
- Static `instance` (lazy; ships EMPTY — no resetToDefaults).
- Private `_groups: Map<String, List<String>> = {};`.
- `void register(String group, List<String> fields)` — APPENDS to existing group (does NOT replace). Implementation: `_groups.putIfAbsent(group, () => []).addAll(fields);`.
- `List<String>? lookup(String group)` — returns null if missing.
- `Map<String, List<String>> all()` — copy of internal map.
- `List<String> groups()` — list of group names sorted alphabetically.
- `void reset()` — clears.

Tests cover every "Registry — EdenTemplateBlockRegistry" and "Registry — EdenTemplateVariablesRegistry" item from the test list. CRITICAL: `setUp(() { EdenTemplateBlockRegistry.instance.resetToDefaults(); EdenTemplateVariablesRegistry.instance.reset(); });` in BOTH test files to prevent bleed.

# CRITICAL: Both registries are singletons — tests MUST reset in setUp.
# GOTCHA: Variables registry register() appends, doesn't replace — verticals can build up incrementally.
# PATTERN: Mirror obj 006's process_entity_type_registry.dart shape.
  </action>
  <verify>
`flutter test test/widgets/eden_template_builder/eden_template_block_registry_test.dart` passes.
`flutter test test/widgets/eden_template_builder/eden_template_variables_registry_test.dart` passes.
`flutter analyze` clean for both registry files.
Test count: ~15 + ~10 = 25 tests.
Parity rows satisfied: R-1 (block registry), R-2 (variables registry), B-1..B-12 (block defaults).
  </verify>
  <done>
Both registries compile and pass all tests; default block registry has 12 entries after resetToDefaults; variables registry ships empty and supports append-register semantics.
  </done>
  <recovery>
If tests bleed (a test sees state from a previous test): check `setUp` calls. Run with `--shuffle` to validate.
If `byCategory('Content').length` is not 10 or `byCategory('Advanced').length` is not 2: re-count the defaults — should be 10 Content (text, field, table, list, photoGrid, signature, divider, spacer, image, qrCode) + 2 Advanced (conditional, repeater) = 12 total.
  </recovery>
</task>

<task type="auto" tdd="true">
  <name>Create variables resolver pure function + tests</name>
  <files>
    lib/src/widgets/eden_template_builder/template_variables_resolver.dart,
    test/widgets/eden_template_builder/eden_template_variables_resolver_test.dart
  </files>
  <action>
Create `EdenTemplateVariablesResolver` static class with single static method `resolve`.

```dart
class EdenTemplateVariablesResolver {
  EdenTemplateVariablesResolver._();

  static final _tokenPattern = RegExp(r'\{\{\s*([a-zA-Z_][a-zA-Z0-9_.]*)\s*\}\}');

  static String resolve(
    String text,
    Map<String, dynamic> data, {
    bool throwOnMissing = false,
    String missingPlaceholder = '',
  }) {
    return text.replaceAllMapped(_tokenPattern, (match) {
      final path = match.group(1)!;
      final value = _walkPath(path, data);
      if (value == null) {
        if (throwOnMissing) {
          throw EdenTemplateMissingVariableException(path);
        }
        return missingPlaceholder;
      }
      return value.toString();
    });
  }

  static Object? _walkPath(String path, Map<String, dynamic> data) {
    final parts = path.split('.');
    Object? current = data;
    for (final part in parts) {
      if (current is! Map) return null;
      current = current[part];
      if (current == null) return null;
    }
    return current;
  }
}
```

Tests cover every "Resolver — EdenTemplateVariablesResolver.resolve" item from the test list:
1. Happy path: simple token substitution.
2. Multiple tokens.
3. Nested dotted path.
4. Whitespace inside `{{ }}`.
5. Missing token — default empty placeholder.
6. Missing token — custom placeholder.
7. Missing token — throwOnMissing throws with correct path.
8. Null intermediate — treated as missing.
9. No tokens — returns input unchanged.
10. Empty input.
11. Token with invalid characters (`{{not-an-identifier!@#}}`) — regex doesn't match — literal preserved.
12. Numeric value substitution (toString conversion).

# CRITICAL: Regex anchors `[a-zA-Z_][a-zA-Z0-9_.]*` — must not match across multiple tokens.
# GOTCHA: Walking past a non-Map intermediate must return null, not throw.
# GOTCHA: `value.toString()` — for ints, doubles, bools, this gives canonical String; for nested Maps/Lists, gives Dart's default toString (often not desired — but per scope, acceptable v1).
# PATTERN: Pure static class with private constructor — no instance state.
  </action>
  <verify>
`flutter test test/widgets/eden_template_builder/eden_template_variables_resolver_test.dart` passes.
`flutter analyze lib/src/widgets/eden_template_builder/template_variables_resolver.dart` clean.
Test count: ~12-15 tests covering all happy / edge / failure cases.
Parity row satisfied: V-6.
  </verify>
  <done>
Resolver compiles; all 12+ tests pass; regex correctly tolerates whitespace and rejects non-identifier paths; throwOnMissing throws with correct path.
  </done>
  <recovery>
If the regex matches across multiple tokens (greedy bug): ensure the character class is strict (`[a-zA-Z0-9_.]*` — NO whitespace or other chars).
If `throwOnMissing: true` throws with wrong path: log `match.group(1)` and verify it's the path inside `{{ }}` without whitespace.
If numeric substitution produces weird output: `100.50` toString is `'100.5'` (Dart strips trailing zero) — assert with `contains('100.5')` not equality.
  </recovery>
</task>

<task type="auto" tdd="true">
  <name>Add public exports + wire into eden_ui.dart</name>
  <files>
    lib/src/widgets/eden_template_builder/eden_template_builder_exports.dart,
    lib/eden_ui.dart
  </files>
  <action>
Create `eden_template_builder_exports.dart`:

```dart
export 'template_models.dart';
export 'template_block_registry.dart';
export 'template_variables_registry.dart';
export 'template_variables_resolver.dart';
```

Append to `lib/eden_ui.dart` under a NEW section header:

```dart
// ---------------------------------------------------------------------------
// Objective 021 — Template builder Wave 1
// ---------------------------------------------------------------------------
export 'src/widgets/eden_template_builder/eden_template_builder_exports.dart';
```

Verify the consumer-facing import works by reading `lib/eden_ui.dart` and confirming the new export line is appended without breaking existing exports.

# CRITICAL: Append, do NOT modify existing exports (Constraint 11 — no breaking changes).
# GOTCHA: File-collision discipline — only THIS TRD writes the Wave 1 section header to eden_ui.dart for obj 021. Later TRDs in the same wave (none here — Wave 1 is just this TRD) would append under the same header.
# PATTERN: Mirror obj 006's eden_ui.dart export pattern.
  </action>
  <verify>
`flutter test` passes (full library test suite; no regressions).
`flutter analyze` clean.
`grep -c "Objective 021" lib/eden_ui.dart` returns 1 (exactly one section header).
Importing `package:eden_ui_flutter/eden_ui.dart` and using `EdenTemplateGraph`, `EdenTemplateBlock`, etc. compiles clean (verified by the existing test suite running).
  </verify>
  <done>
eden_ui.dart has the Wave 1 export section appended; all 12 new value class / enum / registry / resolver symbols are publicly exported via the barrel; full library test suite still passes; flutter analyze clean.
  </done>
  <recovery>
If `flutter analyze` flags an unused export warning: the export line in `eden_template_builder_exports.dart` is wrong (typo in file name) — verify each file exists and is named exactly as listed.
If a downstream test breaks (e.g., obj 006 test): confirm we did NOT modify any obj 006 file — `git diff lib/src/widgets/eden_diagram/ lib/src/widgets/eden_process_canvas/` should be empty.
  </recovery>
</task>

<verification>
**Validation gates (per OBJECTIVE.md success criteria):**
- `flutter test` (full library suite) passes — no regressions to existing ~445+ tests.
- `flutter analyze` clean.
- `grep -r "pubspec.yaml" --include="pubspec.yaml" pubspec.yaml | wc -l` shows no new deps added (manual inspection of `pubspec.yaml` git diff confirms zero adds).
- All parity rows satisfied this TRD: G-1, G-2, G-3 (JSON round-trip), G-4, G-5, L-2, L-3, L-4, L-5, L-6, S-5, R-1, R-2, V-6, B-1..B-12 (default block descriptors).
- All test files use the mandatory fixture header comment `// Do NOT regenerate via LLM — hand-built fixtures for EdenTemplate<Aspect>.`.
- Tests use `setUp(() { ...registry.resetToDefaults(); ...variables.reset(); });` to prevent bleed.
</verification>

<success_criteria>
- 4 source files in `lib/src/widgets/eden_template_builder/` + 1 exports barrel + 1 modification to `lib/eden_ui.dart`.
- 5 test files in `test/widgets/eden_template_builder/` (4 + 1 fixtures file).
- ~50-60 new tests added; all pass.
- `flutter analyze` clean across all new files.
- No pubspec changes.
- No modifications to obj 006 files.
- Public API ready for Wave 2 consumption: `EdenTemplateGraph`, `EdenTemplateBlock`, `EdenTemplateSection`, `EdenTemplatePageSize`, `EdenTemplateOrientation`, `EdenTemplateLayoutSettings`, `EdenTemplateStyleSettings`, `EdenTemplateBlockDescriptor`, `EdenTemplateVariableGroup`, `EdenTemplateMissingVariableException`, `EdenTemplateBlockRegistry`, `EdenTemplateVariablesRegistry`, `EdenTemplateVariablesResolver`.
</success_criteria>
