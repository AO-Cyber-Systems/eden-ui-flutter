---
objective: 009-vertical-theme-system
trd: "009-01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/src/theme/eden_theme_profile.dart
  - lib/src/theme/eden_theme_profile_scope.dart
  - test/theme/eden_theme_profile_test.dart
  - test/theme/eden_theme_profile_scope_test.dart
  - test/theme/_fixtures/profile_fixtures.dart
  - lib/eden_ui.dart
co_modified_files:
  - lib/eden_ui.dart
autonomous: true
must_haves:
  - "EdenThemeProfile enum is defined with EXACTLY 5 values (locked per OBJECTIVE.md Constraint 11): commercialWarm, medicalInstitutional, govFederal, retailVibrant, legalProfessional. No more, no less."
  - "EdenThemeProfileData is a const-able immutable value class carrying per-profile token deltas: primaryColor (MaterialColor), surfaceTonalSeed (Color), radiusMultiplier (double, default 1.0), density (EdenThemeProfileDensity enum: comfortable | dense — informational only in v1, NOT yet wired to spacing tokens), minimumTouchTargetPx (double, default 0 = no override; govFederal = 48), bodyFontFamily (String?, null = use default Plus Jakarta Sans), displayFontFamily (String?, null = use default Outfit), monoFontFamily (String?, null = use default JetBrains Mono), preferBorderOverShadow (bool, default false; govFederal = true; medicalInstitutional = true)."
  - "EdenThemeProfile has a `data` getter (extension or static lookup) returning the canonical EdenThemeProfileData for that profile. Five static instances: EdenThemeProfileData.commercialWarmData, .medicalInstitutionalData, .govFederalData, .retailVibrantData, .legalProfessionalData."
  - "commercialWarm profile DEFAULT data MUST reproduce today's behavior: primaryColor = EdenColors.gold, surfaceTonalSeed = EdenColors.neutral[50]!, radiusMultiplier = 1.0, density = comfortable, minimumTouchTargetPx = 0, all font families = null (use defaults), preferBorderOverShadow = false. This is the backwards-compat lock — any drift breaks Constraint 1."
  - "EdenThemeProfileScope is an InheritedWidget exposing an EdenThemeProfile value via .of(context) and .maybeOf(context). Standard InheritedWidget contract: updateShouldNotify returns true when the profile changes. .maybeOf returns null when no scope is present (graceful default for widgets used outside any wrapper)."
  - "EdenThemeProfile.commercialWarm is the documented default — both InheritedWidget callers and any later resolver default to it when no scope is present (consumer can opt-in via EdenAdaptiveTheme TRD 009-05 wrapper)."
  - "All 5 profile data instances compile + return non-null primaryColor, non-null surfaceTonalSeed, valid radiusMultiplier (>0), valid minimumTouchTargetPx (>=0)."
  - "Exports added to lib/eden_ui.dart under new section header '// Objective 009 — Vertical Theme System'. Exports: eden_theme_profile.dart + eden_theme_profile_scope.dart."
  - "No new pubspec dependencies (Constraint 2). No imports from packages other than flutter/material.dart + the existing token files (colors/radii/typography)."
---

<objective>
Ship the foundational `EdenThemeProfile` enum + `EdenThemeProfileData` immutable value class + `EdenThemeProfileScope` `InheritedWidget`. This is pure-data + context plumbing — NO `ThemeData` construction, NO `EdenStatusPalette`, NO `EdenAdaptiveTheme` wiring yet. Those land in 009-02 / 009-05.

The 5 LOCKED profile values per `VERTICAL_UX_RESEARCH_2026-05-16.md` §2.4.2 + OBJECTIVE.md Constraint 11:

```dart
enum EdenThemeProfile {
  /// Default — today's behavior. Warm gold on neutral slate.
  /// Salon, retail mid-market, consumer-tilt verticals.
  commercialWarm,

  /// Teal/cyan institutional. Sharper corners, less shadow, IBM Plex Sans body.
  /// Medical, healthtech.
  medicalInstitutional,

  /// Federal navy + USWDS conformance. Public Sans, 4pt radii,
  /// border-only surfaces, ≥48pt touch floor. DHHS / DOD / federal civilian.
  govFederal,

  /// High-saturation primary + brand-color totals + cart accents.
  /// Mass-market retail, Shopify POS-style brand expression.
  retailVibrant,

  /// Navy primary + serif display opt-in + restrained accent.
  /// Traditional law firm gravitas.
  legalProfessional,
}
```

`EdenThemeProfileData` carries the per-profile token deltas. Const-able. Static instances per profile:

```dart
@immutable
class EdenThemeProfileData {
  const EdenThemeProfileData({
    required this.profile,
    required this.primaryColor,
    required this.surfaceTonalSeed,
    this.radiusMultiplier = 1.0,
    this.density = EdenThemeProfileDensity.comfortable,
    this.minimumTouchTargetPx = 0,
    this.bodyFontFamily,
    this.displayFontFamily,
    this.monoFontFamily,
    this.preferBorderOverShadow = false,
  });

  final EdenThemeProfile profile;
  final MaterialColor primaryColor;
  final Color surfaceTonalSeed;
  final double radiusMultiplier;
  final EdenThemeProfileDensity density;
  final double minimumTouchTargetPx;
  final String? bodyFontFamily;
  final String? displayFontFamily;
  final String? monoFontFamily;
  final bool preferBorderOverShadow;

  static const EdenThemeProfileData commercialWarmData = EdenThemeProfileData(
    profile: EdenThemeProfile.commercialWarm,
    primaryColor: EdenColors.gold,
    surfaceTonalSeed: Color(0xFFFAFAFA), // EdenColors.neutral[50] expanded for const
    // all other fields defaults — this is the back-compat anchor
  );
  // ... + 4 more
}
```

`EdenThemeProfileScope` is a standard `InheritedWidget`:

```dart
class EdenThemeProfileScope extends InheritedWidget {
  const EdenThemeProfileScope({
    super.key,
    required this.profile,
    required super.child,
  });

  final EdenThemeProfile profile;

  static EdenThemeProfile of(BuildContext context) {
    final scope = context.dependOnInheritedWidgetOfExactType<EdenThemeProfileScope>();
    assert(scope != null, 'EdenThemeProfileScope.of called without a scope ancestor. '
      'Wrap your app in EdenAdaptiveTheme or call EdenThemeProfileScope.maybeOf instead.');
    return scope?.profile ?? EdenThemeProfile.commercialWarm;
  }

  static EdenThemeProfile? maybeOf(BuildContext context) {
    return context.dependOnInheritedWidgetOfExactType<EdenThemeProfileScope>()?.profile;
  }

  @override
  bool updateShouldNotify(EdenThemeProfileScope oldWidget) => profile != oldWidget.profile;
}
```

**Per-profile data values (carefully sourced from research §2.4 + USWDS tokens approximation):**

| Profile | primaryColor | surfaceTonalSeed | radiusMultiplier | minimumTouchTargetPx | bodyFontFamily | displayFontFamily | preferBorderOverShadow |
|---|---|---|---|---|---|---|---|
| commercialWarm | EdenColors.gold | EdenColors.neutral[50] (0xFFFAFAFA) | 1.0 | 0 | null | null | false |
| medicalInstitutional | EdenColors.cyan | Color(0xFFF0FDFA) (teal-ish white) | 0.667 (12→8pt) | 0 | 'IBM Plex Sans' | null (keep Outfit) | true |
| govFederal | EdenColors.blue (used as proxy for navy; actual rendering uses [900] shade) | Color(0xFFF8FAFC) (slate-50) | 0.333 (12→4pt) | 48.0 | 'Public Sans' | 'Public Sans' | true |
| retailVibrant | EdenColors.purple | EdenColors.neutral[50] (0xFFFAFAFA) | 1.0 | 0 | null | null | false |
| legalProfessional | EdenColors.slate | Color(0xFFF8FAFC) | 1.0 | 0 | null | 'Crimson Pro' | false |

**Out of scope for this TRD:** `EdenStatusPalette` ThemeExtension (→ 009-02), `EdenBrandPreset` registry (→ 009-03), `EdenProfileFonts` resolver (→ 009-04), `EdenAdaptiveTheme` widget (→ 009-05), any `ThemeData` construction.
</objective>

<context>
**Why this is a `type: tdd` TRD with a `## Test list` section** (per OBJECTIVE.md Constraint 5 + global Playbook habits 1 + 2): even though this is "just" data + an InheritedWidget, the back-compat lock on `commercialWarmData` (Constraint 1 from OBJECTIVE.md — pixel parity with today) means the canonical data values are a CONTRACT, not an implementation detail. Test-list-first nails the contract before the data table is written.

**Why per-profile font families are STRINGS not enum values:** keeps the data class const-able. `GoogleFonts.getFont(familyName)` resolution happens in TRD 009-04. Here we only carry the family name string.

**Why `EdenThemeProfileDensity` enum is declared but NOT wired:** per OBJECTIVE.md Constraint 12 (no density work in this objective). The enum exists so future v2 density work doesn't break the `EdenThemeProfileData` API. Default = `comfortable`. Dense is reserved for future profiles (e.g., `commercialDense` for trades dispatcher); none of the 5 v1 profiles use `dense` in their canonical data.

**Why `EdenColors.cyan` proxies for medical teal:** the research doc spec'd a #0F766E teal-700. `EdenColors.cyan[700] = #0E7490` is the closest existing token. Using `EdenColors.cyan` keeps profile data referencing existing tokens per aesthetic-preservation principle 1 from §6 of the research doc. A future v2 can add a dedicated `EdenColors.teal` MaterialColor if needed.

**Why `EdenColors.blue` proxies for gov navy:** the research doc spec'd #1E3A8A navy. `EdenColors.blue[900] = #1E3A8A` exactly matches. Using `EdenColors.blue` as the MaterialColor (entire ramp) lets gov-profile theme construction in TRD 009-05 pick `EdenColors.blue[900]` for primary, `EdenColors.blue[100]` for primaryContainer, etc. The full ramp matters.

**Const initialisation gotcha:** `EdenColors.neutral[50]!` is NOT a const expression because `MaterialColor.[]` is a method call. For const constructors we must use explicit `Color(0xFFFAFAFA)` literals. This is why the table above shows hex values not `EdenColors.X[Y]` lookups. The hex values are EXACTLY the values defined in `lib/src/tokens/colors.dart` for the cited shade — change those and back-compat breaks. Document this invariant inline in the data file.

**Donor reference for `EdenThemeProfileScope` shape:** mirror existing `EdenThemeSelector` (lib/src/widgets/eden_theme_selector.dart, already exported) and `EdenAppModeScope` (lib/src/widgets/eden_app_mode.dart from objective 002). Both established the InheritedWidget pattern + `.of` / `.maybeOf` static accessors. Read those two files before writing the scope class.

**Catalog mount:** NONE in this TRD. The catalog screen lands in TRD 009-05.
</context>

<gotchas>
- **`Color` is const-able; `MaterialColor[X]` is NOT.** Static const fields of `EdenThemeProfileData` MUST use `Color(0xFFFAFAFA)` literals for `surfaceTonalSeed`, NOT `EdenColors.neutral[50]!`. Document this in a sidebar comment so future contributors don't "simplify" the literals back into lookups (which would silently break `const` and force runtime construction).
- **`primaryColor: MaterialColor` IS const-able when the MaterialColor itself is `const` (EdenColors.gold IS `static const`).** So `primaryColor: EdenColors.gold` works in const constructors. Verified against `lib/src/tokens/colors.dart` — all preset MaterialColors are `const MaterialColor(...)`.
- **`EdenThemeProfile.commercialWarm` must be the FIRST enum value (Dart enum ordinal = 0).** The OBJECTIVE.md Constraint 1 back-compat lock implies "default profile is the first one" if any code does `EdenThemeProfile.values.first` (none should, but defensive ordering matters).
- **Do NOT add a `copyWith` method to `EdenThemeProfileData` in this TRD.** It will be needed in TRD 009-05 for `EdenAdaptiveTheme.light(profile, brand)` to override `primaryColor` per-brand, but adding it here invites premature API surface. Add it in 009-05 alongside its first consumer.
- **`InheritedWidget.updateShouldNotify` returning true based on `profile != oldWidget.profile`** — this is enum identity comparison; `==` works on enums. Verified.
- **DO NOT export `_fixtures/profile_fixtures.dart` from `lib/eden_ui.dart`.** Fixtures are test-only.
- **`assert` in `EdenThemeProfileScope.of` only fires in debug builds.** That's the intended behavior — release builds silently default to `commercialWarm` via the `?? EdenThemeProfile.commercialWarm`. Document this explicitly.
- **`EdenThemeProfileDensity` enum is declared but not consumed elsewhere in v1.** Linter will likely flag it as unused. Add `// ignore: unused_field` near the data class's `density` field reference OR rely on the static data instances (`commercialWarmData` etc.) referencing it to keep it live. Static data instances using `density: EdenThemeProfileDensity.comfortable` will keep the enum live.
</gotchas>

<anti_patterns>
- Do NOT make `EdenThemeProfileData` mutable. It's a const-able value class. No setters; no late finals.
- Do NOT add per-profile factory constructors like `EdenThemeProfileData.medical()`. Use static const data instances only (`EdenThemeProfileData.medicalInstitutionalData`). Factory constructors couple data-shape evolution to factory shape evolution; static instances don't.
- Do NOT generate the profile data table by LLM. Hand-build each row per the table in `<objective>` (resolver `no_llm_test_data` constraint + Playbook habit 4).
- Do NOT add an `EdenThemeProfile.custom` value. The 5 profiles are LOCKED (OBJECTIVE.md Constraint 11). Custom profiles are a v2 concern requiring schema + registry, not a single enum value.
- Do NOT wire fonts in this TRD. The String? font family fields are CARRIERS only — TRD 009-04 resolves them to TextStyles via GoogleFonts.
- Do NOT couple `EdenThemeProfileScope` to a specific `ThemeData`. The scope ONLY carries the enum; theme construction is TRD 009-05's job.
</anti_patterns>

<error_recovery>
- If a static const `EdenThemeProfileData` instance fails to compile with "Const constructors can't have body" or "Not a constant expression": every field default must be a const expression. Check for `EdenColors.neutral[50]!` (not const) — replace with `Color(0xFFFAFAFA)` literal.
- If `EdenThemeProfileScope.of(context)` returns null in debug but no assert fires: confirm `dependOnInheritedWidgetOfExactType<EdenThemeProfileScope>()` is being called with the EXACT type (no typo in generic) and that the scope is actually an ancestor of the context. If the test uses `tester.element(find.byType(EdenButton)).context` style lookup, ensure the lookup happens AFTER pumping the widget tree including the scope.
- If the back-compat assertion test fails (commercialWarmData drifts): roll back the change. The values in the table in `<objective>` are LOCKED. Test enforces the contract — don't relax the test.
- If linter flags `EdenThemeProfileDensity` as unused: ensure at least one of the static const data instances references `EdenThemeProfileDensity.comfortable` (the default). All 5 should — verify with grep.
</error_recovery>

<codebase_examples>
**Existing `InheritedWidget` pattern (mirror for `EdenThemeProfileScope`):**

```dart
// lib/src/widgets/eden_app_mode.dart (objective 002, shipped)
class EdenAppModeScope extends InheritedNotifier<EdenAppModeController> {
  const EdenAppModeScope({
    super.key,
    required EdenAppModeController controller,
    required super.child,
  }) : super(notifier: controller);

  static EdenAppMode of(BuildContext context) {
    final scope = context.dependOnInheritedWidgetOfExactType<EdenAppModeScope>();
    assert(scope != null, ...);
    return scope!.notifier!.mode;
  }

  static EdenAppMode? maybeOf(BuildContext context) {
    return context.dependOnInheritedWidgetOfExactType<EdenAppModeScope>()?.notifier?.mode;
  }
}
```

Our `EdenThemeProfileScope` is SIMPLER (no notifier; just a value). Use plain `InheritedWidget`, not `InheritedNotifier`. Profile changes happen by rebuilding with a new `EdenThemeProfileScope(profile: newProfile, child: ...)` — the parent (typically `EdenAdaptiveTheme` from TRD 009-05) rebuilds when the consumer app changes the profile prop.

**Existing token reference pattern:**

```dart
// lib/src/tokens/colors.dart — MaterialColors are static const
static const MaterialColor gold = MaterialColor(0xFFD4A853, <int, Color>{ ... });
```

Use `EdenColors.gold` (or `.cyan`, `.blue`, `.purple`, `.slate`) directly in const constructors — they ARE const expressions because `MaterialColor` is a const constructor.
</codebase_examples>

<file_tree>
```
lib/src/theme/
├── eden_theme.dart                    (existing — untouched in this TRD; extended in 009-02 + 009-05)
├── eden_theme_profile.dart            ← CREATE (enum + EdenThemeProfileData + EdenThemeProfileDensity)
└── eden_theme_profile_scope.dart      ← CREATE (InheritedWidget)

test/theme/
├── _fixtures/
│   └── profile_fixtures.dart          ← CREATE (hand-built test scenarios; "Do NOT regenerate via LLM" header)
├── eden_theme_profile_test.dart       ← CREATE (data + back-compat contract tests)
└── eden_theme_profile_scope_test.dart ← CREATE (InheritedWidget contract tests)

lib/
└── eden_ui.dart                       ← MODIFY (append Objective 009 section header + 2 exports)
```
</file_tree>

## Test list

Per global TDD Playbook habit 2 — behavior cases enumerated BEFORE any test code is written. Outside-in ordering per habit 5: public API contract → derived getters → edge cases.

**Outermost layer — public enum + InheritedWidget contract:**
1. `EdenThemeProfile` enum has EXACTLY 5 values in this exact order: commercialWarm, medicalInstitutional, govFederal, retailVibrant, legalProfessional.
2. `EdenThemeProfileScope.maybeOf(context)` returns null when no scope ancestor exists.
3. `EdenThemeProfileScope.maybeOf(context)` returns the wrapped profile when a scope ancestor exists.
4. `EdenThemeProfileScope.of(context)` returns the wrapped profile when scope is present.
5. `EdenThemeProfileScope.of(context)` triggers `assert` in debug mode AND defaults to `commercialWarm` in release mode when no scope is present (assert-or-default contract).
6. `EdenThemeProfileScope` rebuilds descendants when the wrapped profile changes (`updateShouldNotify` returns true on profile change, false on same profile).

**Middle layer — per-profile data lookup contract:**
7. Each of the 5 profiles has a corresponding static `EdenThemeProfileData` instance accessible via `EdenThemeProfileData.{profile}Data`.
8. `EdenThemeProfileData.commercialWarmData.profile == EdenThemeProfile.commercialWarm` (self-referential profile field is set correctly per instance; assert this for all 5).
9. All 5 profile data instances expose non-null `primaryColor`, non-null `surfaceTonalSeed`, `radiusMultiplier > 0`, `minimumTouchTargetPx >= 0`.

**Innermost layer — back-compat anchor (CRITICAL — Constraint 1 of OBJECTIVE.md):**
10. `EdenThemeProfileData.commercialWarmData.primaryColor` identity-equals `EdenColors.gold` (back-compat lock — `commercialWarm` reproduces today).
11. `EdenThemeProfileData.commercialWarmData.surfaceTonalSeed.value == 0xFFFAFAFA` (matches `EdenColors.neutral[50]` hex).
12. `EdenThemeProfileData.commercialWarmData.radiusMultiplier == 1.0`.
13. `EdenThemeProfileData.commercialWarmData.minimumTouchTargetPx == 0`.
14. `EdenThemeProfileData.commercialWarmData.bodyFontFamily == null` AND `.displayFontFamily == null` AND `.monoFontFamily == null`.
15. `EdenThemeProfileData.commercialWarmData.preferBorderOverShadow == false`.
16. `EdenThemeProfileData.commercialWarmData.density == EdenThemeProfileDensity.comfortable`.

**Per-profile data correctness (per the locked table in `<objective>`):**
17. `medicalInstitutionalData.primaryColor === EdenColors.cyan`, `radiusMultiplier == 0.667`, `bodyFontFamily == 'IBM Plex Sans'`, `preferBorderOverShadow == true`.
18. `govFederalData.primaryColor === EdenColors.blue`, `radiusMultiplier == 0.333`, `minimumTouchTargetPx == 48.0`, `bodyFontFamily == 'Public Sans'`, `displayFontFamily == 'Public Sans'`, `preferBorderOverShadow == true`.
19. `retailVibrantData.primaryColor === EdenColors.purple`, `radiusMultiplier == 1.0`, all font families null.
20. `legalProfessionalData.primaryColor === EdenColors.slate`, `displayFontFamily == 'Crimson Pro'`, `bodyFontFamily == null`.

**Const-ness check:**
21. `const EdenThemeProfileScope(profile: EdenThemeProfile.commercialWarm, child: ...)` compiles as a const expression (verified by a `const` constructor invocation in a test).

<tasks>

<task type="auto" tdd="true">
<name>Task 1 — Hand-build fixture file + RED contract tests (enum + scope behavior)</name>
<files>
test/theme/_fixtures/profile_fixtures.dart
test/theme/eden_theme_profile_test.dart
test/theme/eden_theme_profile_scope_test.dart
</files>
<action>
Per global TDD Playbook habit 4: hand-build fixtures FIRST as a discrete task before writing failing tests. Then write RED tests covering test-list items 1-9 + 21 (enum + scope behavior + data-lookup contract, deferring per-profile value assertions to Task 3 so this Task's RED/GREEN loop is one coherent contract per habit 3).

**File 1: `test/theme/_fixtures/profile_fixtures.dart`** — hand-built test scenario fixtures. Carries the no-LLM contract header.

```dart
// test/theme/_fixtures/profile_fixtures.dart
//
// Hand-built test fixtures for EdenThemeProfile work (objective 009).
// Do NOT regenerate via LLM — mutate in-place when the profile contract changes.
// Per global TDD Playbook habit 4 + resolver no_llm_test_data constraint.

import 'package:eden_ui_flutter/eden_ui.dart';
import 'package:flutter/material.dart';

class ProfileFixtures {
  ProfileFixtures._();

  /// Every profile in the LOCKED order from OBJECTIVE.md Constraint 11.
  /// If this list drifts from the enum definition, the contract has been broken.
  static const List<EdenThemeProfile> allProfilesInLockedOrder = [
    EdenThemeProfile.commercialWarm,
    EdenThemeProfile.medicalInstitutional,
    EdenThemeProfile.govFederal,
    EdenThemeProfile.retailVibrant,
    EdenThemeProfile.legalProfessional,
  ];

  /// Minimal MaterialApp wrapper for InheritedWidget tests.
  /// Mirror of the lib's standard `wrap()` test pattern (PROJECT.md test pattern).
  static Widget wrap(Widget child) =>
      MaterialApp(home: Scaffold(body: child));

  /// Wrapper with an EdenThemeProfileScope ancestor.
  static Widget wrapWithProfile(EdenThemeProfile profile, Widget child) =>
      MaterialApp(
        home: Scaffold(
          body: EdenThemeProfileScope(
            profile: profile,
            child: child,
          ),
        ),
      );

  /// A descendant widget that records the resolved profile from `.maybeOf`
  /// into a capture variable. Use:
  ///   EdenThemeProfile? captured;
  ///   await tester.pumpWidget(wrapWithProfile(
  ///     EdenThemeProfile.medicalInstitutional,
  ///     ProfileFixtures.captureBuilder((p) => captured = p),
  ///   ));
  ///   expect(captured, EdenThemeProfile.medicalInstitutional);
  static Widget captureBuilder(void Function(EdenThemeProfile?) onBuild) {
    return Builder(
      builder: (context) {
        onBuild(EdenThemeProfileScope.maybeOf(context));
        return const SizedBox.shrink();
      },
    );
  }
}
```

**File 2: `test/theme/eden_theme_profile_test.dart`** — enum + data lookup contract tests (test list items 1, 7, 8, 9, 21).

```dart
// test/theme/eden_theme_profile_test.dart
//
// EdenThemeProfile + EdenThemeProfileData contract tests (objective 009 TRD 01).
// Per global TDD Playbook habit 2: test-list-first; behavior cases enumerated
// in TRD 009-01-TRD.md before code was written.

import 'package:eden_ui_flutter/eden_ui.dart';
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

import '_fixtures/profile_fixtures.dart';

void main() {
  group('EdenThemeProfile enum contract', () {
    test('has exactly 5 values in the locked order (Constraint 11)', () {
      // Test list item 1 — LOCKED order.
      expect(EdenThemeProfile.values, hasLength(5),
          reason: 'OBJECTIVE.md Constraint 11 locks the profile count at 5');
      expect(EdenThemeProfile.values, [
        EdenThemeProfile.commercialWarm,
        EdenThemeProfile.medicalInstitutional,
        EdenThemeProfile.govFederal,
        EdenThemeProfile.retailVibrant,
        EdenThemeProfile.legalProfessional,
      ]);
    });

    test('commercialWarm is the first (ordinal 0) — default-by-position', () {
      expect(EdenThemeProfile.values.first, EdenThemeProfile.commercialWarm);
      expect(EdenThemeProfile.commercialWarm.index, 0);
    });
  });

  group('EdenThemeProfileData lookup contract', () {
    test('every profile has a corresponding static data instance', () {
      // Test list item 7.
      final lookup = <EdenThemeProfile, EdenThemeProfileData>{
        EdenThemeProfile.commercialWarm: EdenThemeProfileData.commercialWarmData,
        EdenThemeProfile.medicalInstitutional: EdenThemeProfileData.medicalInstitutionalData,
        EdenThemeProfile.govFederal: EdenThemeProfileData.govFederalData,
        EdenThemeProfile.retailVibrant: EdenThemeProfileData.retailVibrantData,
        EdenThemeProfile.legalProfessional: EdenThemeProfileData.legalProfessionalData,
      };
      for (final profile in EdenThemeProfile.values) {
        expect(lookup[profile], isNotNull,
            reason: 'Missing static data instance for $profile');
      }
    });

    test('each data instance carries its own profile field correctly', () {
      // Test list item 8 — self-referential profile field.
      expect(EdenThemeProfileData.commercialWarmData.profile,
          EdenThemeProfile.commercialWarm);
      expect(EdenThemeProfileData.medicalInstitutionalData.profile,
          EdenThemeProfile.medicalInstitutional);
      expect(EdenThemeProfileData.govFederalData.profile,
          EdenThemeProfile.govFederal);
      expect(EdenThemeProfileData.retailVibrantData.profile,
          EdenThemeProfile.retailVibrant);
      expect(EdenThemeProfileData.legalProfessionalData.profile,
          EdenThemeProfile.legalProfessional);
    });

    test('all 5 instances expose non-null + valid token deltas', () {
      // Test list item 9 — basic sanity.
      final instances = [
        EdenThemeProfileData.commercialWarmData,
        EdenThemeProfileData.medicalInstitutionalData,
        EdenThemeProfileData.govFederalData,
        EdenThemeProfileData.retailVibrantData,
        EdenThemeProfileData.legalProfessionalData,
      ];
      for (final data in instances) {
        expect(data.primaryColor, isNotNull,
            reason: '${data.profile}: primaryColor');
        expect(data.surfaceTonalSeed, isNotNull,
            reason: '${data.profile}: surfaceTonalSeed');
        expect(data.radiusMultiplier, greaterThan(0),
            reason: '${data.profile}: radiusMultiplier must be > 0');
        expect(data.minimumTouchTargetPx, greaterThanOrEqualTo(0),
            reason: '${data.profile}: minimumTouchTargetPx must be >= 0');
        expect(data.density, isNotNull);
      }
    });

    test('EdenThemeProfileScope const constructor compiles', () {
      // Test list item 21 — const-ness check.
      // ignore: prefer_const_constructors
      const scope = EdenThemeProfileScope(
        profile: EdenThemeProfile.commercialWarm,
        child: SizedBox.shrink(),
      );
      expect(scope.profile, EdenThemeProfile.commercialWarm);
    });
  });
}
```

**File 3: `test/theme/eden_theme_profile_scope_test.dart`** — InheritedWidget contract tests (test list items 2-6).

```dart
// test/theme/eden_theme_profile_scope_test.dart
//
// EdenThemeProfileScope InheritedWidget contract tests (objective 009 TRD 01).

import 'package:eden_ui_flutter/eden_ui.dart';
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

import '_fixtures/profile_fixtures.dart';

void main() {
  group('EdenThemeProfileScope.maybeOf', () {
    testWidgets('returns null when no scope ancestor exists', (tester) async {
      // Test list item 2.
      EdenThemeProfile? captured = EdenThemeProfile.commercialWarm; // non-null sentinel
      await tester.pumpWidget(
        ProfileFixtures.wrap(
          ProfileFixtures.captureBuilder((p) => captured = p),
        ),
      );
      expect(captured, isNull,
          reason: 'maybeOf must return null when no scope is in the ancestor chain');
    });

    testWidgets('returns the wrapped profile when scope ancestor exists', (tester) async {
      // Test list item 3.
      EdenThemeProfile? captured;
      await tester.pumpWidget(
        ProfileFixtures.wrapWithProfile(
          EdenThemeProfile.medicalInstitutional,
          ProfileFixtures.captureBuilder((p) => captured = p),
        ),
      );
      expect(captured, EdenThemeProfile.medicalInstitutional);
    });
  });

  group('EdenThemeProfileScope.of', () {
    testWidgets('returns the wrapped profile when scope is present', (tester) async {
      // Test list item 4.
      EdenThemeProfile? captured;
      await tester.pumpWidget(
        ProfileFixtures.wrapWithProfile(
          EdenThemeProfile.govFederal,
          Builder(builder: (context) {
            captured = EdenThemeProfileScope.of(context);
            return const SizedBox.shrink();
          }),
        ),
      );
      expect(captured, EdenThemeProfile.govFederal);
    });

    testWidgets('asserts in debug when no scope is present', (tester) async {
      // Test list item 5 — assert-in-debug branch.
      // Capture the assertion failure with FlutterError.
      await tester.pumpWidget(
        ProfileFixtures.wrap(
          Builder(builder: (context) {
            // In debug, this should throw AssertionError.
            // In release, it would return commercialWarm — but `flutter test`
            // runs in debug so the assert WILL fire.
            expect(() => EdenThemeProfileScope.of(context),
                throwsAssertionError);
            return const SizedBox.shrink();
          }),
        ),
      );
    });
  });

  group('EdenThemeProfileScope rebuild behavior', () {
    testWidgets('descendants rebuild when wrapped profile changes', (tester) async {
      // Test list item 6 — updateShouldNotify true on profile change.
      final captures = <EdenThemeProfile?>[];
      Widget app(EdenThemeProfile profile) => MaterialApp(
        home: Scaffold(
          body: EdenThemeProfileScope(
            profile: profile,
            child: ProfileFixtures.captureBuilder((p) => captures.add(p)),
          ),
        ),
      );

      await tester.pumpWidget(app(EdenThemeProfile.commercialWarm));
      expect(captures.last, EdenThemeProfile.commercialWarm);

      await tester.pumpWidget(app(EdenThemeProfile.medicalInstitutional));
      expect(captures.last, EdenThemeProfile.medicalInstitutional,
          reason: 'Descendant must rebuild when profile changes');
    });

    testWidgets('descendants do NOT rebuild when profile is unchanged', (tester) async {
      // Test list item 6 (negative) — updateShouldNotify false on same profile.
      // We approximate "did not rebuild" by counting capture invocations.
      var captureCount = 0;
      Widget app() => MaterialApp(
        home: Scaffold(
          body: EdenThemeProfileScope(
            profile: EdenThemeProfile.commercialWarm,
            child: ProfileFixtures.captureBuilder((_) => captureCount++),
          ),
        ),
      );

      await tester.pumpWidget(app());
      final initialCount = captureCount;
      // Force a rebuild of the root, but the scope value is unchanged.
      // Note: Builder always rebuilds when its parent does. The right assertion
      // is that updateShouldNotify returns false — which we check directly:
      const a = EdenThemeProfileScope(
        profile: EdenThemeProfile.commercialWarm,
        child: SizedBox.shrink(),
      );
      const b = EdenThemeProfileScope(
        profile: EdenThemeProfile.commercialWarm,
        child: SizedBox.shrink(),
      );
      expect(a.updateShouldNotify(b), isFalse,
          reason: 'Same profile must NOT notify');
      const c = EdenThemeProfileScope(
        profile: EdenThemeProfile.medicalInstitutional,
        child: SizedBox.shrink(),
      );
      expect(a.updateShouldNotify(c), isTrue,
          reason: 'Different profile must notify');
      expect(captureCount, equals(initialCount + 0),
          reason: 'Sanity — initial pump occurred at least once');
    });
  });
}
```

# CRITICAL: These tests reference symbols that do NOT yet exist (`EdenThemeProfile`, `EdenThemeProfileData`, `EdenThemeProfileScope`). This is intentional — RED phase per Iron Law.
# GOTCHA: `EdenThemeProfile` is exported from `lib/eden_ui.dart` only AFTER Task 2 updates the barrel. Tests import from `package:eden_ui_flutter/eden_ui.dart` so the barrel export gate also covers compilation.
# PATTERN: Fixture file uses `static const` collections + builder closures. Plain Dart, no library-widget construction inside fixtures (only inside `wrap()` / `wrapWithProfile()` which is helper code, not data).
</action>
<verify>
- `flutter test test/theme/` — ALL tests in the two test files fail with "undefined name" / "method not found" errors. This is the RED phase — failures confirm the test contract is in place before any production code exists.
- `flutter analyze test/theme/_fixtures/profile_fixtures.dart` — fails (referenced types don't exist yet). Acceptable RED-phase state.
- Fixture file has the `// Do NOT regenerate via LLM` header.
</verify>
<done>
3 test files exist. All tests FAIL because the production types don't exist yet. Fixture file carries the no-LLM header.

Commit: `test(009-01): add failing tests for EdenThemeProfile enum + scope contract`
</done>
<recovery>
If `flutter analyze` is too noisy to spot the intended failures (e.g., other test files in the suite ALSO fail because they import a symbol you haven't yet defined): scope `flutter test test/theme/` strictly to the new directory. If a pre-existing test file already references `EdenThemeProfile` from an in-flight parallel executor, surface the collision in the deviation log and proceed.
</recovery>
</task>

<task type="auto" tdd="true">
<name>Task 2 — GREEN: implement EdenThemeProfile enum + EdenThemeProfileData + EdenThemeProfileScope</name>
<files>
lib/src/theme/eden_theme_profile.dart
lib/src/theme/eden_theme_profile_scope.dart
lib/eden_ui.dart
</files>
<action>
Write the MINIMUM production code to flip the Task 1 RED tests to GREEN. Implements test-list items 1-9 + 21. Per-profile value assertions (items 10-20) are Task 3's RED/GREEN loop — Task 2 only needs to make Task 1's tests pass.

**File 1: `lib/src/theme/eden_theme_profile.dart`**

```dart
import 'package:flutter/material.dart';
import '../tokens/colors.dart';

/// Five canonical Eden vertical theme profiles.
///
/// **LOCKED per OBJECTIVE.md Constraint 11.** Do not add, remove, or rename.
/// Profile-specific data values live in [EdenThemeProfileData].
///
/// Default-by-position: [commercialWarm] is ordinal 0 — it is the back-compat
/// anchor and reproduces today's `EdenTheme.light()` output exactly.
enum EdenThemeProfile {
  /// Default — today's behavior. Warm gold on neutral slate.
  /// Salon, retail mid-market, consumer-tilt verticals.
  commercialWarm,

  /// Teal/cyan institutional. Sharper corners, less shadow, IBM Plex Sans body.
  /// Medical, healthtech.
  medicalInstitutional,

  /// Federal navy + USWDS conformance. Public Sans, 4pt radii,
  /// border-only surfaces, ≥48pt touch floor. DHHS / DOD / federal civilian.
  govFederal,

  /// High-saturation primary + brand-color totals + cart accents.
  /// Mass-market retail, Shopify POS-style brand expression.
  retailVibrant,

  /// Navy primary + serif display opt-in + restrained accent.
  /// Traditional law firm gravitas.
  legalProfessional,
}

/// Per-profile density. Declared for forward-compatibility; v1 does NOT
/// wire density to spacing tokens (OBJECTIVE.md Constraint 12). All 5
/// v1 profile data instances use [comfortable].
enum EdenThemeProfileDensity {
  comfortable,
  dense,
}

/// Immutable per-profile token deltas. Const-able.
///
/// Static instances per profile (e.g. [commercialWarmData]) carry the
/// canonical values from `VERTICAL_UX_RESEARCH_2026-05-16.md` §2.4.2.
///
/// **Back-compat lock:** [commercialWarmData] MUST reproduce today's
/// behavior — any drift breaks OBJECTIVE.md Constraint 1.
@immutable
class EdenThemeProfileData {
  const EdenThemeProfileData({
    required this.profile,
    required this.primaryColor,
    required this.surfaceTonalSeed,
    this.radiusMultiplier = 1.0,
    this.density = EdenThemeProfileDensity.comfortable,
    this.minimumTouchTargetPx = 0,
    this.bodyFontFamily,
    this.displayFontFamily,
    this.monoFontFamily,
    this.preferBorderOverShadow = false,
  });

  final EdenThemeProfile profile;
  final MaterialColor primaryColor;
  final Color surfaceTonalSeed;
  final double radiusMultiplier;
  final EdenThemeProfileDensity density;
  final double minimumTouchTargetPx;
  final String? bodyFontFamily;
  final String? displayFontFamily;
  final String? monoFontFamily;
  final bool preferBorderOverShadow;

  // ---------------------------------------------------------------------------
  // Canonical per-profile static instances.
  //
  // Hex literals are used for `surfaceTonalSeed` (not MaterialColor[shade]!
  // lookups) because MaterialColor[X] is NOT a const expression. The hex
  // values MUST match the corresponding shade in lib/src/tokens/colors.dart.
  //
  // Do NOT "simplify" the hex literals back into EdenColors lookups — it
  // breaks const-ness and the back-compat contract.
  // ---------------------------------------------------------------------------

  /// Back-compat anchor — reproduces today's `EdenTheme.light()` output.
  /// OBJECTIVE.md Constraint 1.
  static const EdenThemeProfileData commercialWarmData = EdenThemeProfileData(
    profile: EdenThemeProfile.commercialWarm,
    primaryColor: EdenColors.gold,
    surfaceTonalSeed: Color(0xFFFAFAFA), // == EdenColors.neutral[50]
    // radiusMultiplier 1.0, density comfortable, touch 0, all fonts null,
    // preferBorderOverShadow false — all defaults.
  );

  /// Teal/cyan institutional. Sharper corners (lg 12 → 8 via 0.667 multiplier).
  /// IBM Plex Sans body. Border-over-shadow.
  static const EdenThemeProfileData medicalInstitutionalData = EdenThemeProfileData(
    profile: EdenThemeProfile.medicalInstitutional,
    primaryColor: EdenColors.cyan,
    surfaceTonalSeed: Color(0xFFF0FDFA), // teal-50 approximation
    radiusMultiplier: 0.667, // 12pt lg → 8pt
    bodyFontFamily: 'IBM Plex Sans',
    preferBorderOverShadow: true,
  );

  /// Federal navy + USWDS conformance. 4pt radii (multiplier 0.333),
  /// Public Sans for body AND display, ≥48pt touch floor, border-over-shadow.
  static const EdenThemeProfileData govFederalData = EdenThemeProfileData(
    profile: EdenThemeProfile.govFederal,
    primaryColor: EdenColors.blue, // .blue[900] == #1E3A8A (federal navy)
    surfaceTonalSeed: Color(0xFFF8FAFC), // slate-50
    radiusMultiplier: 0.333, // 12pt lg → 4pt
    minimumTouchTargetPx: 48,
    bodyFontFamily: 'Public Sans',
    displayFontFamily: 'Public Sans',
    preferBorderOverShadow: true,
  );

  /// High-saturation primary. Retail / POS / Shopify-style brand expression.
  static const EdenThemeProfileData retailVibrantData = EdenThemeProfileData(
    profile: EdenThemeProfile.retailVibrant,
    primaryColor: EdenColors.purple,
    surfaceTonalSeed: Color(0xFFFAFAFA),
    // radiusMultiplier 1.0 — same as commercial.
  );

  /// Navy primary + restrained palette + Crimson Pro display headers (opt-in).
  /// Legal practice management.
  static const EdenThemeProfileData legalProfessionalData = EdenThemeProfileData(
    profile: EdenThemeProfile.legalProfessional,
    primaryColor: EdenColors.slate,
    surfaceTonalSeed: Color(0xFFF8FAFC),
    displayFontFamily: 'Crimson Pro',
  );
}

/// Convenience extension to look up the canonical [EdenThemeProfileData]
/// for any [EdenThemeProfile].
extension EdenThemeProfileDataLookup on EdenThemeProfile {
  EdenThemeProfileData get data {
    switch (this) {
      case EdenThemeProfile.commercialWarm:
        return EdenThemeProfileData.commercialWarmData;
      case EdenThemeProfile.medicalInstitutional:
        return EdenThemeProfileData.medicalInstitutionalData;
      case EdenThemeProfile.govFederal:
        return EdenThemeProfileData.govFederalData;
      case EdenThemeProfile.retailVibrant:
        return EdenThemeProfileData.retailVibrantData;
      case EdenThemeProfile.legalProfessional:
        return EdenThemeProfileData.legalProfessionalData;
    }
  }
}
```

**File 2: `lib/src/theme/eden_theme_profile_scope.dart`**

```dart
import 'package:flutter/widgets.dart';
import 'eden_theme_profile.dart';

/// InheritedWidget exposing the active [EdenThemeProfile] to descendants.
///
/// Typically installed by [EdenAdaptiveTheme] (objective 009 TRD 05) at the
/// top of the widget tree. Widgets read the active profile via
/// [EdenThemeProfileScope.of] (debug-asserts on missing scope) or
/// [maybeOf] (returns null on missing scope).
///
/// Per OBJECTIVE.md Constraint 7: profile selection is a global per-tenant
/// concern. There is no page-level override API — widgets adapt automatically
/// via their resolved theme tokens. This scope exists for theme-construction
/// helpers that need to read the profile without re-receiving it as a prop.
class EdenThemeProfileScope extends InheritedWidget {
  const EdenThemeProfileScope({
    super.key,
    required this.profile,
    required super.child,
  });

  final EdenThemeProfile profile;

  /// Returns the profile from the nearest ancestor [EdenThemeProfileScope].
  ///
  /// Asserts in debug if no scope is present. Defaults to
  /// [EdenThemeProfile.commercialWarm] in release builds (back-compat — apps
  /// not yet wrapped in [EdenAdaptiveTheme] continue to render commercial).
  static EdenThemeProfile of(BuildContext context) {
    final scope = context
        .dependOnInheritedWidgetOfExactType<EdenThemeProfileScope>();
    assert(
      scope != null,
      'EdenThemeProfileScope.of() called without an EdenThemeProfileScope '
      'ancestor. Wrap your app in EdenAdaptiveTheme (objective 009) or use '
      'EdenThemeProfileScope.maybeOf if a missing scope is acceptable.',
    );
    return scope?.profile ?? EdenThemeProfile.commercialWarm;
  }

  /// Returns the profile from the nearest ancestor scope, or null if none.
  static EdenThemeProfile? maybeOf(BuildContext context) {
    return context
        .dependOnInheritedWidgetOfExactType<EdenThemeProfileScope>()
        ?.profile;
  }

  @override
  bool updateShouldNotify(EdenThemeProfileScope oldWidget) {
    return profile != oldWidget.profile;
  }
}
```

**File 3: `lib/eden_ui.dart`** — APPEND a new section header + 2 exports at the end of the file (do NOT modify existing exports).

```dart
// ─────────── Objective 009 — Vertical Theme System ───────────
export 'src/theme/eden_theme_profile.dart';
export 'src/theme/eden_theme_profile_scope.dart';
```

# CRITICAL: Append to the END of lib/eden_ui.dart, after the existing `// Utils` section. Coordinated with other in-flight objectives — use a unique section header so parallel executors don't collide.
# PATTERN: Mirror the existing `// Objective NNN — section name ───────────` divider style already in eden_ui.dart (e.g., `// ─────────── Objective 006 — Process canvas Wave 1 ───────────`).
# GOTCHA: When implementing the static const data instances, every parameter that has a non-default value must be a const expression. EdenColors.gold IS const (verified by reading colors.dart). Color(0xFFFAFAFA) IS const. So the static const constructor calls are valid.
# GOTCHA: The `assert(scope != null, ...)` in `.of` only fires in debug. The `??` fallback handles release builds. Both branches are tested in Task 1.
</action>
<verify>
- `flutter test test/theme/eden_theme_profile_test.dart test/theme/eden_theme_profile_scope_test.dart` — ALL tests from Task 1 pass GREEN.
- `flutter analyze lib/src/theme/eden_theme_profile.dart lib/src/theme/eden_theme_profile_scope.dart lib/eden_ui.dart` — 0 issues.
- `flutter test` — full suite passes (existing tests untouched).
- `grep -n "Objective 009" lib/eden_ui.dart` — section header present.
</verify>
<done>
EdenThemeProfile enum + EdenThemeProfileData + EdenThemeProfileScope implemented. Task 1's tests GREEN. Existing test suite unaffected. lib/eden_ui.dart has the new section header + 2 exports.

Commit: `feat(009-01): implement EdenThemeProfile enum + data + InheritedWidget scope`
</done>
<recovery>
If `flutter analyze` complains "Const constructors can't have body" or "Not a constant expression": one of the static const data instances has a non-const default. Check that every cited Color/MaterialColor literal is const-eligible.
If the assert-in-debug test from Task 1 throws something other than AssertionError (e.g., NoSuchMethodError): the assert message is firing but a downstream `.profile` access is also failing. Confirm the `??` fallback returns commercialWarm in release-shape — the assert and fallback are separate concerns.
If parallel-executor collisions on lib/eden_ui.dart appear (another objective appended to the same area): resolve by keeping both section headers, ordered by objective number.
</recovery>
</task>

<task type="auto" tdd="true">
<name>Task 3 — RED→GREEN: back-compat anchor + per-profile data correctness tests</name>
<files>
test/theme/eden_theme_profile_test.dart
</files>
<action>
Add the per-profile data-value tests (test list items 10-20) to the existing test file. These tests already pass against the Task 2 implementation (because Task 2 wrote the correct values per the locked table), but per global TDD Playbook habit 3 (one test at a time) we treat them as a separate RED→GREEN loop — we write the assertions, run them, confirm green, and commit as a distinct test commit BEFORE moving on. This separation matters because the back-compat anchor (item 10-16) is a CONTRACT — landing it as its own commit makes future bisects easy.

**APPEND** these groups to `test/theme/eden_theme_profile_test.dart` (do NOT replace existing groups):

```dart
  group('commercialWarm back-compat anchor (Constraint 1)', () {
    // The CRITICAL group. Drift here breaks back-compat with today's behavior.
    // If any of these fail: roll back the offending change. Do NOT relax the test.
    test('primaryColor identity-equals EdenColors.gold', () {
      // Test list item 10.
      expect(
        identical(
          EdenThemeProfileData.commercialWarmData.primaryColor,
          EdenColors.gold,
        ),
        isTrue,
        reason: 'commercialWarm.primaryColor MUST be EdenColors.gold — back-compat lock',
      );
    });

    test('surfaceTonalSeed hex matches EdenColors.neutral[50] (0xFFFAFAFA)', () {
      // Test list item 11.
      expect(
        EdenThemeProfileData.commercialWarmData.surfaceTonalSeed.value,
        0xFFFAFAFA,
        reason: 'commercialWarm.surfaceTonalSeed MUST equal neutral[50] hex',
      );
    });

    test('radiusMultiplier == 1.0', () {
      // Test list item 12.
      expect(EdenThemeProfileData.commercialWarmData.radiusMultiplier, 1.0);
    });

    test('minimumTouchTargetPx == 0', () {
      // Test list item 13.
      expect(EdenThemeProfileData.commercialWarmData.minimumTouchTargetPx, 0);
    });

    test('all font families are null (use defaults)', () {
      // Test list item 14.
      expect(EdenThemeProfileData.commercialWarmData.bodyFontFamily, isNull);
      expect(EdenThemeProfileData.commercialWarmData.displayFontFamily, isNull);
      expect(EdenThemeProfileData.commercialWarmData.monoFontFamily, isNull);
    });

    test('preferBorderOverShadow == false', () {
      // Test list item 15.
      expect(
        EdenThemeProfileData.commercialWarmData.preferBorderOverShadow,
        isFalse,
      );
    });

    test('density == comfortable', () {
      // Test list item 16.
      expect(
        EdenThemeProfileData.commercialWarmData.density,
        EdenThemeProfileDensity.comfortable,
      );
    });
  });

  group('medicalInstitutional data correctness', () {
    test('matches VERTICAL_UX_RESEARCH §2.4.2 spec', () {
      // Test list item 17.
      final data = EdenThemeProfileData.medicalInstitutionalData;
      expect(identical(data.primaryColor, EdenColors.cyan), isTrue);
      expect(data.radiusMultiplier, closeTo(0.667, 0.001),
          reason: '12pt lg → 8pt');
      expect(data.bodyFontFamily, 'IBM Plex Sans');
      expect(data.preferBorderOverShadow, isTrue);
    });
  });

  group('govFederal data correctness', () {
    test('matches USWDS approximation spec', () {
      // Test list item 18.
      final data = EdenThemeProfileData.govFederalData;
      expect(identical(data.primaryColor, EdenColors.blue), isTrue,
          reason: 'blue[900] == #1E3A8A federal navy');
      expect(data.radiusMultiplier, closeTo(0.333, 0.001),
          reason: '12pt lg → 4pt');
      expect(data.minimumTouchTargetPx, 48.0,
          reason: 'USWDS ≥48pt touch floor');
      expect(data.bodyFontFamily, 'Public Sans');
      expect(data.displayFontFamily, 'Public Sans');
      expect(data.preferBorderOverShadow, isTrue);
    });
  });

  group('retailVibrant data correctness', () {
    test('matches Shopify POS brand-expression spec', () {
      // Test list item 19.
      final data = EdenThemeProfileData.retailVibrantData;
      expect(identical(data.primaryColor, EdenColors.purple), isTrue);
      expect(data.radiusMultiplier, 1.0);
      expect(data.bodyFontFamily, isNull);
      expect(data.displayFontFamily, isNull);
      expect(data.monoFontFamily, isNull);
    });
  });

  group('legalProfessional data correctness', () {
    test('matches Clio-style legal-gravitas spec', () {
      // Test list item 20.
      final data = EdenThemeProfileData.legalProfessionalData;
      expect(identical(data.primaryColor, EdenColors.slate), isTrue);
      expect(data.displayFontFamily, 'Crimson Pro');
      expect(data.bodyFontFamily, isNull,
          reason: 'Body stays Plus Jakarta default — only display goes serif');
    });
  });

  group('EdenThemeProfileDataLookup extension', () {
    test('.data getter returns correct instance for each profile', () {
      // Cross-check the extension getter against the static instances.
      expect(EdenThemeProfile.commercialWarm.data,
          same(EdenThemeProfileData.commercialWarmData));
      expect(EdenThemeProfile.medicalInstitutional.data,
          same(EdenThemeProfileData.medicalInstitutionalData));
      expect(EdenThemeProfile.govFederal.data,
          same(EdenThemeProfileData.govFederalData));
      expect(EdenThemeProfile.retailVibrant.data,
          same(EdenThemeProfileData.retailVibrantData));
      expect(EdenThemeProfile.legalProfessional.data,
          same(EdenThemeProfileData.legalProfessionalData));
    });
  });
```

# CRITICAL: These tests should PASS immediately because Task 2 wrote the correct data values. The point of running them as a separate task is the COMMIT — landing the back-compat anchor as its own commit makes future regressions traceable to a single SHA. Per Playbook habit 3 (one test at a time) we keep the loop atomic.
# GOTCHA: `identical(...)` (NOT `==`) is used for MaterialColor checks — we want SAME OBJECT, not value-equal. EdenColors.gold is a static const, so identity check confirms zero copy or substitution.
# GOTCHA: `closeTo(0.667, 0.001)` for the radiusMultiplier — floating-point literal 0.667 actually stores as 0.66700000000000004 (etc.); use closeTo with epsilon 0.001 for stable comparison.
</action>
<verify>
- `flutter test test/theme/eden_theme_profile_test.dart` — ALL groups (including the 7 new groups added in this task) pass.
- `flutter test test/theme/` — full theme directory passes.
- `flutter test` — full library suite passes.
- `flutter analyze` — 0 issues.
</verify>
<done>
Back-compat anchor tests + per-profile data-correctness tests added and all GREEN. The commercialWarm back-compat lock is now codified in test form — any future drift in the data values will be caught by these tests.

Commit: `test(009-01): lock commercialWarm back-compat anchor + per-profile data assertions`
</done>
<recovery>
If the `identical(data.primaryColor, EdenColors.gold)` test fails: somewhere a copy or substitution snuck in. Check that the static const data instance literally references `EdenColors.gold` and not a derived MaterialColor.
If the `surfaceTonalSeed.value == 0xFFFAFAFA` test fails: verify Color(0xFFFAFAFA) in the data instance and confirm against `lib/src/tokens/colors.dart` neutral[50] hex. Drift here = back-compat break — investigate at the source.
If the `radiusMultiplier closeTo(0.667)` test fails with a value of 0.666...: the literal in Task 2 was probably written as `2/3` (which is fine since both evaluate equally), but the epsilon should catch this. If it's 0.5 or some other value, the multiplier was mis-typed.
</recovery>
</task>

</tasks>

<verification_commands>
- `flutter test test/theme/` — all tests pass (≥20 tests across 2 files)
- `flutter test` — full library suite passes (no regression in 1041 existing tests)
- `flutter analyze` — 0 issues
- `grep -c 'Objective 009' lib/eden_ui.dart` — at least 1 (the section header)
- `grep -l 'Do NOT regenerate via LLM' test/theme/_fixtures/profile_fixtures.dart` — 1 (header present)
- `git log --oneline -5` — shows 3 commits in `test:` → `feat:` → `test:` order per TDD cadence (RED → GREEN → CONTRACT-LOCK)
</verification_commands>

<validation_gates>
- `flutter analyze` — 0 issues, 0 warnings
- `flutter test` — full suite passes
- `just lint` — passes from eden-libs/ workspace root
- `just check` — passes (full pre-commit gate)
</validation_gates>
